# Kotest - Proptest

**Pages:** 169

---

## Custom Generators | Kotest

**URL:** https://kotest.io/docs/proptest/custom-generators.html

**Contents:**
- Custom Generators
  - Arbitrary​
  - Exhaustive​

To write your own generator for a type T, you just create an instance of Arb<T> or Exhaustive<T>.

When writing a custom arbitrary we can use the arbitrary builder which accepts a lambda that must return the type we are generating for. The parameter to this lambda is a RandomSource parameter which contains the seed and the Random instance. We should typically use the provided RandomSource if we need access to a kotlin.Random instance, as this instance will have been seeded by the framework to allow for repeatable tests.

For example, here is a custom arb that generates a random int between 3 and 6 using the arbitrary builder.

In addition to the RandomSource parameter, the arbitrary builder lambda also provides the ArbitraryBuilderSyntax context which we can leverage to compose other arbitraries when building ours.

For example, here is an Arbitrary that supports a custom class called Person, delegating to a String arbitrary and an Int arbitrary.

The resulting arbitrary produced using this syntax is equivalent to using map, flatMap and bind.

When writing a custom exhaustive we can use the exhaustive() extension function on a List. Nothing more to it than that really!

**Examples:**

Example 1 (kotlin):
```kotlin
val sillyArb = arbitrary { rs: RandomSource ->   rs.random.nextInt(3..6)}
```

Example 2 (kotlin):
```kotlin
data class Person(val name: String, val age: Int)val personArb = arbitrary {   val name = Arb.string(10..12).bind()   val age = Arb.int(21, 150).bind()   Person(name, age)}
```

Example 3 (kotlin):
```kotlin
val singleDigitPrimes = listOf(2,3,5,7).exhaustive()
```

Example 4 (kotlin):
```kotlin
class PropertyExample: FreeSpec({    "testing single digit primes" {        checkAll(singleDigitPrimes) { prime ->           isPrime(prime) shouldBe true           isPrime(prime * prime) shouldBe false        }    }})
```

---

## Property-based Testing | Kotest

**URL:** https://kotest.io/docs/6.0/proptest/property-based-testing.html

**Contents:**
- Property-based Testing
- What is Property Testing?​
- Getting Started​
- Next Steps​

Kotest is split into several subprojects which can be used independently. One of these subprojects is the property test framework. You do not need to be using Kotest as your test framework (although you should!) to benefit from the property test support.

Developers typically write example-based tests. These are your garden variety unit tests you know and love. You provide some inputs, and some expected results, and a test framework like Kotest or JUnit checks that the actual results meet the expectations.

One problem with this approach is that it is very easy to miss errors due to edge cases that the developer didn't think about, or lack of coverage in the chosen inputs. Instead, with property testing, hundreds or thousands of values are fed into the same test, and the values are (usually) randomly generated by your property test framework.

For example, a good property test framework will include values like negative infinity, empty lists, strings with non-ascii characters, and so on. Things we often forget about when writing example based tests.

Property tests were originally conceived in frameworks like Quickcheck with the notion of testing a property on some object, ie. something that should hold true for all inputs. An invariant in other words. An example of an invariant is given two strings, a and b, then length(a + b) should always be equal to length(a) + length(b).

That is where the term property testing originates.

However, you do not have to use property tests to only test things like monad laws or basic numeric functions. Any test that would benefit from a wide array of input values is a good candidate. For example, we might have a function that validates usernames, and we want to test that valid emails are accepted. A property test would be useful here in generating 1000s of combinations to help harden our validation logic.

The property test framework is supported on all targets.

Add the following dependency to your build:

Add the following dependency to your build.

Add the following dependency to your commonTest sourceset:

Alternatively, add the dependency to a specific target. For example, we could add to the Javascript target only be using the jsTest sourceset.

To create input values for tests, Kotest uses the term generator. One generator per input argument is passed to a test function, and the test will execute for a set number of iterations.

**Examples:**

Example 1 (kotlin):
```kotlin
dependencies {   testImplementation("io.kotest:kotest-property:$version")}
```

Example 2 (xml):
```xml
<dependency>   <groupId>io.kotest</groupId>   <artifactId>kotest-property-jvm</artifactId>   <version>${version}</version>   <scope>test</scope></dependency>
```

Example 3 (kotlin):
```kotlin
kotlin {  sourceSets {    val commonTest by getting {      dependencies {        implementation("io.kotest:kotest-property:$version")      }    }  }}
```

Example 4 (kotlin):
```kotlin
kotlin {   targets {      js {         browser()         nodejs()      }   }   sourceSets {      val jsTest by getting {         dependencies {            implementation("io.kotest:kotest-property:$version")         }      }   }}
```

---

## Arrow Generators | Kotest

**URL:** https://kotest.io/docs/5.4.x/proptest/property-test-generators-arrow.html

**Contents:**
- Arrow Generators

Kotest provides an optional module that provides generators for Arrow.

To use, add io.kotest.extensions:kotest-property-arrow:versionand io.arrow-kt:arrow-core:arrow-version to your build. This holds true for the optics module kotest-property-arrow-optics, by adding io.arrow-kt:arrow-optics:arrow-version, too.

---

## Arrow Generators | Kotest

**URL:** https://kotest.io/docs/5.6.x/proptest/property-test-generators-arrow.html

**Contents:**
- Arrow Generators

Kotest provides an optional module that provides generators for Arrow.

To use, add io.kotest.extensions:kotest-property-arrow:versionand io.arrow-kt:arrow-core:arrow-version to your build. This holds true for the optics module kotest-property-arrow-optics, by adding io.arrow-kt:arrow-optics:arrow-version, too.

---

## Reflective Arbs | Kotest

**URL:** https://kotest.io/docs/5.6.x/proptest/reflective-arbs.html

**Contents:**
- Reflective Arbs

When running tests on JVM, Kotest supports generating more complex Arbs automatically. The generated Arb relies on build-in default and further reflective Arbs to populate the class parameters. If you just need to create and instance and don't need filtering you can use the class type in the checkAll/forAll calls directly. When you want to obtain the Arb to manipulate it further or filter invalid values, you can use Arb.bind with the type argument to obtain the Arb. If the required type depends on types that are not supported by default, it is possible to provide Arbs for those types in the call to Arb.bind.

Reflective binding is supported for:

**Examples:**

Example 1 (kotlin):
```kotlin
enum class Currency {  USD, GBP, EUR}class CurrencyAmount(  val amount: Long,  val currency: Currency)context("Currencies converts to EUR") { // In some spec  checkAll(Arb.bind<CurrencyAmount>().filter { it.currency != EUR }) { currencyAmount ->    val converted = currencyAmount.convertTo(EUR)    converted.currency shouldBe EUR  }}context("Converting to a currency and back yields the same amount") { // In some spec  checkAll<CurrencyAmount, Currency>() { currencyAmount, currency ->    val converted = currencyAmount.convertTo(currency).convertTo(currencyAmount.currency)    converted.currency shouldBe currencyAmount.currency  }}
```

---

## Custom Generators | Kotest

**URL:** https://kotest.io/docs/5.4.x/proptest/custom-generators.html

**Contents:**
- Custom Generators
  - Arbitrary​
  - Exhaustive​

To write your own generator for a type T, you just create an instance of Arb<T> or Exhaustive<T>.

When writing a custom arbitrary we can use the arbitrary builder which accepts a lambda that must return the type we are generating for. The parameter to this lambda is a RandomSource parameter which contains the seed and the Random instance. We should typically use the provided RandomSource if we need access to a kotlin.Random instance, as this instance will have been seeded by the framework to allow for repeatable tests.

For example, here is a custom arb that generates a random int between 3 and 6 using the arbitrary builder.

In addition to the RandomSource parameter, the arbitrary builder lambda also provides the ArbitraryBuilderSyntax context which we can leverage to compose other arbitraries when building ours.

For example, here is an Arbitrary that supports a custom class called Person, delegating to a String arbitrary and an Int arbitrary.

The resulting arbitrary produced using this syntax is equivalent to using map, flatMap and bind.

When writing a custom exhaustive we can use the exhaustive() extension function on a List. Nothing more to it than that really!

**Examples:**

Example 1 (kotlin):
```kotlin
val sillyArb = arbitrary { rs: RandomSource ->   rs.random.nextInt(3..6)}
```

Example 2 (kotlin):
```kotlin
data class Person(val name: String, val age: Int)val personArb = arbitrary {   val name = Arb.string(10..12).bind()   val age = Arb.int(21, 150).bind()   Person(name, age)}
```

Example 3 (kotlin):
```kotlin
val singleDigitPrimes = listOf(2,3,5,7).exhaustive()
```

Example 4 (kotlin):
```kotlin
class PropertyExample: StringSpec({    "testing single digit primes" {        checkAll(singleDigitPrimes) { prime ->           isPrime(prime) shouldBe true           isPrime(prime * prime) shouldBe false        }    }})
```

---

## Generators | Kotest

**URL:** https://kotest.io/docs/5.5.x/proptest/property-test-generators.html

**Contents:**
- Generators
- Arbitrary​
- Exhaustive​

Generated values are provided by instances of the sealed class Gen. You can think of a Gen as kind of like an input stream but for property test values. Each Gen will provide a (usually) infinite stream of these values for one particular type.

Kotest has two types of generators - Arb for generating arbitrary (random) values and Exhaustive for generating a finite set of values in a closed space.

Both types of gens can be mixed and matched in property tests. For example, you could test a function with 100 random positive integers (an arb) alongside every even number from 0 to 200 (exhaustive).

Some generators are only available on the JVM. See the full list here.

Arbs generate two types of values - a hard coded set of edge cases and an infinite stream of randomly chosen samples.

The samples may be repeated, and some values may never be generated at all. For example generating 1000 integers between 0 and Int.MAX cannot return all possible values, and some values may happen to be generated more than once. Similarly, generating 1000 random integers between 0 and 500 will definitely result in some values appearing more than once.

Some common arbitraries include numbers with or without a range, strings in the unicode set, random lists, data classes with random parameters, emails, codepoints, chars and so on.

In addition to the random values, arbs may provide edge cases. One of the design features of Kotest's property testing is that values for some types will always include "common" edge cases that you probably want to be included in your tests.

For example, when testing a function that accepts an integer, you probably want to ensure that, at the very least, it is tested with zero, a positive number and a negative number. If only random values were provided, the chances of zero appearing would be fairly low, so Kotest will always provide some "edge cases" for integers (unless you specify otherwise).

When executing tests, the framework will alternate randomly between samples and edge cases. The split is determined by a configuration value which defaults to 2% edge cases.

Not all arbs have edge cases, but the arbs for the most common types do. Here are some examples of edge cases used by some arbs:

Exhaustives generate all values from a given space. This is useful when you want to ensure every value in that space is used. For example, for enum values, it is usually more helpful to ensure each enum is used, rather than picking randomly from the enums values and potentially missing some and duplicating others.

Typical exhaustives include small collections, enums, boolean values, powerset of a list or set, pre-defined integer ranges, and predefined string ranges.

Once an exhaustive has provided all it's values, it will loop and start again, so an exhaustive can be used in a test that requires any number of inputs.

Here we asked for 100 iterations, so each value of Season would be provided 25 times.

**Examples:**

Example 1 (php):
```php
enum class Season { Winter, Fall, Spring, Summer }forAll<Int, Season>(100) { a, season -> ... }
```

---

## Property Test Seeds | Kotest

**URL:** https://kotest.io/docs/proptest/property-test-seeds.html

**Contents:**
- Property Test Seeds
  - Manually specifying the seed​
  - Rerunning failed seeds​
  - Failing when seeds set​

When a property test is executed, the values are generated using a random source that is created from a seed value. By default this seed value is itself randomly chosen (using the default kotlin.random.Random instance). However, there are times when this value needs to be fixed or repeated.

You can change the default used by all tests, unless overriden through the options listed below, by changing the configuration value PropertyTesting.defaultSeed.

To manually set the seed, pass an instance of PropTestConfig to your prop test methods. You might want to do this if you find a test failure, and you want to ensure that those values continue to be executed in the future as a regression test.

Whenever a property test fails, Kotest will output the seed that was used. You can duplicate the test, setting it to use this seed so you have permanent regression test for those values.

By default, when a property test fails, the seed used by that test is written to a file in ~/.kotest/seeds/<spec>/<testname>. Whenever a property test runs, this seed is detected if the file exists, and used in place of a random seed. Next time the test is successful, the seed file will be removed.

A manually specified seed always takes precedence over a failed seed.

This feature can be disabled by setting PropertyTesting.writeFailedSeed = false

Some users prefer to avoid manually specifying seeds. They want to use them locally only, when developing, but to avoid checking them in. If this is your style, then set PropertyTesting.failOnSeed = false or the env var kotest.proptest.seed.fail-if-set to false on your server.

Then if a seed is detected, the test suite will fail.

**Examples:**

Example 1 (kotlin):
```kotlin
class PropertyExample : FreeSpec({  "String size" {    forAll<String, String>(PropTestConfig(seed = 127305235)) { a, b ->      (a + b).length == a.length + b.length    }  }})
```

---

## Reflective Arbs | Kotest

**URL:** https://kotest.io/docs/5.7.x/proptest/reflective-arbs.html

**Contents:**
- Reflective Arbs

When running tests on JVM, Kotest supports generating more complex Arbs automatically. The generated Arb relies on build-in default and further reflective Arbs to populate the class parameters. If you just need to create and instance and don't need filtering you can use the class type in the checkAll/forAll calls directly. When you want to obtain the Arb to manipulate it further or filter invalid values, you can use Arb.bind with the type argument to obtain the Arb. If the required type depends on types that are not supported by default, it is possible to provide Arbs for those types in the call to Arb.bind.

Reflective binding is supported for:

**Examples:**

Example 1 (kotlin):
```kotlin
enum class Currency {  USD, GBP, EUR}class CurrencyAmount(  val amount: Long,  val currency: Currency)context("Currencies converts to EUR") { // In some spec  checkAll(Arb.bind<CurrencyAmount>().filter { it.currency != EUR }) { currencyAmount ->    val converted = currencyAmount.convertTo(EUR)    converted.currency shouldBe EUR  }}context("Converting to a currency and back yields the same amount") { // In some spec  checkAll<CurrencyAmount, Currency>() { currencyAmount, currency ->    val converted = currencyAmount.convertTo(currency).convertTo(currencyAmount.currency)    converted.currency shouldBe currencyAmount.currency  }}
```

---

## Generators List | Kotest

**URL:** https://kotest.io/docs/5.7.x/proptest/property-test-generators-list.html

**Contents:**
- Generators List

This page lists all current generators in Kotest. There are two types of generator: arbitrary and exhaustive.

Most generators are available on all platforms. Some are JVM or JS specific.

We also provide generators for Arrow as a separate module.

---

## Property Test Seeds | Kotest

**URL:** https://kotest.io/docs/5.6.x/proptest/property-test-seeds.html

**Contents:**
- Property Test Seeds
  - Manually specifying the seed​
  - Rerunning failed seeds​
  - Failing when seeds set​

When a property test is executed, the values are generated using a random source that is created from a seed value. By default this seed value is itself randomly chosen (using the default kotlin.random.Random instance). However, there are times when this value needs to be fixed or repeated.

You can change the default used by all tests, unless overriden through the options listed below, by changing the configuration value PropertyTesting.defaultSeed.

To manually set the seed, pass an instance of PropTestConfig to your prop test methods. You might want to do this if you find a test failure, and you want to ensure that those values continue to be executed in the future as a regression test.

Whenever a property test fails, Kotest will output the seed that was used. You can duplicate the test, setting it to use this seed so you have permanent regression test for those values.

By default, when a property test fails, the seed used by that test is written to a file in ~/.kotest/seeds/<spec>/<testname>. Whenever a property test runs, this seed is detected if the file exists, and used in place of a random seed. Next time the test is successful, the seed file will be removed.

A manually specified seed always takes precedence over a failed seed.

This feature can be disabled by setting PropertyTesting.writeFailedSeed = false

Some users prefer to avoid manually specifying seeds. They want to use them locally only, when developing, but to avoid checking them in. If this is your style, then set PropertyTesting.failOnSeed = false or the env var kotest.proptest.seed.fail-if-set to false on your server.

Then if a seed is detected, the test suite will fail.

**Examples:**

Example 1 (kotlin):
```kotlin
class PropertyExample : StringSpec({  "String size" {    forAll<String, String>(PropTestConfig(seed = 127305235)) { a, b ->      (a + b).length == a.length + b.length    }  }})
```

---

## Generators | Kotest

**URL:** https://kotest.io/docs/5.3.x/proptest/property-test-generators.html

**Contents:**
- Generators
- Arbitrary​
- Exhaustive​

Generated values are provided by instances of the sealed class Gen. You can think of a Gen as kind of like an input stream but for property test values. Each Gen will provide a (usually) infinite stream of these values for one particular type.

Kotest has two types of generators - Arb for generating arbitrary (random) values and Exhaustive for generating a finite set of values in a closed space.

Both types of gens can be mixed and matched in property tests. For example, you could test a function with 100 random positive integers (an arb) alongside every even number from 0 to 200 (exhaustive).

Some generators are only available on the JVM. See the full list here.

Arbs generate two types of values - a hard coded set of edge cases and an infinite stream of randomly chosen samples.

The samples may be repeated, and some values may never be generated at all. For example generating 1000 integers between 0 and Int.MAX cannot return all possible values, and some values may happen to be generated more than once. Similarly, generating 1000 random integers between 0 and 500 will definitely result in some values appearing more than once.

Some common arbitraries include numbers with or without a range, strings in the unicode set, random lists, data classes with random parameters, emails, codepoints, chars and so on.

In addition to the random values, arbs may provide edge cases. One of the design features of Kotest's property testing is that values for some types will always include "common" edge cases that you probably want to be included in your tests.

For example, when testing a function that accepts an integer, you probably want to ensure that, at the very least, it is tested with zero, a positive number and a negative number. If only random values were provided, the chances of zero appearing would be fairly low, so Kotest will always provide some "edge cases" for integers (unless you specify otherwise).

When executing tests, the framework will alternate randomly between samples and edge cases. The split is determined by a configuration value which defaults to 2% edge cases.

Not all arbs have edge cases, but the arbs for the most common types do. Here are some examples of edge cases used by some arbs:

Exhaustives generate all values from a given space. This is useful when you want to ensure every value in that space is used. For example, for enum values, it is usually more helpful to ensure each enum is used, rather than picking randomly from the enums values and potentially missing some and duplicating others.

Typical exhaustives include small collections, enums, boolean values, powerset of a list or set, pre-defined integer ranges, and predefined string ranges.

Once an exhaustive has provided all it's values, it will loop and start again, so an exhaustive can be used in a test that requires any number of inputs.

Here we asked for 100 iterations, so each value of Season would be provided 25 times.

**Examples:**

Example 1 (php):
```php
enum class Season { Winter, Fall, Spring, Summer }forAll<Int, Season>(100) { a, season -> ... }
```

---

## Assumptions | Kotest

**URL:** https://kotest.io/docs/5.8.x/proptest/property-test-assumptions.html

**Contents:**
- Assumptions
  - Assertions​
  - Max Discard Percentage​

To restrict the set the generated values from a generator, we can use filter to return a constrained arb.

However if we need to restrict inputs based on the relationship between values, then filtering on a single arb won't work. Consider an example where we want to test that two non-equal strings have a non-zero Levenshtein distance.

This will periodically fail - whenever two equal strings are generated. One approach would be to just wrap the tests in an if/else block and avoid those undesired inputs.

But in more complicated scenarios we could easily introduce a bug and filter all our inputs.

Kotest provides a feature called assumptions that will filter out unwanted combinations, while tracking that we are not filtering too many.

An assumption accepts a boolean value passed to the withAssumptions function, that if true, will allow the property test to continue, but if false, that particular iteration is skipped. For example, the previous example will now pass:

Alternatively, you can use inline syntax:

Kotest expands on basic boolean assumptions by allowing you to specify assertions in an assumption function.

For example, building on the previous example:

Here we are ensuring that all inputs are not equal, and that the inputs have the same length. Any assertion that throws AssertionError can be used here, including all the assertions provided by Kotest.

This also supports inline syntax:

By default, the maximum discard percentage is 10%. If more combinations are discarded than that, the property test will fail. This helps avoid a scenario where we erroneously discard too many, or even all, our inputs.

For example, the following would fail by default because we would be filtering ~50% of values.

But if we wanted to allow this regardless, we can use the maxDiscardPercentage to increase the allowed discard rate.

It is generally better to adjust your arbs to produce values closer to what you need, so that you only need to filter out unwanted edge cases.

**Examples:**

Example 1 (kotlin):
```kotlin
val evens = Arb.int().filter { it.value % 2 == 0 }
```

Example 2 (kotlin):
```kotlin
checkAll<String, String> { a, b ->  levenshtein(a, b) shouldBeGreaterThan 0}
```

Example 3 (kotlin):
```kotlin
checkAll<String, String> { a, b ->  if (a != b)    levenshtein(a, b) shouldBeGreaterThan 0}
```

Example 4 (kotlin):
```kotlin
checkAll<String, String> { a, b ->  withAssumptions(a != b) {    levenshtein(a, b) shouldBeGreaterThan 0  }}
```

---

## Property-based Testing | Kotest

**URL:** https://kotest.io/docs/5.8.x/proptest/property-based-testing.html

**Contents:**
- Property-based Testing
- What is Property Testing?​
- Getting Started​
- Next Steps​

Kotest is split into several subprojects which can be used independently. One of these subprojects is the property test framework. You do not need to be using Kotest as your test framework (although you should!) to benefit from the property test support.

Developers typically write example-based tests. These are your garden variety unit tests you know and love. You provide some inputs, and some expected results, and a test framework like Kotest or JUnit checks that the actual results meet the expectations.

One problem with this approach is that it is very easy to miss errors due to edge cases that the developer didn't think about, or lack of coverage in the chosen inputs. Instead, with property testing, hundreds or thousands of values are fed into the same test, and the values are (usually) randomly generated by your property test framework.

For example, a good property test framework will include values like negative infinity, empty lists, strings with non-ascii characters, and so on. Things we often forget about when writing example based tests.

Property tests were originally conceived in frameworks like Quickcheck with the notion of testing a property on some object, ie. something that should hold true for all inputs. An invariant in other words. An example of an invariant is given two strings, a and b, then length(a + b) should always be equal to length(a) + length(b).

That is where the term property testing originates.

However, you do not have to use property tests to only test things like monad laws or basic numeric functions. Any test that would benefit from a wide array of input values is a good candidate. For example, we might have a function that validates usernames, and we want to test that valid emails are accepted. A property test would be useful here in generating 1000s of combinations to help harden our validation logic.

The property test framework is supported on all targets.

Add the following dependency to your build:

Add the following dependency to your build.

Add the following dependency to your commonTest sourceset:

Alternatively, add the dependency to a specific target. For example, we could add to the Javascript target only be using the jsTest sourceset.

To create input values for tests, Kotest uses the term generator. One generator per input argument is passed to a test function, and the test will execute for a set number of iterations.

**Examples:**

Example 1 (kotlin):
```kotlin
dependencies {   testImplementation("io.kotest:kotest-property:$version")}
```

Example 2 (xml):
```xml
<dependency>   <groupId>io.kotest</groupId>   <artifactId>kotest-property-jvm</artifactId>   <version>${version}</version>   <scope>test</scope></dependency>
```

Example 3 (kotlin):
```kotlin
kotlin {  sourceSets {    val commonTest by getting {      dependencies {        implementation("io.kotest:kotest-property:$version")      }    }  }}
```

Example 4 (kotlin):
```kotlin
kotlin {   targets {      js {         browser()         nodejs()      }   }   sourceSets {      val jsTest by getting {         dependencies {            implementation("io.kotest:kotest-property:$version")         }      }   }}
```

---

## Statistics | Kotest

**URL:** https://kotest.io/docs/5.8.x/proptest/property-test-statistics.html

**Contents:**
- Statistics
- Labels​
  - Report Mode​
  - Checking Coverage of Statistics​
  - Custom Reports​

Sometimes you may like to know the types of values being generated by Kotest, to ensure that your generators are configured in the way you expect. Property test statistics are designed to fulfil this need.

The function collect is the entry point to statistcs and is used to count categories of values. We use this by invoking it inside a property test with the category we want to increment.

For example, lets say we wanted to gather statistics on the values of RoundingMode used by a BigDecimal. We would invoke checkAll as normal, passing the rounding mode to the collect function.

Now after the test is completed, Kotest will output the test name, and the counts/percentages for each category:

The category we use does not have to be an enum. It can be any object, and you can wrap in conditionals if you want more control. For example:

Sometimes you may wish to have orthogonal sets of statistics. For example, for a simple number test, we might want to confirm that a certain percentage are even numbers, and a certain percentage are negative. One way would be to have EVEN_POS, EVEN_NEG, ODD_POS, ODD_NEG:

This gives us one set of outputs:

However, as the combinations grow this will become unwieldy, so Kotest supports labelled statistics. You can think of this as distinct sets of statistics. To use labels, just pass the label name as the first arg to the collect method.

Now, Kotest will output multiple sets of statistics, with the label name in the title:

By default, statistics are printed for every property test. There are four modes which can be configured using the global configuration object PropertyTesting.

The possible options are:

If you wish to programmatically assert that certain values are being generated, then you can use the specify constraints that must be met.

For example, in our previous rounding example, we can check that at least 10% of inputs are covering HALF_DOWN, and 10% are covering FLOOR using withCoveragePercentages:

If we want to check by absolute numbers rather than percentages, we can use withCoverageCounts:

You can customize the report format, or generate reports from the raw data, by using your own instance of StatisticsReporter. This is configured via the global configuration object PropertyTesting.

**Examples:**

Example 1 (kotlin):
```kotlin
checkAll(Arb.enum<RoundingMode>(), Arb.bigDecimal()) { mode, decimal ->  collect(mode)  // test here}
```

Example 2 (yaml):
```yaml
Statistics: [collecting stats] (1000 iterations, 1 args)HALF_DOWN                                                     142 (14%)HALF_UP                                                       141 (14%)CEILING                                                       132 (13%)FLOOR                                                         122 (12%)UP                                                            119 (12%)UNNECESSARY                                                   119 (12%)HALF_EVEN                                                     118 (12%)DOWN                                                          107 (11%)
```

Example 3 (kotlin):
```kotlin
checkAll(Arb.int()) { k ->  when {    k % 2 == 0 -> collect("EVEN")    else -> collect("ODD")  }  // test here}
```

Example 4 (kotlin):
```kotlin
checkAll(Arb.int()) { k ->  when {    k > 0 && k % 2 == 0 -> collect("EVEN_POS")    k % 2 == 0 -> collect("EVEN_NEG")    k > 0 -> collect("ODD_POS")    else -> collect("ODD_NEG")  }  // test here}
```

---

## Property Test Functions | Kotest

**URL:** https://kotest.io/docs/5.8.x/proptest/property-test-functions.html

**Contents:**
- Property Test Functions
  - For All​
  - Check All​
  - Iterations​
  - Specifying Generators​

There are two variants of functions that are used to execute a property test in Kotest: forAll and checkAll.

The first, forAll, accepts an n-arity function (a, ..., n) -> Boolean that tests the property. The test will pass if, for all input values, the function returns true.

Notice that this functions accepts type parameters for the argument types, with arity up to 14. Kotest uses these type parameters to locate a generator which provides (generates) random values of a suitable type.

For example, forAll<String, Int, Boolean> { a, b, c -> } is a 3-arity property test where argument a is a random String, argument b is a random int, and argument c is a random boolean.

The second, checkAll, accepts an n-arity function (a, ..., n) -> Unit in which you can simply execute assertions against the inputs. This approach will consider a test valid if no exceptions are thrown. Here is the same example again written in the equivalent way using checkAll.

The second approach is more general purpose than returning a boolean, but the first approach is from the original haskell libraries that inspired this library.

By default, Kotest will run the property test 1000 times. We can easily customize this by specifying the iteration count when invoking the test method.

Let's say we want to run a test 10,000 times.

You saw in the previous examples that Kotest would provide values automatically based on the type parameter(s). It does this by locating a generator that generates values for the required type.

For example, the automatically provided Integer generator generates random ints from all possible values - negative, positive, infinities, zero and so on.

This is fine for basic tests but often we want more control over the sample space. For example, we may want to test a function for numbers in a certain range only.

Then you would need to specify the generator(s) manually.

You can see we created two tests and in each test passed a generator into the forAll function with a suitable int range.

See here for a list of the built in generators.

**Examples:**

Example 1 (kotlin):
```kotlin
class PropertyExample: StringSpec({   "String size" {      forAll<String, String> { a, b ->         (a + b).length == a.length + b.length      }   }})
```

Example 2 (kotlin):
```kotlin
class PropertyExample: StringSpec({   "String size" {      checkAll<String, String> { a, b ->         a + b shouldHaveLength a.length + b.length      }   }})
```

Example 3 (kotlin):
```kotlin
class PropertyExample: StringSpec({   "a many iterations test" {      checkAll<Double, Double>(10_000) { a, b ->         // test here      }   }})
```

Example 4 (kotlin):
```kotlin
class PropertyExample: StringSpec({   "is allowed to drink in Chicago" {      forAll(Arb.int(21..150)) { a ->         isDrinkingAge(a) // assuming some function that calculates if we're old enough to drink      }   }   "is allowed to drink in London" {      forAll(Arb.int(18..150)) { a ->         isDrinkingAge(a) // assuming some function that calculates if we're old enough to drink      }   }})
```

---

## Generator Operations | Kotest

**URL:** https://kotest.io/docs/proptest/generator-operations.html

**Contents:**
- Generator Operations
- Next​
- Filter​
- Map​
- FlatMap​
- Merging​
- Bind​

If you want to use an Arb to just return a value (even outside of a property test), then you can call next on it.

If you have an arb and you want to create a new arb that provides a subset of values, you can call filter on the source arb. For example, one way of generating even numbers is to take the integer arb, and filter out odd values. Viz:

If you have an arb and you want to transform the value generated, you can use map.

If you have an arb whose emission or edge cases depends on the emission of the previous arbitraries, you can use flatMap.

Two generators can be merged together, so that the resulting elements are equally sampled from both generators.

So with the following example would have an equal chance to yield either "a" or "b" on each random sample:

For merging more than two arbitraries, Arb.choice or Arb.choose might be more appropriate. For instance we can use:

Bind is useful if you want to apply multiple arbitraries. We can take a look at how we might construct values for a data class using bind.

**Examples:**

Example 1 (kotlin):
```kotlin
val arbA: Arb<A> = ...val a = arbA.next() // use Random.Defaultval a2 = arbA.next(rs) // pass in Random
```

Example 2 (kotlin):
```kotlin
val evens = Arb.int().filter { it.value % 2 == 0 }val odds = Arb.int().filter { it.value % 2 == 1 }
```

Example 3 (kotlin):
```kotlin
val integerStrings: Arb<String> = Arb.int().map { it.toString() }
```

Example 4 (kotlin):
```kotlin
val dependentArbs: Arb<String> = Arb.of("foo", "bar").flatMap { prefix ->   Arb.int(1..10).map { integer ->      "${prefix}-${integer}"   }}
```

---

## Generators | Kotest

**URL:** https://kotest.io/docs/5.7.x/proptest/property-test-generators.html

**Contents:**
- Generators
- Arbitrary​
- Exhaustive​

Generated values are provided by instances of the sealed class Gen. You can think of a Gen as kind of like an input stream but for property test values. Each Gen will provide a (usually) infinite stream of these values for one particular type.

Kotest has two types of generators - Arb for generating arbitrary (random) values and Exhaustive for generating a finite set of values in a closed space.

Both types of gens can be mixed and matched in property tests. For example, you could test a function with 100 random positive integers (an arb) alongside every even number from 0 to 200 (exhaustive).

Some generators are only available on the JVM. See the full list here.

Arbs generate two types of values - a hard coded set of edge cases and an infinite stream of randomly chosen samples.

The samples may be repeated, and some values may never be generated at all. For example generating 1000 integers between 0 and Int.MAX cannot return all possible values, and some values may happen to be generated more than once. Similarly, generating 1000 random integers between 0 and 500 will definitely result in some values appearing more than once.

Some common arbitraries include numbers with or without a range, strings in the unicode set, random lists, data classes with random parameters, emails, codepoints, chars and so on.

In addition to the random values, arbs may provide edge cases. One of the design features of Kotest's property testing is that values for some types will always include "common" edge cases that you probably want to be included in your tests.

For example, when testing a function that accepts an integer, you probably want to ensure that, at the very least, it is tested with zero, a positive number and a negative number. If only random values were provided, the chances of zero appearing would be fairly low, so Kotest will always provide some "edge cases" for integers (unless you specify otherwise).

When executing tests, the framework will alternate randomly between samples and edge cases. The split is determined by a configuration value which defaults to 2% edge cases.

Not all arbs have edge cases, but the arbs for the most common types do. Here are some examples of edge cases used by some arbs:

Exhaustives generate all values from a given space. This is useful when you want to ensure every value in that space is used. For example, for enum values, it is usually more helpful to ensure each enum is used, rather than picking randomly from the enums values and potentially missing some and duplicating others.

Typical exhaustives include small collections, enums, boolean values, powerset of a list or set, pre-defined integer ranges, and predefined string ranges.

Once an exhaustive has provided all it's values, it will loop and start again, so an exhaustive can be used in a test that requires any number of inputs.

Here we asked for 100 iterations, so each value of Season would be provided 25 times.

**Examples:**

Example 1 (php):
```php
enum class Season { Winter, Fall, Spring, Summer }forAll<Int, Season>(100) { a, season -> ... }
```

---

## Global Configuration | Kotest

**URL:** https://kotest.io/docs/5.8.x/proptest/property-test-global-config.html

**Contents:**
- Global Configuration
  - Default Iterations​
  - Printing Shrink Steps​

Some property test settings can be set globally for all property tests.

The standard default iteration count is 1000. This means when you don't specify the iteration count in a property test, the default will be 1000.

We can override this default either by assigning a value to PropertyTesting.defaultIterationCount, or by using the system property kotest.proptest.default.iteration.count.

Any test which directly sets the iteration count will of course use that value.

If you are using Kotest framework, then you can perform this action before any tests by using project config. For example:

By default, when using shrinking, each shrinking step will not be logged, but only the final shrunk value.

To enable logging of each intermediate value, assign true to PropertyTesting.shouldPrintShrinkSteps or use the system property kotest.proptest.output.shrink-steps=true.

**Examples:**

Example 1 (kotlin):
```kotlin
PropertyTesting.defaultIterationCount = 123// will use 555 iterations specified in the testforAll<String, String>(555) { a,b -> a + b == "$a$b" }// will use 123 iterations from the global defaultforAll<String, String> { a,b -> a + b == "$a$b" }
```

Example 2 (kotlin):
```kotlin
class KotestConfig : AbstractProjectConfig() {   override suspend fun beforeProject() {      PropertyTesting.defaultIterationCount = 123   }}
```

---

## Generators List | Kotest

**URL:** https://kotest.io/docs/proptest/property-test-generators-list.html

**Contents:**
- Generators List

This page lists all current generators in Kotest. There are two types of generator: arbitrary and exhaustive.

Most generators are available on all platforms. Some are JVM or JS specific.

We also provide generators for Arrow as a separate module.

---

## Generators | Kotest

**URL:** https://kotest.io/docs/5.8.x/proptest/property-test-generators.html

**Contents:**
- Generators
- Arbitrary​
- Exhaustive​

Generated values are provided by instances of the sealed class Gen. You can think of a Gen as kind of like an input stream but for property test values. Each Gen will provide a (usually) infinite stream of these values for one particular type.

Kotest has two types of generators - Arb for generating arbitrary (random) values and Exhaustive for generating a finite set of values in a closed space.

Both types of gens can be mixed and matched in property tests. For example, you could test a function with 100 random positive integers (an arb) alongside every even number from 0 to 200 (exhaustive).

Some generators are only available on the JVM. See the full list here.

Arbs generate two types of values - a hard coded set of edge cases and an infinite stream of randomly chosen samples.

The samples may be repeated, and some values may never be generated at all. For example generating 1000 integers between 0 and Int.MAX cannot return all possible values, and some values may happen to be generated more than once. Similarly, generating 1000 random integers between 0 and 500 will definitely result in some values appearing more than once.

Some common arbitraries include numbers with or without a range, strings in the unicode set, random lists, data classes with random parameters, emails, codepoints, chars and so on.

In addition to the random values, arbs may provide edge cases. One of the design features of Kotest's property testing is that values for some types will always include "common" edge cases that you probably want to be included in your tests.

For example, when testing a function that accepts an integer, you probably want to ensure that, at the very least, it is tested with zero, a positive number and a negative number. If only random values were provided, the chances of zero appearing would be fairly low, so Kotest will always provide some "edge cases" for integers (unless you specify otherwise).

When executing tests, the framework will alternate randomly between samples and edge cases. The split is determined by a configuration value which defaults to 2% edge cases.

Not all arbs have edge cases, but the arbs for the most common types do. Here are some examples of edge cases used by some arbs:

Exhaustives generate all values from a given space. This is useful when you want to ensure every value in that space is used. For example, for enum values, it is usually more helpful to ensure each enum is used, rather than picking randomly from the enums values and potentially missing some and duplicating others.

Typical exhaustives include small collections, enums, boolean values, powerset of a list or set, pre-defined integer ranges, and predefined string ranges.

Once an exhaustive has provided all it's values, it will loop and start again, so an exhaustive can be used in a test that requires any number of inputs.

Here we asked for 100 iterations, so each value of Season would be provided 25 times.

**Examples:**

Example 1 (php):
```php
enum class Season { Winter, Fall, Spring, Summer }forAll<Int, Season>(100) { a, season -> ... }
```

---

## Kotlinx DateTime Gens | Kotest

**URL:** https://kotest.io/docs/proptest/kotlinx-datetime-gens.html

**Contents:**
- Kotlinx DateTime Gens

Kotest provides an optional module that provides generators for KotlinX DateTime.

To use, add io.kotest.extensions:kotest-property-datetime:version to your build.

---

## Generator Operations | Kotest

**URL:** https://kotest.io/docs/5.9.x/proptest/generator-operations.html

**Contents:**
- Generator Operations
- Next​
- Filter​
- Map​
- FlatMap​
- Merging​
- Bind​

If you want to use an Arb to just return a value (even outside of a property test), then you can call next on it.

If you have an arb and you want to create a new arb that provides a subset of values, you can call filter on the source arb. For example, one way of generating even numbers is to take the integer arb, and filter out odd values. Viz:

If you have an arb and you want to transform the value generated, you can use map.

If you have an arb whose emission or edge cases depends on the emission of the previous arbitraries, you can use flatMap.

Two generators can be merged together, so that the resulting elements are equally sampled from both generators.

So with the following example would have an equal chance to yield either "a" or "b" on each random sample:

For merging more than two arbitraries, Arb.choice or Arb.choose might be more appropriate. For instance we can use:

Bind is useful if you want to apply multiple arbitraries. We can take a look at how we might construct values for a data class using bind.

**Examples:**

Example 1 (kotlin):
```kotlin
val arbA: Arb<A> = ...val a = arbA.next() // use Random.Defaultval a2 = arbA.next(rs) // pass in Random
```

Example 2 (kotlin):
```kotlin
val evens = Arb.int().filter { it.value % 2 == 0 }val odds = Arb.int().filter { it.value % 2 == 1 }
```

Example 3 (kotlin):
```kotlin
val integerStrings: Arb<String> = Arb.int().map { it.toString() }
```

Example 4 (kotlin):
```kotlin
val dependentArbs: Arb<String> = Arb.of("foo", "bar").flatMap { prefix ->   Arb.int(1..10).map { integer ->      "${prefix}-${integer}"   }}
```

---

## Generators List | Kotest

**URL:** https://kotest.io/docs/5.9.x/proptest/property-test-generators-list.html

**Contents:**
- Generators List

This page lists all current generators in Kotest. There are two types of generator: arbitrary and exhaustive.

Most generators are available on all platforms. Some are JVM or JS specific.

We also provide generators for Arrow as a separate module.

---

## Configuration | Kotest

**URL:** https://kotest.io/docs/5.5.x/proptest/property-test-config.html

**Contents:**
- Configuration
  - Seed​
  - Min Failure​
  - PropTestListener​

Kotest provides for the ability to specify some configuration options when running a property test. We do this by passing in an instance of PropTestConfig to the test methods.

A commonly used configuration option is specifying the seed used by the random source. This is used when you want to repeat the same values each time the test is run. You might want to do this if you find a test failure, and you want to ensure that that particular set of values continues to be executed in the future as a regression test.

For full details on how the seed is used click here.

By default, Kotest tolerates no failure. Perhaps you want to run some non-deterministic test a bunch of times, and you're happy to accept some small number of failures. You can specify that in config.

Sometimes in property test it is required to perform some setup and tear down in each iteration of test. For this purpose you can register a PropTestListener with PropTestConfig.

**Examples:**

Example 1 (kotlin):
```kotlin
class PropertyExample: StringSpec({   "String size" {      forAll<String, String>(PropTestConfig(options here...)) { a,b ->         (a + b).length == a.length + b.length      }   }})
```

Example 2 (kotlin):
```kotlin
class PropertyExample: StringSpec({   "some flakey test" {      forAll<String, String>(PropTestConfig(maxFailure = 3)) { a,b ->         // max of 3 inputs can fail      }   }})
```

Example 3 (kotlin):
```kotlin
class PropertyExample: StringSpec({   "some property test which require setup and tear down in each iteration" {      forAll<String, String>(PropTestConfig(listeners = listOf(MyPropTestListener))) { a,b ->         // some assertion      }   }})
```

---

## Custom Generators | Kotest

**URL:** https://kotest.io/docs/5.5.x/proptest/custom-generators.html

**Contents:**
- Custom Generators
  - Arbitrary​
  - Exhaustive​

To write your own generator for a type T, you just create an instance of Arb<T> or Exhaustive<T>.

When writing a custom arbitrary we can use the arbitrary builder which accepts a lambda that must return the type we are generating for. The parameter to this lambda is a RandomSource parameter which contains the seed and the Random instance. We should typically use the provided RandomSource if we need access to a kotlin.Random instance, as this instance will have been seeded by the framework to allow for repeatable tests.

For example, here is a custom arb that generates a random int between 3 and 6 using the arbitrary builder.

In addition to the RandomSource parameter, the arbitrary builder lambda also provides the ArbitraryBuilderSyntax context which we can leverage to compose other arbitraries when building ours.

For example, here is an Arbitrary that supports a custom class called Person, delegating to a String arbitrary and an Int arbitrary.

The resulting arbitrary produced using this syntax is equivalent to using map, flatMap and bind.

When writing a custom exhaustive we can use the exhaustive() extension function on a List. Nothing more to it than that really!

**Examples:**

Example 1 (kotlin):
```kotlin
val sillyArb = arbitrary { rs: RandomSource ->   rs.random.nextInt(3..6)}
```

Example 2 (kotlin):
```kotlin
data class Person(val name: String, val age: Int)val personArb = arbitrary {   val name = Arb.string(10..12).bind()   val age = Arb.int(21, 150).bind()   Person(name, age)}
```

Example 3 (kotlin):
```kotlin
val singleDigitPrimes = listOf(2,3,5,7).exhaustive()
```

Example 4 (kotlin):
```kotlin
class PropertyExample: StringSpec({    "testing single digit primes" {        checkAll(singleDigitPrimes) { prime ->           isPrime(prime) shouldBe true           isPrime(prime * prime) shouldBe false        }    }})
```

---

## Kotlinx DateTime Gens | Kotest

**URL:** https://kotest.io/docs/5.5.x/proptest/kotlinx-datetime-gens.html

**Contents:**
- Kotlinx DateTime Gens

Kotest provides an optional module that provides generators for KotlinX DateTime.

To use, add io.kotest.extensions:kotest-property-datetime:version to your build.

---

## Generator Operations | Kotest

**URL:** https://kotest.io/docs/5.7.x/proptest/generator-operations.html

**Contents:**
- Generator Operations
- Next​
- Filter​
- Map​
- FlatMap​
- Merging​
- Bind​

If you want to use an Arb to just return a value (even outside of a property test), then you can call next on it.

If you have an arb and you want to create a new arb that provides a subset of values, you can call filter on the source arb. For example, one way of generating even numbers is to take the integer arb, and filter out odd values. Viz:

If you have an arb and you want to transform the value generated, you can use map.

If you have an arb whose emission or edge cases depends on the emission of the previous arbitraries, you can use flatMap.

Two generators can be merged together, so that the resulting elements are equally sampled from both generators.

So with the following example would have an equal chance to yield either "a" or "b" on each random sample:

For merging more than two arbitraries, Arb.choice or Arb.choose might be more appropriate. For instance we can use:

Bind is useful if you want to apply multiple arbitraries. We can take a look at how we might construct values for a data class using bind.

**Examples:**

Example 1 (kotlin):
```kotlin
val arbA: Arb<A> = ...val a = arbA.next() // use Random.Defaultval a2 = arbA.next(rs) // pass in Random
```

Example 2 (kotlin):
```kotlin
val evens = Arb.int().filter { it.value % 2 == 0 }val odds = Arb.int().filter { it.value % 2 == 1 }
```

Example 3 (kotlin):
```kotlin
val integerStrings: Arb<String> = Arb.int().map { it.toString() }
```

Example 4 (kotlin):
```kotlin
val dependentArbs: Arb<String> = Arb.of("foo", "bar").flatMap { prefix ->   Arb.int(1..10).map { integer ->      "${prefix}-${integer}"   }}
```

---

## Shrinking | Kotest

**URL:** https://kotest.io/docs/6.0/proptest/property-test-shrinking.html

**Contents:**
- Shrinking
- Shrinking for built-in generators​
- Shrinking for custom generators​

In property-based testing, the initially found failing case may contain a lot of complexity that does actually cause the test to fail. Shrinking is the mechanism through which a property-based testing framework can simplify failing cases in order to find out the minimal reproducible case is. In Kotest, the way in which failing cases from generators are shrunk is defined by implementations of the Shrinker interface. Built-in generators generally have a default Shrinker defined by the framework, while custom generators can be given a custom Shrinker implementation.

Built-in generators (see Generators List) have a default Shrinker defined by the framework. A shrink function takes as input the value that failed the test and returns a list of new values on which Kotest can apply the test. The exact behaviour depends on the data type. For instance, a string could be shrunk by dropping the first or last character while for integers we could decrement or halve the value. In addition, shrink behaviour is defined for edge cases such as an empty string or the integer 0. The shrinking is performed when a test that uses such generator fails.

If the test fails for one of the generated inputs then the shrinking result is shown:

By default, Kotest will shrink 1000 times. This behaviour is configurable. For example, if you want to continue shrinking without bounds:

If you want to configure the behaviour globally, then either modify the value of PropertyTesting.defaultShrinkingMode or use the following system properties as in this example:

Custom generators do not have a Shrinker defined by Kotest. Instead, custom Shrinkers can be implemented. Below is an example where the Shrinker returns coordinates that are next to the value itself.

**Examples:**

Example 1 (kotlin):
```kotlin
Arb.positiveInt().checkAll { i ->    calculateProperty(i) shouldBe true}
```

Example 2 (typescript):
```typescript
Property test failed for inputs0) 1792716902Caused by io.kotest.assertions.AssertionFailedError: expected:<1792716902> but was:<0> at    PropertyBasedTest$1$1$3$1.invokeSuspend(PropertyBasedTest.kt:54)    PropertyBasedTest$1$1$3$1.invoke(PropertyBasedTest.kt)    PropertyBasedTest$1$1$3$1.invoke(PropertyBasedTest.kt)    io.kotest.property.internal.ProptestKt$proptest$3$2.invokeSuspend(proptest.kt:45)Attempting to shrink arg 1792716902Shrink #1: 1 passShrink #2: 597572300 failShrink #3: 199190766 failShrink #4: 66396922 failShrink #5: 22132307 failShrink #6: 7377435 failShrink #7: 2459145 fail[...]Shrink #999: 29948 passShrink #1000: 44922 passShrink #1001: 59896 passShrink #1002: 89839 failShrink result (after 1002 shrinks) => 89839Caused by io.kotest.assertions.AssertionFailedError: expected:<89839> but was:<0> at    PropertyBasedTest$1$1$3$1.invokeSuspend(PropertyBasedTest.kt:54)    PropertyBasedTest$1$1$3$1.invoke(PropertyBasedTest.kt)    PropertyBasedTest$1$1$3$1.invoke(PropertyBasedTest.kt)    io.kotest.property.internal.ShrinkfnsKt$shrinkfn$1$1$smallestA$1.invokeSuspend(shrinkfns.kt:19)
```

Example 3 (kotlin):
```kotlin
Arb.positiveInt().checkAll(PropTestConfig(shrinkingMode = ShrinkingMode.Unbounded)) { i ->    calculateProperty(i) shouldBe true}
```

Example 4 (markdown):
```markdown
# If you want to disable shrinking:kotest.proptest.default.shrinking.mode=off# If you want to continue shrinking without bounds:kotest.proptest.default.shrinking.mode=unbounded# If you want to shrink a bounded number of times (e.g. 500):kotest.proptest.default.shrinking.mode=boundedkotest.proptest.default.shrinking.bound=500
```

---

## Arrow Generators | Kotest

**URL:** https://kotest.io/docs/5.7.x/proptest/property-test-generators-arrow.html

**Contents:**
- Arrow Generators

Kotest provides an optional module that provides generators for Arrow.

To use, add io.kotest.extensions:kotest-property-arrow:versionand io.arrow-kt:arrow-core:arrow-version to your build. This holds true for the optics module kotest-property-arrow-optics, by adding io.arrow-kt:arrow-optics:arrow-version, too.

---

## Property Test Seeds | Kotest

**URL:** https://kotest.io/docs/5.7.x/proptest/property-test-seeds.html

**Contents:**
- Property Test Seeds
  - Manually specifying the seed​
  - Rerunning failed seeds​
  - Failing when seeds set​

When a property test is executed, the values are generated using a random source that is created from a seed value. By default this seed value is itself randomly chosen (using the default kotlin.random.Random instance). However, there are times when this value needs to be fixed or repeated.

You can change the default used by all tests, unless overriden through the options listed below, by changing the configuration value PropertyTesting.defaultSeed.

To manually set the seed, pass an instance of PropTestConfig to your prop test methods. You might want to do this if you find a test failure, and you want to ensure that those values continue to be executed in the future as a regression test.

Whenever a property test fails, Kotest will output the seed that was used. You can duplicate the test, setting it to use this seed so you have permanent regression test for those values.

By default, when a property test fails, the seed used by that test is written to a file in ~/.kotest/seeds/<spec>/<testname>. Whenever a property test runs, this seed is detected if the file exists, and used in place of a random seed. Next time the test is successful, the seed file will be removed.

A manually specified seed always takes precedence over a failed seed.

This feature can be disabled by setting PropertyTesting.writeFailedSeed = false

Some users prefer to avoid manually specifying seeds. They want to use them locally only, when developing, but to avoid checking them in. If this is your style, then set PropertyTesting.failOnSeed = false or the env var kotest.proptest.seed.fail-if-set to false on your server.

Then if a seed is detected, the test suite will fail.

**Examples:**

Example 1 (kotlin):
```kotlin
class PropertyExample : StringSpec({  "String size" {    forAll<String, String>(PropTestConfig(seed = 127305235)) { a, b ->      (a + b).length == a.length + b.length    }  }})
```

---

## Generators | Kotest

**URL:** https://kotest.io/docs/6.0/proptest/property-test-generators.html

**Contents:**
- Generators
- Arbitrary​
- Exhaustive​

Generated values are provided by instances of the sealed class Gen. You can think of a Gen as kind of like an input stream but for property test values. Each Gen will provide a (usually) infinite stream of these values for one particular type.

Kotest has two types of generators - Arb for generating arbitrary (random) values and Exhaustive for generating a finite set of values in a closed space.

Both types of gens can be mixed and matched in property tests. For example, you could test a function with 100 random positive integers (an arb) alongside every even number from 0 to 200 (exhaustive).

Some generators are only available on the JVM. See the full list here.

Arbs generate two types of values - a hard coded set of edge cases and an infinite stream of randomly chosen samples.

The samples may be repeated, and some values may never be generated at all. For example generating 1000 integers between 0 and Int.MAX cannot return all possible values, and some values may happen to be generated more than once. Similarly, generating 1000 random integers between 0 and 500 will definitely result in some values appearing more than once.

Some common arbitraries include numbers with or without a range, strings in the unicode set, random lists, data classes with random parameters, emails, codepoints, chars and so on.

In addition to the random values, arbs may provide edge cases. One of the design features of Kotest's property testing is that values for some types will always include "common" edge cases that you probably want to be included in your tests.

For example, when testing a function that accepts an integer, you probably want to ensure that, at the very least, it is tested with zero, a positive number and a negative number. If only random values were provided, the chances of zero appearing would be fairly low, so Kotest will always provide some "edge cases" for integers (unless you specify otherwise).

When executing tests, the framework will alternate randomly between samples and edge cases. The split is determined by a configuration value which defaults to 2% edge cases.

Not all arbs have edge cases, but the arbs for the most common types do. Here are some examples of edge cases used by some arbs:

Exhaustives generate all values from a given space. This is useful when you want to ensure every value in that space is used. For example, for enum values, it is usually more helpful to ensure each enum is used, rather than picking randomly from the enums values and potentially missing some and duplicating others.

Typical exhaustives include small collections, enums, boolean values, powerset of a list or set, pre-defined integer ranges, and predefined string ranges.

Once an exhaustive has provided all it's values, it will loop and start again, so an exhaustive can be used in a test that requires any number of inputs.

Here we asked for 100 iterations, so each value of Season would be provided 25 times on average, but the exact counts will randomly change from run to run, and are very likely not to be exactly 25.

**Examples:**

Example 1 (php):
```php
enum class Season { Winter, Fall, Spring, Summer }forAll<Int, Season>(100) { a, season -> ... }
```

---

## Kotlinx DateTime Gens | Kotest

**URL:** https://kotest.io/docs/5.4.x/proptest/kotlinx-datetime-gens.html

**Contents:**
- Kotlinx DateTime Gens

Kotest provides an optional module that provides generators for KotlinX DateTime.

To use, add io.kotest.extensions:kotest-property-datetime:version to your build.

---

## Custom Generators | Kotest

**URL:** https://kotest.io/docs/5.7.x/proptest/custom-generators.html

**Contents:**
- Custom Generators
  - Arbitrary​
  - Exhaustive​

To write your own generator for a type T, you just create an instance of Arb<T> or Exhaustive<T>.

When writing a custom arbitrary we can use the arbitrary builder which accepts a lambda that must return the type we are generating for. The parameter to this lambda is a RandomSource parameter which contains the seed and the Random instance. We should typically use the provided RandomSource if we need access to a kotlin.Random instance, as this instance will have been seeded by the framework to allow for repeatable tests.

For example, here is a custom arb that generates a random int between 3 and 6 using the arbitrary builder.

In addition to the RandomSource parameter, the arbitrary builder lambda also provides the ArbitraryBuilderSyntax context which we can leverage to compose other arbitraries when building ours.

For example, here is an Arbitrary that supports a custom class called Person, delegating to a String arbitrary and an Int arbitrary.

The resulting arbitrary produced using this syntax is equivalent to using map, flatMap and bind.

When writing a custom exhaustive we can use the exhaustive() extension function on a List. Nothing more to it than that really!

**Examples:**

Example 1 (kotlin):
```kotlin
val sillyArb = arbitrary { rs: RandomSource ->   rs.random.nextInt(3..6)}
```

Example 2 (kotlin):
```kotlin
data class Person(val name: String, val age: Int)val personArb = arbitrary {   val name = Arb.string(10..12).bind()   val age = Arb.int(21, 150).bind()   Person(name, age)}
```

Example 3 (kotlin):
```kotlin
val singleDigitPrimes = listOf(2,3,5,7).exhaustive()
```

Example 4 (kotlin):
```kotlin
class PropertyExample: StringSpec({    "testing single digit primes" {        checkAll(singleDigitPrimes) { prime ->           isPrime(prime) shouldBe true           isPrime(prime * prime) shouldBe false        }    }})
```

---

## Generators List | Kotest

**URL:** https://kotest.io/docs/5.4.x/proptest/property-test-generators-list.html

**Contents:**
- Generators List

This page lists all current generators in Kotest. There are two types of generator: arbitrary and exhaustive.

Most generators are available on all platforms. Some are JVM or JS specific.

We also provide generators for Arrow as a separate module.

---

## Global Configuration | Kotest

**URL:** https://kotest.io/docs/proptest/property-test-global-config.html

**Contents:**
- Global Configuration
  - Default Iterations​
  - Printing Shrink Steps​

Some property test settings can be set globally for all property tests.

The standard default iteration count is 1000. This means when you don't specify the iteration count in a property test, the default will be 1000.

We can override this default either by assigning a value to PropertyTesting.defaultIterationCount, or by using the system property kotest.proptest.default.iteration.count.

Any test which directly sets the iteration count will of course use that value.

If you are using Kotest framework, then you can perform this action before any tests by using project config. For example:

By default, when using shrinking, each shrinking step will not be logged, but only the final shrunk value.

To enable logging of each intermediate value, assign true to PropertyTesting.shouldPrintShrinkSteps or use the system property kotest.proptest.output.shrink-steps=true.

**Examples:**

Example 1 (kotlin):
```kotlin
PropertyTesting.defaultIterationCount = 123// will use 555 iterations specified in the testforAll<String, String>(555) { a,b -> a + b == "$a$b" }// will use 123 iterations from the global defaultforAll<String, String> { a,b -> a + b == "$a$b" }
```

Example 2 (kotlin):
```kotlin
class KotestConfig : AbstractProjectConfig() {   override suspend fun beforeProject() {      PropertyTesting.defaultIterationCount = 123   }}
```

---

## Property-based Testing | Kotest

**URL:** https://kotest.io/docs/5.2.x/proptest/property-based-testing.html

**Contents:**
- Property-based Testing
- What is Property Testing?​
- Getting Started​
- Next Steps​

Kotest is split into several subprojects which can be used independently. One of these subprojects is the property test framework. You do not need to be using Kotest as your test framework (although you should!) to benefit from the property test support.

Developers typically write example-based tests. These are your garden variety unit tests you know and love. You provide some inputs, and some expected results, and a test framework like Kotest or JUnit checks that the actual results meet the expectations.

One problem with this approach is that it is very easy to miss errors due to edge cases that the developer didn't think about, or lack of coverage in the chosen inputs. Instead, with property testing, hundreds or thousands of values are fed into the same test, and the values are (usually) randomly generated by your property test framework.

For example, a good property test framework will include values like negative infinity, empty lists, strings with non-ascii characters, and so on. Things we often forget about when writing example based tests.

Property tests were originally conceived in frameworks like Quickcheck with the notion of testing a property on some object, ie. something that should hold true for all inputs. An invariant in other words. An example of an invariant is given two strings, a and b, then length(a + b) should always be equal to length(a) + length(b).

That is where the term property testing originates.

However, you do not have to use property tests to only test things like monad laws or basic numeric functions. Any test that would benefit from a wide array of input values is a good candidate. For example, we might have a function that validates usernames, and we want to test that valid emails are accepted. A property test would be useful here in generating 1000s of combinations to help harden our validation logic.

The property test framework is supported on all targets.

Add the following dependency to your build:

Add the following dependency to your build.

Add the following dependency to your commonTest sourceset:

Alternatively, add the dependency to a specific target. For example, we could add to the Javascript target only be using the jsTest sourceset.

To create input values for tests, Kotest uses the term generator. One generator per input argument is passed to a test function, and the test will execute for a set number of iterations.

**Examples:**

Example 1 (kotlin):
```kotlin
dependencies {   testImplementation("io.kotest:kotest-property:$version")}
```

Example 2 (xml):
```xml
<dependency>   <groupId>io.kotest</groupId>   <artifactId>kotest-property-jvm</artifactId>   <version>${version}</version>   <scope>test</scope></dependency>
```

Example 3 (kotlin):
```kotlin
kotlin {  sourceSets {    val commonTest by getting {      dependencies {        implementation("io.kotest:kotest-property:$version")      }    }  }}
```

Example 4 (kotlin):
```kotlin
kotlin {   targets {      js {         browser()         nodejs()      }   }   sourceSets {      val jsTest by getting {         dependencies {            implementation("io.kotest:kotest-property:$version")         }      }   }}
```

---

## Assumptions | Kotest

**URL:** https://kotest.io/docs/5.4.x/proptest/property-test-assumptions.html

**Contents:**
- Assumptions
  - Assertions​
  - Max Discard Percentage​

To restrict the set the generated values from a generator, we can use filter to return a constrained arb.

However if we need to restrict inputs based on the relationship between values, then filtering on a single arb won't work. Consider an example where we want to test that two non-equal strings have a non-zero Levenshtein distance.

This will periodically fail - whenever two equal strings are generated. One approach would be to just wrap the tests in an if/else block and avoid those undesired inputs.

But in more complicated scenarios we could easily introduce a bug and filter all our inputs.

Kotest provides a feature called assumptions that will filter out unwanted combinations, while tracking that we are not filtering too many.

An assumption accepts a boolean value passed to the withAssumptions function, that if true, will allow the property test to continue, but if false, that particular iteration is skipped. For example, the previous example will now pass:

Alternatively, you can use inline syntax:

Kotest expands on basic boolean assumptions by allowing you to specify assertions in an assumption function.

For example, building on the previous example:

Here we are ensuring that all inputs are not equal, and that the inputs have the same length. Any assertion that throws AssertionError can be used here, including all the assertions provided by Kotest.

This also supports inline syntax:

By default, the maximum discard percentage is 10%. If more combinations are discarded than that, the property test will fail. This helps avoid a scenario where we erroneously discard too many, or even all, our inputs.

For example, the following would fail by default because we would be filtering ~50% of values.

But if we wanted to allow this regardless, we can use the maxDiscardPercentage to increase the allowed discard rate.

It is generally better to adjust your arbs to produce values closer to what you need, so that you only need to filter out unwanted edge cases.

**Examples:**

Example 1 (kotlin):
```kotlin
val evens = Arb.int().filter { it.value % 2 == 0 }
```

Example 2 (kotlin):
```kotlin
checkAll<String, String> { a, b ->  levenshtein(a, b) shouldBeGreaterThan 0}
```

Example 3 (kotlin):
```kotlin
checkAll<String, String> { a, b ->  if (a != b)    levenshtein(a, b) shouldBeGreaterThan 0}
```

Example 4 (kotlin):
```kotlin
checkAll<String, String> { a, b ->  withAssumptions(a != b) {    levenshtein(a, b) shouldBeGreaterThan 0  }}
```

---

## Assumptions | Kotest

**URL:** https://kotest.io/docs/5.9.x/proptest/property-test-assumptions.html

**Contents:**
- Assumptions
  - Assertions​
  - Max Discard Percentage​

To restrict the set the generated values from a generator, we can use filter to return a constrained arb.

However if we need to restrict inputs based on the relationship between values, then filtering on a single arb won't work. Consider an example where we want to test that two non-equal strings have a non-zero Levenshtein distance.

This will periodically fail - whenever two equal strings are generated. One approach would be to just wrap the tests in an if/else block and avoid those undesired inputs.

But in more complicated scenarios we could easily introduce a bug and filter all our inputs.

Kotest provides a feature called assumptions that will filter out unwanted combinations, while tracking that we are not filtering too many.

An assumption accepts a boolean value passed to the withAssumptions function, that if true, will allow the property test to continue, but if false, that particular iteration is skipped. For example, the previous example will now pass:

Alternatively, you can use inline syntax:

Kotest expands on basic boolean assumptions by allowing you to specify assertions in an assumption function.

For example, building on the previous example:

Here we are ensuring that all inputs are not equal, and that the inputs have the same length. Any assertion that throws AssertionError can be used here, including all the assertions provided by Kotest.

This also supports inline syntax:

By default, the maximum discard percentage is 10%. If more combinations are discarded than that, the property test will fail. This helps avoid a scenario where we erroneously discard too many, or even all, our inputs.

For example, the following would fail by default because we would be filtering ~50% of values.

But if we wanted to allow this regardless, we can use the maxDiscardPercentage to increase the allowed discard rate.

It is generally better to adjust your arbs to produce values closer to what you need, so that you only need to filter out unwanted edge cases.

**Examples:**

Example 1 (kotlin):
```kotlin
val evens = Arb.int().filter { it.value % 2 == 0 }
```

Example 2 (kotlin):
```kotlin
checkAll<String, String> { a, b ->  levenshtein(a, b) shouldBeGreaterThan 0}
```

Example 3 (kotlin):
```kotlin
checkAll<String, String> { a, b ->  if (a != b)    levenshtein(a, b) shouldBeGreaterThan 0}
```

Example 4 (kotlin):
```kotlin
checkAll<String, String> { a, b ->  withAssumptions(a != b) {    levenshtein(a, b) shouldBeGreaterThan 0  }}
```

---

## Reflective Arbs | Kotest

**URL:** https://kotest.io/docs/proptest/reflective-arbs.html

**Contents:**
- Reflective Arbs
- Provided Arbs​

When running tests on JVM, Kotest supports generating more complex Arbs automatically. The generated Arb relies on build-in default and further reflective Arbs to populate the class parameters. If you just need to create and instance and don't need filtering you can use the class type in the checkAll/forAll calls directly. When you want to obtain the Arb to manipulate it further or filter invalid values, you can use Arb.bind with the type argument to obtain the Arb. If the required type depends on types that are not supported by default, it is possible to provide Arbs for those types in the call to Arb.bind.

Reflective binding is supported for:

When doing reflective binding, Kotest supports a builder API to provide Arbs for specific types (classes) and properties. Binding specific properties allow greater control in cases where types might be more widely used, like primitives for instance.

**Examples:**

Example 1 (kotlin):
```kotlin
enum class Currency {  USD, GBP, EUR}class CurrencyAmount(  val amount: Long,  val currency: Currency)context("Currencies converts to EUR") { // In some spec  checkAll(Arb.bind<CurrencyAmount>().filter { it.currency != EUR }) { currencyAmount ->    val converted = currencyAmount.convertTo(EUR)    converted.currency shouldBe EUR  }}context("Converting to a currency and back yields the same amount") { // In some spec  checkAll<CurrencyAmount, Currency>() { currencyAmount, currency ->    val converted = currencyAmount.convertTo(currency).convertTo(currencyAmount.currency)    converted.currency shouldBe currencyAmount.currency  }}
```

Example 2 (kotlin):
```kotlin
data class User(  val name: String,  val password: String,  val age: Int,)// in some speccontext("Some tests with an arbitrary user") {  checkAll(Arb.bind<User> {    bind(User::name to Arb.string(1..10))    bind(User::password to Arb.string(24..80)) // binds a specific property to an arb    bind(Int::class to Arb.int(0..100))  // binds a type to an arb  }) { user ->    // ...  }}
```

---

## Kotlinx DateTime Gens | Kotest

**URL:** https://kotest.io/docs/next/proptest/kotlinx-datetime-gens.html

**Contents:**
- Kotlinx DateTime Gens

Kotest provides an optional module that provides generators for KotlinX DateTime.

To use, add io.kotest.extensions:kotest-property-datetime:version to your build.

---

## Generator Operations | Kotest

**URL:** https://kotest.io/docs/5.2.x/proptest/generator-operations.html

**Contents:**
- Generator Operations
- Next​
- Filter​
- Map​
- FlatMap​
- Merging​
- Bind​

If you want to use an Arb to just return a value (even outside of a property test), then you can call next on it.

If you have an arb and you want to create a new arb that provides a subset of values, you can call filter on the source arb. For example, one way of generating even numbers is to take the integer arb, and filter out odd values. Viz:

If you have an arb and you want to transform the value generated, you can use map.

If you have an arb whose emission or edge cases depends on the emission of the previous arbitraries, you can use flatMap.

Two generators can be merged together, so that the resulting elements are equally sampled from both generators.

So with the following example would have an equal chance to yield either "a" or "b" on each random sample:

For merging more than two arbitraries, Arb.choice or Arb.choose might be more appropriate. For instance we can use:

Bind is useful if you want to apply multiple arbitraries. We can take a look at how we might construct values for a data class using bind.

**Examples:**

Example 1 (kotlin):
```kotlin
val arbA: Arb<A> = ...val a = arbA.next() // use Random.Defaultval a2 = arbA.next(rs) // pass in Random
```

Example 2 (kotlin):
```kotlin
val evens = Arb.int().filter { it.value % 2 == 0 }val odds = Arb.int().filter { it.value % 2 == 1 }
```

Example 3 (kotlin):
```kotlin
val integerStrings: Arb<String> = Arb.int().map { it.toString() }
```

Example 4 (kotlin):
```kotlin
val dependentArbs: Arb<String> = Arb.of("foo", "bar").flatMap { prefix ->   Arb.int(1..10).map { integer ->      "${prefix}-${integer}"   }}
```

---

## Kotlinx DateTime Gens | Kotest

**URL:** https://kotest.io/docs/5.9.x/proptest/kotlinx-datetime-gens.html

**Contents:**
- Kotlinx DateTime Gens

Kotest provides an optional module that provides generators for KotlinX DateTime.

To use, add io.kotest.extensions:kotest-property-datetime:version to your build.

---

## Property-based Testing | Kotest

**URL:** https://kotest.io/docs/5.9.x/proptest/property-based-testing.html

**Contents:**
- Property-based Testing
- What is Property Testing?​
- Getting Started​
- Next Steps​

Kotest is split into several subprojects which can be used independently. One of these subprojects is the property test framework. You do not need to be using Kotest as your test framework (although you should!) to benefit from the property test support.

Developers typically write example-based tests. These are your garden variety unit tests you know and love. You provide some inputs, and some expected results, and a test framework like Kotest or JUnit checks that the actual results meet the expectations.

One problem with this approach is that it is very easy to miss errors due to edge cases that the developer didn't think about, or lack of coverage in the chosen inputs. Instead, with property testing, hundreds or thousands of values are fed into the same test, and the values are (usually) randomly generated by your property test framework.

For example, a good property test framework will include values like negative infinity, empty lists, strings with non-ascii characters, and so on. Things we often forget about when writing example based tests.

Property tests were originally conceived in frameworks like Quickcheck with the notion of testing a property on some object, ie. something that should hold true for all inputs. An invariant in other words. An example of an invariant is given two strings, a and b, then length(a + b) should always be equal to length(a) + length(b).

That is where the term property testing originates.

However, you do not have to use property tests to only test things like monad laws or basic numeric functions. Any test that would benefit from a wide array of input values is a good candidate. For example, we might have a function that validates usernames, and we want to test that valid emails are accepted. A property test would be useful here in generating 1000s of combinations to help harden our validation logic.

The property test framework is supported on all targets.

Add the following dependency to your build:

Add the following dependency to your build.

Add the following dependency to your commonTest sourceset:

Alternatively, add the dependency to a specific target. For example, we could add to the Javascript target only be using the jsTest sourceset.

To create input values for tests, Kotest uses the term generator. One generator per input argument is passed to a test function, and the test will execute for a set number of iterations.

**Examples:**

Example 1 (kotlin):
```kotlin
dependencies {   testImplementation("io.kotest:kotest-property:$version")}
```

Example 2 (xml):
```xml
<dependency>   <groupId>io.kotest</groupId>   <artifactId>kotest-property-jvm</artifactId>   <version>${version}</version>   <scope>test</scope></dependency>
```

Example 3 (kotlin):
```kotlin
kotlin {  sourceSets {    val commonTest by getting {      dependencies {        implementation("io.kotest:kotest-property:$version")      }    }  }}
```

Example 4 (kotlin):
```kotlin
kotlin {   targets {      js {         browser()         nodejs()      }   }   sourceSets {      val jsTest by getting {         dependencies {            implementation("io.kotest:kotest-property:$version")         }      }   }}
```

---

## Generators List | Kotest

**URL:** https://kotest.io/docs/5.5.x/proptest/property-test-generators-list.html

**Contents:**
- Generators List

This page lists all current generators in Kotest. There are two types of generator: arbitrary and exhaustive.

Most generators are available on all platforms. Some are JVM or JS specific.

We also provide generators for Arrow as a separate module.

---

## Generator Operations | Kotest

**URL:** https://kotest.io/docs/next/proptest/generator-operations.html

**Contents:**
- Generator Operations
- Next​
- Filter​
- Map​
- FlatMap​
- Merging​
- Bind​

If you want to use an Arb to just return a value (even outside of a property test), then you can call next on it.

If you have an arb and you want to create a new arb that provides a subset of values, you can call filter on the source arb. For example, one way of generating even numbers is to take the integer arb, and filter out odd values. Viz:

If you have an arb and you want to transform the value generated, you can use map.

If you have an arb whose emission or edge cases depends on the emission of the previous arbitraries, you can use flatMap.

Two generators can be merged together, so that the resulting elements are equally sampled from both generators.

So with the following example would have an equal chance to yield either "a" or "b" on each random sample:

For merging more than two arbitraries, Arb.choice or Arb.choose might be more appropriate. For instance we can use:

Bind is useful if you want to apply multiple arbitraries. We can take a look at how we might construct values for a data class using bind.

**Examples:**

Example 1 (kotlin):
```kotlin
val arbA: Arb<A> = ...val a = arbA.next() // use Random.Defaultval a2 = arbA.next(rs) // pass in Random
```

Example 2 (kotlin):
```kotlin
val evens = Arb.int().filter { it.value % 2 == 0 }val odds = Arb.int().filter { it.value % 2 == 1 }
```

Example 3 (kotlin):
```kotlin
val integerStrings: Arb<String> = Arb.int().map { it.toString() }
```

Example 4 (kotlin):
```kotlin
val dependentArbs: Arb<String> = Arb.of("foo", "bar").flatMap { prefix ->   Arb.int(1..10).map { integer ->      "${prefix}-${integer}"   }}
```

---

## Statistics | Kotest

**URL:** https://kotest.io/docs/5.4.x/proptest/property-test-statistics.html

**Contents:**
- Statistics
- Labels​
  - Report Mode​
  - Checking Coverage of Statistics​
  - Custom Reports​

Soemtimes you may like to know the types of values being generated by Kotest, to ensure that your generators are configured in the way you expect. Property test statistics are designed to fulfil this need.

The function collect is the entry point to statistcs and is used to count categories of values. We use this by invoking it inside a property test with the category we want to increment.

For example, lets say we wanted to gather statistics on the values of RoundingMode used by a BigDecimal. We would invoke checkAll as normal, passing the rounding mode to the collect function.

Now after the test is completed, Kotest will output the test name, and the counts/percentages for each category:

The category we use does not have to be an enum. It can be any object, and you can wrap in conditionals if you want more control. For example:

Sometimes you may wish to have orthogonal sets of statistics. For example, for a simple number test, we might want to confirm that a certain percentage are even numbers, and a certain percentage are negative. One way would be to have EVEN_POS, EVEN_NEG, ODD_POS, ODD_NEG:

This gives us one set of outputs:

However, as the combinations grow this will become unwieldy, so Kotest supports labelled statistics. You can think of this as distinct sets of statistics. To use labels, just pass the label name as the first arg to the collect method.

Now, Kotest will output multiple sets of statistics, with the label name in the title:

By default, statistics are printed for every property test. There are four modes which can be configured using the global configuration object PropertyTesting.

The possible options are:

If you wish to programmatically assert that certain values are being generated, then you can use the specify constraints that must be met.

For example, in our previous rounding example, we can check that at least 10% of inputs are covering HALF_DOWN, and 10% are covering FLOOR using withCoveragePercentages:

If we want to check by absolute numbers rather than percentages, we can use withCoverageCounts:

You can customize the report format, or generate reports from the raw data, by using your own instance of StatisticsReporter. This is configured via the global configuration object PropertyTesting.

**Examples:**

Example 1 (kotlin):
```kotlin
checkAll(Arb.enum<RoundingMode>(), Arb.bigDecimal()) { mode, decimal ->  collect(mode)  // test here}
```

Example 2 (yaml):
```yaml
Statistics: [collecting stats] (1000 iterations, 1 args)HALF_DOWN                                                     142 (14%)HALF_UP                                                       141 (14%)CEILING                                                       132 (13%)FLOOR                                                         122 (12%)UP                                                            119 (12%)UNNECESSARY                                                   119 (12%)HALF_EVEN                                                     118 (12%)DOWN                                                          107 (11%)
```

Example 3 (kotlin):
```kotlin
checkAll(Arb.int()) { k ->  when {    k % 2 == 0 -> collect("EVEN")    else -> collect("ODD")  }  // test here}
```

Example 4 (kotlin):
```kotlin
checkAll(Arb.int()) { k ->  when {    k > 0 && k % 2 == 0 -> collect("EVEN_POS")    k % 2 == 0 -> collect("EVEN_NEG")    k > 0 -> collect("ODD_POS")    else -> collect("ODD_NEG")  }  // test here}
```

---

## Reflective Arbs | Kotest

**URL:** https://kotest.io/docs/next/proptest/reflective-arbs.html

**Contents:**
- Reflective Arbs
- Provided Arbs​

When running tests on JVM, Kotest supports generating more complex Arbs automatically. The generated Arb relies on build-in default and further reflective Arbs to populate the class parameters. If you just need to create and instance and don't need filtering you can use the class type in the checkAll/forAll calls directly. When you want to obtain the Arb to manipulate it further or filter invalid values, you can use Arb.bind with the type argument to obtain the Arb. If the required type depends on types that are not supported by default, it is possible to provide Arbs for those types in the call to Arb.bind.

Reflective binding is supported for:

When doing reflective binding, Kotest supports a builder API to provide Arbs for specific types (classes) and properties. Binding specific properties allow greater control in cases where types might be more widely used, like primitives for instance.

**Examples:**

Example 1 (kotlin):
```kotlin
enum class Currency {  USD, GBP, EUR}class CurrencyAmount(  val amount: Long,  val currency: Currency)context("Currencies converts to EUR") { // In some spec  checkAll(Arb.bind<CurrencyAmount>().filter { it.currency != EUR }) { currencyAmount ->    val converted = currencyAmount.convertTo(EUR)    converted.currency shouldBe EUR  }}context("Converting to a currency and back yields the same amount") { // In some spec  checkAll<CurrencyAmount, Currency>() { currencyAmount, currency ->    val converted = currencyAmount.convertTo(currency).convertTo(currencyAmount.currency)    converted.currency shouldBe currencyAmount.currency  }}
```

Example 2 (kotlin):
```kotlin
data class User(  val name: String,  val password: String,  val age: Int,)// in some speccontext("Some tests with an arbitrary user") {  checkAll(Arb.bind<User> {    bind(User::name to Arb.string(1..10))    bind(User::password to Arb.string(24..80)) // binds a specific property to an arb    bind(Int::class to Arb.int(0..100))  // binds a type to an arb  }) { user ->    // ...  }}
```

---

## Extra Arbs | Kotest

**URL:** https://kotest.io/docs/proptest/property-test-extra-arbs.html

**Contents:**
- Extra Arbs

If you are looking for a collection of Arbs for general purpose data generation, then Kotest has such a collection.

To use, add io.kotest.extensions:kotest-property-arbs:version to your build.

---

## Property-based Testing | Kotest

**URL:** https://kotest.io/docs/5.4.x/proptest/property-based-testing.html

**Contents:**
- Property-based Testing
- What is Property Testing?​
- Getting Started​
- Next Steps​

Kotest is split into several subprojects which can be used independently. One of these subprojects is the property test framework. You do not need to be using Kotest as your test framework (although you should!) to benefit from the property test support.

Developers typically write example-based tests. These are your garden variety unit tests you know and love. You provide some inputs, and some expected results, and a test framework like Kotest or JUnit checks that the actual results meet the expectations.

One problem with this approach is that it is very easy to miss errors due to edge cases that the developer didn't think about, or lack of coverage in the chosen inputs. Instead, with property testing, hundreds or thousands of values are fed into the same test, and the values are (usually) randomly generated by your property test framework.

For example, a good property test framework will include values like negative infinity, empty lists, strings with non-ascii characters, and so on. Things we often forget about when writing example based tests.

Property tests were originally conceived in frameworks like Quickcheck with the notion of testing a property on some object, ie. something that should hold true for all inputs. An invariant in other words. An example of an invariant is given two strings, a and b, then length(a + b) should always be equal to length(a) + length(b).

That is where the term property testing originates.

However, you do not have to use property tests to only test things like monad laws or basic numeric functions. Any test that would benefit from a wide array of input values is a good candidate. For example, we might have a function that validates usernames, and we want to test that valid emails are accepted. A property test would be useful here in generating 1000s of combinations to help harden our validation logic.

The property test framework is supported on all targets.

Add the following dependency to your build:

Add the following dependency to your build.

Add the following dependency to your commonTest sourceset:

Alternatively, add the dependency to a specific target. For example, we could add to the Javascript target only be using the jsTest sourceset.

To create input values for tests, Kotest uses the term generator. One generator per input argument is passed to a test function, and the test will execute for a set number of iterations.

**Examples:**

Example 1 (kotlin):
```kotlin
dependencies {   testImplementation("io.kotest:kotest-property:$version")}
```

Example 2 (xml):
```xml
<dependency>   <groupId>io.kotest</groupId>   <artifactId>kotest-property-jvm</artifactId>   <version>${version}</version>   <scope>test</scope></dependency>
```

Example 3 (kotlin):
```kotlin
kotlin {  sourceSets {    val commonTest by getting {      dependencies {        implementation("io.kotest:kotest-property:$version")      }    }  }}
```

Example 4 (kotlin):
```kotlin
kotlin {   targets {      js {         browser()         nodejs()      }   }   sourceSets {      val jsTest by getting {         dependencies {            implementation("io.kotest:kotest-property:$version")         }      }   }}
```

---

## Property Test Seeds | Kotest

**URL:** https://kotest.io/docs/5.8.x/proptest/property-test-seeds.html

**Contents:**
- Property Test Seeds
  - Manually specifying the seed​
  - Rerunning failed seeds​
  - Failing when seeds set​

When a property test is executed, the values are generated using a random source that is created from a seed value. By default this seed value is itself randomly chosen (using the default kotlin.random.Random instance). However, there are times when this value needs to be fixed or repeated.

You can change the default used by all tests, unless overriden through the options listed below, by changing the configuration value PropertyTesting.defaultSeed.

To manually set the seed, pass an instance of PropTestConfig to your prop test methods. You might want to do this if you find a test failure, and you want to ensure that those values continue to be executed in the future as a regression test.

Whenever a property test fails, Kotest will output the seed that was used. You can duplicate the test, setting it to use this seed so you have permanent regression test for those values.

By default, when a property test fails, the seed used by that test is written to a file in ~/.kotest/seeds/<spec>/<testname>. Whenever a property test runs, this seed is detected if the file exists, and used in place of a random seed. Next time the test is successful, the seed file will be removed.

A manually specified seed always takes precedence over a failed seed.

This feature can be disabled by setting PropertyTesting.writeFailedSeed = false

Some users prefer to avoid manually specifying seeds. They want to use them locally only, when developing, but to avoid checking them in. If this is your style, then set PropertyTesting.failOnSeed = false or the env var kotest.proptest.seed.fail-if-set to false on your server.

Then if a seed is detected, the test suite will fail.

**Examples:**

Example 1 (kotlin):
```kotlin
class PropertyExample : StringSpec({  "String size" {    forAll<String, String>(PropTestConfig(seed = 127305235)) { a, b ->      (a + b).length == a.length + b.length    }  }})
```

---

## Kotlinx DateTime Gens | Kotest

**URL:** https://kotest.io/docs/5.8.x/proptest/kotlinx-datetime-gens.html

**Contents:**
- Kotlinx DateTime Gens

Kotest provides an optional module that provides generators for KotlinX DateTime.

To use, add io.kotest.extensions:kotest-property-datetime:version to your build.

---

## Extra Arbs | Kotest

**URL:** https://kotest.io/docs/6.0/proptest/property-test-extra-arbs.html

**Contents:**
- Extra Arbs

If you are looking for a collection of Arbs for general purpose data generation, then Kotest has such a collection.

To use, add io.kotest.extensions:kotest-property-arbs:version to your build.

---

## Generator Operations | Kotest

**URL:** https://kotest.io/docs/6.0/proptest/generator-operations.html

**Contents:**
- Generator Operations
- Next​
- Filter​
- Map​
- FlatMap​
- Merging​
- Bind​

If you want to use an Arb to just return a value (even outside of a property test), then you can call next on it.

If you have an arb and you want to create a new arb that provides a subset of values, you can call filter on the source arb. For example, one way of generating even numbers is to take the integer arb, and filter out odd values. Viz:

If you have an arb and you want to transform the value generated, you can use map.

If you have an arb whose emission or edge cases depends on the emission of the previous arbitraries, you can use flatMap.

Two generators can be merged together, so that the resulting elements are equally sampled from both generators.

So with the following example would have an equal chance to yield either "a" or "b" on each random sample:

For merging more than two arbitraries, Arb.choice or Arb.choose might be more appropriate. For instance we can use:

Bind is useful if you want to apply multiple arbitraries. We can take a look at how we might construct values for a data class using bind.

**Examples:**

Example 1 (kotlin):
```kotlin
val arbA: Arb<A> = ...val a = arbA.next() // use Random.Defaultval a2 = arbA.next(rs) // pass in Random
```

Example 2 (kotlin):
```kotlin
val evens = Arb.int().filter { it.value % 2 == 0 }val odds = Arb.int().filter { it.value % 2 == 1 }
```

Example 3 (kotlin):
```kotlin
val integerStrings: Arb<String> = Arb.int().map { it.toString() }
```

Example 4 (kotlin):
```kotlin
val dependentArbs: Arb<String> = Arb.of("foo", "bar").flatMap { prefix ->   Arb.int(1..10).map { integer ->      "${prefix}-${integer}"   }}
```

---

## Statistics | Kotest

**URL:** https://kotest.io/docs/5.6.x/proptest/property-test-statistics.html

**Contents:**
- Statistics
- Labels​
  - Report Mode​
  - Checking Coverage of Statistics​
  - Custom Reports​

Sometimes you may like to know the types of values being generated by Kotest, to ensure that your generators are configured in the way you expect. Property test statistics are designed to fulfil this need.

The function collect is the entry point to statistcs and is used to count categories of values. We use this by invoking it inside a property test with the category we want to increment.

For example, lets say we wanted to gather statistics on the values of RoundingMode used by a BigDecimal. We would invoke checkAll as normal, passing the rounding mode to the collect function.

Now after the test is completed, Kotest will output the test name, and the counts/percentages for each category:

The category we use does not have to be an enum. It can be any object, and you can wrap in conditionals if you want more control. For example:

Sometimes you may wish to have orthogonal sets of statistics. For example, for a simple number test, we might want to confirm that a certain percentage are even numbers, and a certain percentage are negative. One way would be to have EVEN_POS, EVEN_NEG, ODD_POS, ODD_NEG:

This gives us one set of outputs:

However, as the combinations grow this will become unwieldy, so Kotest supports labelled statistics. You can think of this as distinct sets of statistics. To use labels, just pass the label name as the first arg to the collect method.

Now, Kotest will output multiple sets of statistics, with the label name in the title:

By default, statistics are printed for every property test. There are four modes which can be configured using the global configuration object PropertyTesting.

The possible options are:

If you wish to programmatically assert that certain values are being generated, then you can use the specify constraints that must be met.

For example, in our previous rounding example, we can check that at least 10% of inputs are covering HALF_DOWN, and 10% are covering FLOOR using withCoveragePercentages:

If we want to check by absolute numbers rather than percentages, we can use withCoverageCounts:

You can customize the report format, or generate reports from the raw data, by using your own instance of StatisticsReporter. This is configured via the global configuration object PropertyTesting.

**Examples:**

Example 1 (kotlin):
```kotlin
checkAll(Arb.enum<RoundingMode>(), Arb.bigDecimal()) { mode, decimal ->  collect(mode)  // test here}
```

Example 2 (yaml):
```yaml
Statistics: [collecting stats] (1000 iterations, 1 args)HALF_DOWN                                                     142 (14%)HALF_UP                                                       141 (14%)CEILING                                                       132 (13%)FLOOR                                                         122 (12%)UP                                                            119 (12%)UNNECESSARY                                                   119 (12%)HALF_EVEN                                                     118 (12%)DOWN                                                          107 (11%)
```

Example 3 (kotlin):
```kotlin
checkAll(Arb.int()) { k ->  when {    k % 2 == 0 -> collect("EVEN")    else -> collect("ODD")  }  // test here}
```

Example 4 (kotlin):
```kotlin
checkAll(Arb.int()) { k ->  when {    k > 0 && k % 2 == 0 -> collect("EVEN_POS")    k % 2 == 0 -> collect("EVEN_NEG")    k > 0 -> collect("ODD_POS")    else -> collect("ODD_NEG")  }  // test here}
```

---

## Global Configuration | Kotest

**URL:** https://kotest.io/docs/6.0/proptest/property-test-global-config.html

**Contents:**
- Global Configuration
  - Default Iterations​
  - Printing Shrink Steps​

Some property test settings can be set globally for all property tests.

The standard default iteration count is 1000. This means when you don't specify the iteration count in a property test, the default will be 1000.

We can override this default either by assigning a value to PropertyTesting.defaultIterationCount, or by using the system property kotest.proptest.default.iteration.count.

Any test which directly sets the iteration count will of course use that value.

If you are using Kotest framework, then you can perform this action before any tests by using project config. For example:

By default, when using shrinking, each shrinking step will not be logged, but only the final shrunk value.

To enable logging of each intermediate value, assign true to PropertyTesting.shouldPrintShrinkSteps or use the system property kotest.proptest.output.shrink-steps=true.

**Examples:**

Example 1 (kotlin):
```kotlin
PropertyTesting.defaultIterationCount = 123// will use 555 iterations specified in the testforAll<String, String>(555) { a,b -> a + b == "$a$b" }// will use 123 iterations from the global defaultforAll<String, String> { a,b -> a + b == "$a$b" }
```

Example 2 (kotlin):
```kotlin
class KotestConfig : AbstractProjectConfig() {   override suspend fun beforeProject() {      PropertyTesting.defaultIterationCount = 123   }}
```

---

## Configuration | Kotest

**URL:** https://kotest.io/docs/5.7.x/proptest/property-test-config.html

**Contents:**
- Configuration
  - Seed​
  - Min Failure​
  - PropTestListener​

Kotest provides for the ability to specify some configuration options when running a property test. We do this by passing in an instance of PropTestConfig to the test methods.

A commonly used configuration option is specifying the seed used by the random source. This is used when you want to repeat the same values each time the test is run. You might want to do this if you find a test failure, and you want to ensure that that particular set of values continues to be executed in the future as a regression test.

For full details on how the seed is used click here.

By default, Kotest tolerates no failure. Perhaps you want to run some non-deterministic test a bunch of times, and you're happy to accept some small number of failures. You can specify that in config.

Sometimes in property test it is required to perform some setup and tear down in each iteration of test. For this purpose you can register a PropTestListener with PropTestConfig.

**Examples:**

Example 1 (kotlin):
```kotlin
class PropertyExample: StringSpec({   "String size" {      forAll<String, String>(PropTestConfig(options here...)) { a,b ->         (a + b).length == a.length + b.length      }   }})
```

Example 2 (kotlin):
```kotlin
class PropertyExample: StringSpec({   "some flakey test" {      forAll<String, String>(PropTestConfig(maxFailure = 3)) { a,b ->         // max of 3 inputs can fail      }   }})
```

Example 3 (kotlin):
```kotlin
class PropertyExample: StringSpec({   "some property test which require setup and tear down in each iteration" {      forAll<String, String>(PropTestConfig(listeners = listOf(MyPropTestListener))) { a,b ->         // some assertion      }   }})
```

---

## Kotlinx DateTime Gens | Kotest

**URL:** https://kotest.io/docs/5.2.x/proptest/kotlinx-datetime-gens.html

**Contents:**
- Kotlinx DateTime Gens

Kotest provides an optional module that provides generators for KotlinX DateTime.

To use, add io.kotest.extensions:kotest-property-datetime:version to your build.

---

## Statistics | Kotest

**URL:** https://kotest.io/docs/5.5.x/proptest/property-test-statistics.html

**Contents:**
- Statistics
- Labels​
  - Report Mode​
  - Checking Coverage of Statistics​
  - Custom Reports​

Sometimes you may like to know the types of values being generated by Kotest, to ensure that your generators are configured in the way you expect. Property test statistics are designed to fulfil this need.

The function collect is the entry point to statistcs and is used to count categories of values. We use this by invoking it inside a property test with the category we want to increment.

For example, lets say we wanted to gather statistics on the values of RoundingMode used by a BigDecimal. We would invoke checkAll as normal, passing the rounding mode to the collect function.

Now after the test is completed, Kotest will output the test name, and the counts/percentages for each category:

The category we use does not have to be an enum. It can be any object, and you can wrap in conditionals if you want more control. For example:

Sometimes you may wish to have orthogonal sets of statistics. For example, for a simple number test, we might want to confirm that a certain percentage are even numbers, and a certain percentage are negative. One way would be to have EVEN_POS, EVEN_NEG, ODD_POS, ODD_NEG:

This gives us one set of outputs:

However, as the combinations grow this will become unwieldy, so Kotest supports labelled statistics. You can think of this as distinct sets of statistics. To use labels, just pass the label name as the first arg to the collect method.

Now, Kotest will output multiple sets of statistics, with the label name in the title:

By default, statistics are printed for every property test. There are four modes which can be configured using the global configuration object PropertyTesting.

The possible options are:

If you wish to programmatically assert that certain values are being generated, then you can use the specify constraints that must be met.

For example, in our previous rounding example, we can check that at least 10% of inputs are covering HALF_DOWN, and 10% are covering FLOOR using withCoveragePercentages:

If we want to check by absolute numbers rather than percentages, we can use withCoverageCounts:

You can customize the report format, or generate reports from the raw data, by using your own instance of StatisticsReporter. This is configured via the global configuration object PropertyTesting.

**Examples:**

Example 1 (kotlin):
```kotlin
checkAll(Arb.enum<RoundingMode>(), Arb.bigDecimal()) { mode, decimal ->  collect(mode)  // test here}
```

Example 2 (yaml):
```yaml
Statistics: [collecting stats] (1000 iterations, 1 args)HALF_DOWN                                                     142 (14%)HALF_UP                                                       141 (14%)CEILING                                                       132 (13%)FLOOR                                                         122 (12%)UP                                                            119 (12%)UNNECESSARY                                                   119 (12%)HALF_EVEN                                                     118 (12%)DOWN                                                          107 (11%)
```

Example 3 (kotlin):
```kotlin
checkAll(Arb.int()) { k ->  when {    k % 2 == 0 -> collect("EVEN")    else -> collect("ODD")  }  // test here}
```

Example 4 (kotlin):
```kotlin
checkAll(Arb.int()) { k ->  when {    k > 0 && k % 2 == 0 -> collect("EVEN_POS")    k % 2 == 0 -> collect("EVEN_NEG")    k > 0 -> collect("ODD_POS")    else -> collect("ODD_NEG")  }  // test here}
```

---

## Arrow Generators | Kotest

**URL:** https://kotest.io/docs/next/proptest/property-test-generators-arrow.html

**Contents:**
- Arrow Generators

Kotest provides an optional module that provides generators for Arrow.

To use, add io.kotest.extensions:kotest-property-arrow:versionand io.arrow-kt:arrow-core:arrow-version to your build. This holds true for the optics module kotest-property-arrow-optics, by adding io.arrow-kt:arrow-optics:arrow-version, too.

---

## Shrinking | Kotest

**URL:** https://kotest.io/docs/5.9.x/proptest/property-test-shrinking.html

**Contents:**
- Shrinking
- Shrinking for built-in generators​
- Shrinking for custom generators​

In property-based testing, the initially found failing case may contain a lot of complexity that does actually cause the test to fail. Shrinking is the mechanism through which a property-based testing framework can simplify failing cases in order to find out the minimal reproducible case is. In Kotest, the way in which failing cases from generators are shrunk is defined by implementations of the Shrinker interface. Built-in generators generally have a default Shrinker defined by the framework, while custom generators can be given a custom Shrinker implementation.

Built-in generators (see Generators List) have a default Shrinker defined by the framework. A shrink function takes as input the value that failed the test and returns a list of new values on which Kotest can appy the test. The exact behaviour depends on the data type. For instance, a string could be shrunk by dropping the first or last character while for integers we could decrement or halve the value. In addition, shrink behaviour is defined for edge cases such as an empty string or the integer 0. The shrinking is performed when a test that uses such generator fails.

If the test fails for one of the generated inputs then the shrinking result is shown:

By default, Kotest will shrink 1000 times. This behaviour is configurable. For example, if you want to continue shrinking without bounds:

Custom generators do not have a Shrinker defined by Kotest. Instead, custom Shrinkers can be implemented. Below is an example where the Shrinker returns coordinates that are next to the value itself.

**Examples:**

Example 1 (kotlin):
```kotlin
Arb.positiveInt().checkAll { i ->    calculateProperty(i) shouldBe true}
```

Example 2 (typescript):
```typescript
Property test failed for inputs0) 1792716902Caused by io.kotest.assertions.AssertionFailedError: expected:<1792716902> but was:<0> at    PropertyBasedTest$1$1$3$1.invokeSuspend(PropertyBasedTest.kt:54)    PropertyBasedTest$1$1$3$1.invoke(PropertyBasedTest.kt)    PropertyBasedTest$1$1$3$1.invoke(PropertyBasedTest.kt)    io.kotest.property.internal.ProptestKt$proptest$3$2.invokeSuspend(proptest.kt:45)Attempting to shrink arg 1792716902Shrink #1: 1 passShrink #2: 597572300 failShrink #3: 199190766 failShrink #4: 66396922 failShrink #5: 22132307 failShrink #6: 7377435 failShrink #7: 2459145 fail[...]Shrink #999: 29948 passShrink #1000: 44922 passShrink #1001: 59896 passShrink #1002: 89839 failShrink result (after 1002 shrinks) => 89839Caused by io.kotest.assertions.AssertionFailedError: expected:<89839> but was:<0> at    PropertyBasedTest$1$1$3$1.invokeSuspend(PropertyBasedTest.kt:54)    PropertyBasedTest$1$1$3$1.invoke(PropertyBasedTest.kt)    PropertyBasedTest$1$1$3$1.invoke(PropertyBasedTest.kt)    io.kotest.property.internal.ShrinkfnsKt$shrinkfn$1$1$smallestA$1.invokeSuspend(shrinkfns.kt:19)
```

Example 3 (kotlin):
```kotlin
Arb.positiveInt().checkAll(PropTestConfig(shrinkingMode = ShrinkingMode.Unbounded)) { i ->    calculateProperty(i) shouldBe true}
```

Example 4 (kotlin):
```kotlin
data class Coordinate(val x: Int, val y: Int)class CoordinateTest : FunSpec({    context("Coordinate Transformations") {        // Shrinker takes the four neighbouring coordinates        val coordinateShrinker = Shrinker<Coordinate> { c ->            listOf(                Coordinate(c.x - 1, c.y),                Coordinate(c.x, c.y - 1),                Coordinate(c.x + 1, c.y),                Coordinate(c.x, c.y + 1),            )        }        val coordinateArb = arbitrary(coordinateShrinker) {            Coordinate(Arb.nonNegativeInt().bind(), Arb.nonNegativeInt().bind())        }        test("Coordinates are always positive after transformation") {            coordinateArb.checkAll {                transform(it).x shouldBeGreaterThanOrEqualTo 0                transform(it).y shouldBeGreaterThanOrEqualTo 0            }        }    }})
```

---

## Generators List | Kotest

**URL:** https://kotest.io/docs/5.6.x/proptest/property-test-generators-list.html

**Contents:**
- Generators List

This page lists all current generators in Kotest. There are two types of generator: arbitrary and exhaustive.

Most generators are available on all platforms. Some are JVM or JS specific.

We also provide generators for Arrow as a separate module.

---

## Arrow Generators | Kotest

**URL:** https://kotest.io/docs/5.5.x/proptest/property-test-generators-arrow.html

**Contents:**
- Arrow Generators

Kotest provides an optional module that provides generators for Arrow.

To use, add io.kotest.extensions:kotest-property-arrow:versionand io.arrow-kt:arrow-core:arrow-version to your build. This holds true for the optics module kotest-property-arrow-optics, by adding io.arrow-kt:arrow-optics:arrow-version, too.

---

## Arrow Generators | Kotest

**URL:** https://kotest.io/docs/5.9.x/proptest/property-test-generators-arrow.html

**Contents:**
- Arrow Generators

Kotest provides an optional module that provides generators for Arrow.

To use, add io.kotest.extensions:kotest-property-arrow:versionand io.arrow-kt:arrow-core:arrow-version to your build. This holds true for the optics module kotest-property-arrow-optics, by adding io.arrow-kt:arrow-optics:arrow-version, too.

---

## Generators | Kotest

**URL:** https://kotest.io/docs/5.4.x/proptest/property-test-generators.html

**Contents:**
- Generators
- Arbitrary​
- Exhaustive​

Generated values are provided by instances of the sealed class Gen. You can think of a Gen as kind of like an input stream but for property test values. Each Gen will provide a (usually) infinite stream of these values for one particular type.

Kotest has two types of generators - Arb for generating arbitrary (random) values and Exhaustive for generating a finite set of values in a closed space.

Both types of gens can be mixed and matched in property tests. For example, you could test a function with 100 random positive integers (an arb) alongside every even number from 0 to 200 (exhaustive).

Some generators are only available on the JVM. See the full list here.

Arbs generate two types of values - a hard coded set of edge cases and an infinite stream of randomly chosen samples.

The samples may be repeated, and some values may never be generated at all. For example generating 1000 integers between 0 and Int.MAX cannot return all possible values, and some values may happen to be generated more than once. Similarly, generating 1000 random integers between 0 and 500 will definitely result in some values appearing more than once.

Some common arbitraries include numbers with or without a range, strings in the unicode set, random lists, data classes with random parameters, emails, codepoints, chars and so on.

In addition to the random values, arbs may provide edge cases. One of the design features of Kotest's property testing is that values for some types will always include "common" edge cases that you probably want to be included in your tests.

For example, when testing a function that accepts an integer, you probably want to ensure that, at the very least, it is tested with zero, a positive number and a negative number. If only random values were provided, the chances of zero appearing would be fairly low, so Kotest will always provide some "edge cases" for integers (unless you specify otherwise).

When executing tests, the framework will alternate randomly between samples and edge cases. The split is determined by a configuration value which defaults to 2% edge cases.

Not all arbs have edge cases, but the arbs for the most common types do. Here are some examples of edge cases used by some arbs:

Exhaustives generate all values from a given space. This is useful when you want to ensure every value in that space is used. For example, for enum values, it is usually more helpful to ensure each enum is used, rather than picking randomly from the enums values and potentially missing some and duplicating others.

Typical exhaustives include small collections, enums, boolean values, powerset of a list or set, pre-defined integer ranges, and predefined string ranges.

Once an exhaustive has provided all it's values, it will loop and start again, so an exhaustive can be used in a test that requires any number of inputs.

Here we asked for 100 iterations, so each value of Season would be provided 25 times.

**Examples:**

Example 1 (php):
```php
enum class Season { Winter, Fall, Spring, Summer }forAll<Int, Season>(100) { a, season -> ... }
```

---

## Shrinking | Kotest

**URL:** https://kotest.io/docs/5.6.x/proptest/property-test-shrinking.html

**Contents:**
- Shrinking
- Shrinking for built-in generators​
- Shrinking for custom generators​

In property-based testing, the initially found failing case may contain a lot of complexity that does actually cause the test to fail. Shrinking is the mechanism through which a property-based testing framework can simplify failing cases in order to find out the minimal reproducible case is. In Kotest, the way in which failing cases from generators are shrunk is defined by implementations of the Shrinker interface. Built-in generators generally have a default Shrinker defined by the framework, while custom generators can be given a custom Shrinker implementation.

Built-in generators (see Generators List) have a default Shrinker defined by the framework. A shrink function takes as input the value that failed the test and returns a list of new values on which Kotest can appy the test. The exact behaviour depends on the data type. For instance, a string could be shrunk by dropping the first or last character while for integers we could decrement or halve the value. In addition, shrink behaviour is defined for edge cases such as an empty string or the integer 0. The shrinking is performed when a test that uses such generator fails.

If the test fails for one of the generated inputs then the shrinking result is shown:

By default, Kotest will shrink 1000 times. This behaviour is configurable. For example, if you want to continue shrinking without bounds:

Custom generators do not have a Shrinker defined by Kotest. Instead, custom Shrinkers can be implemented. Below is an example where the Shrinker returns coordinates that are next to the value itself.

**Examples:**

Example 1 (kotlin):
```kotlin
Arb.positiveInt().checkAll { i ->    calculateProperty(i) shouldBe true}
```

Example 2 (typescript):
```typescript
Property test failed for inputs0) 1792716902Caused by io.kotest.assertions.AssertionFailedError: expected:<1792716902> but was:<0> at    PropertyBasedTest$1$1$3$1.invokeSuspend(PropertyBasedTest.kt:54)    PropertyBasedTest$1$1$3$1.invoke(PropertyBasedTest.kt)    PropertyBasedTest$1$1$3$1.invoke(PropertyBasedTest.kt)    io.kotest.property.internal.ProptestKt$proptest$3$2.invokeSuspend(proptest.kt:45)Attempting to shrink arg 1792716902Shrink #1: 1 passShrink #2: 597572300 failShrink #3: 199190766 failShrink #4: 66396922 failShrink #5: 22132307 failShrink #6: 7377435 failShrink #7: 2459145 fail[...]Shrink #999: 29948 passShrink #1000: 44922 passShrink #1001: 59896 passShrink #1002: 89839 failShrink result (after 1002 shrinks) => 89839Caused by io.kotest.assertions.AssertionFailedError: expected:<89839> but was:<0> at    PropertyBasedTest$1$1$3$1.invokeSuspend(PropertyBasedTest.kt:54)    PropertyBasedTest$1$1$3$1.invoke(PropertyBasedTest.kt)    PropertyBasedTest$1$1$3$1.invoke(PropertyBasedTest.kt)    io.kotest.property.internal.ShrinkfnsKt$shrinkfn$1$1$smallestA$1.invokeSuspend(shrinkfns.kt:19)
```

Example 3 (kotlin):
```kotlin
Arb.positiveInt().checkAll(PropTestConfig(shrinkingMode = ShrinkingMode.Unbounded)) { i ->    calculateProperty(i) shouldBe true}
```

Example 4 (kotlin):
```kotlin
data class Coordinate(val x: Int, val y: Int)class CoordinateTest : FunSpec({    context("Coordinate Transformations") {        // Shrinker takes the four neighbouring coordinates        val coordinateShrinker = Shrinker<Coordinate> { c ->            listOf(                Coordinate(c.x - 1, c.y),                Coordinate(c.x, c.y - 1),                Coordinate(c.x + 1, c.y),                Coordinate(c.x, c.y + 1),            )        }        val coordinateArb = arbitrary(coordinateShrinker) {            Coordinate(Arb.nonNegativeInt().bind(), Arb.nonNegativeInt().bind())        }        test("Coordinates are always positive after transformation") {            coordinateArb.checkAll {                transform(it).x shouldBeGreaterThanOrEqualTo 0                transform(it).y shouldBeGreaterThanOrEqualTo 0            }        }    }})
```

---

## Shrinking | Kotest

**URL:** https://kotest.io/docs/next/proptest/property-test-shrinking.html

**Contents:**
- Shrinking
- Shrinking for built-in generators​
- Shrinking for custom generators​

In property-based testing, the initially found failing case may contain a lot of complexity that does actually cause the test to fail. Shrinking is the mechanism through which a property-based testing framework can simplify failing cases in order to find out the minimal reproducible case is. In Kotest, the way in which failing cases from generators are shrunk is defined by implementations of the Shrinker interface. Built-in generators generally have a default Shrinker defined by the framework, while custom generators can be given a custom Shrinker implementation.

Built-in generators (see Generators List) have a default Shrinker defined by the framework. A shrink function takes as input the value that failed the test and returns a list of new values on which Kotest can apply the test. The exact behaviour depends on the data type. For instance, a string could be shrunk by dropping the first or last character while for integers we could decrement or halve the value. In addition, shrink behaviour is defined for edge cases such as an empty string or the integer 0. The shrinking is performed when a test that uses such generator fails.

If the test fails for one of the generated inputs then the shrinking result is shown:

By default, Kotest will shrink 1000 times. This behaviour is configurable. For example, if you want to continue shrinking without bounds:

If you want to configure the behaviour globally, then either modify the value of PropertyTesting.defaultShrinkingMode or use the following system properties as in this example:

Custom generators do not have a Shrinker defined by Kotest. Instead, custom Shrinkers can be implemented. Below is an example where the Shrinker returns coordinates that are next to the value itself.

**Examples:**

Example 1 (kotlin):
```kotlin
Arb.positiveInt().checkAll { i ->    calculateProperty(i) shouldBe true}
```

Example 2 (typescript):
```typescript
Property test failed for inputs0) 1792716902Caused by io.kotest.assertions.AssertionFailedError: expected:<1792716902> but was:<0> at    PropertyBasedTest$1$1$3$1.invokeSuspend(PropertyBasedTest.kt:54)    PropertyBasedTest$1$1$3$1.invoke(PropertyBasedTest.kt)    PropertyBasedTest$1$1$3$1.invoke(PropertyBasedTest.kt)    io.kotest.property.internal.ProptestKt$proptest$3$2.invokeSuspend(proptest.kt:45)Attempting to shrink arg 1792716902Shrink #1: 1 passShrink #2: 597572300 failShrink #3: 199190766 failShrink #4: 66396922 failShrink #5: 22132307 failShrink #6: 7377435 failShrink #7: 2459145 fail[...]Shrink #999: 29948 passShrink #1000: 44922 passShrink #1001: 59896 passShrink #1002: 89839 failShrink result (after 1002 shrinks) => 89839Caused by io.kotest.assertions.AssertionFailedError: expected:<89839> but was:<0> at    PropertyBasedTest$1$1$3$1.invokeSuspend(PropertyBasedTest.kt:54)    PropertyBasedTest$1$1$3$1.invoke(PropertyBasedTest.kt)    PropertyBasedTest$1$1$3$1.invoke(PropertyBasedTest.kt)    io.kotest.property.internal.ShrinkfnsKt$shrinkfn$1$1$smallestA$1.invokeSuspend(shrinkfns.kt:19)
```

Example 3 (kotlin):
```kotlin
Arb.positiveInt().checkAll(PropTestConfig(shrinkingMode = ShrinkingMode.Unbounded)) { i ->    calculateProperty(i) shouldBe true}
```

Example 4 (markdown):
```markdown
# If you want to disable shrinking:kotest.proptest.default.shrinking.mode=off# If you want to continue shrinking without bounds:kotest.proptest.default.shrinking.mode=unbounded# If you want to shrink a bounded number of times (e.g. 500):kotest.proptest.default.shrinking.mode=boundedkotest.proptest.default.shrinking.bound=500
```

---

## Extra Arbs | Kotest

**URL:** https://kotest.io/docs/5.6.x/proptest/property-test-extra-arbs.html

**Contents:**
- Extra Arbs

If you are looking for a collection of Arbs for general purpose data generation, then Kotest has such a collection.

To use, add io.kotest.extensions:kotest-property-arbs:version to your build.

---

## Global Configuration | Kotest

**URL:** https://kotest.io/docs/5.6.x/proptest/property-test-global-config.html

**Contents:**
- Global Configuration
  - Default Iterations​
  - Printing Shrink Steps​

Some property test settings can be set globally for all property tests.

The standard default iteration count is 1000. This means when you don't specify the iteration count in a property test, the default will be 1000.

We can override this default either by assigning a value to PropertyTesting.defaultIterationCount, or by using the system property kotest.proptest.default.iteration.count.

Any test which directly sets the iteration count will of course use that value.

If you are using Kotest framework, then you can perform this action before any tests by using project config. For example:

By default, when using shrinking, each shrinking step will not be logged, but only the final shrunk value.

To enable logging of each intermediate value, assign true to PropertyTesting.shouldPrintShrinkSteps or use the system property kotest.proptest.output.shrink-steps=true.

**Examples:**

Example 1 (kotlin):
```kotlin
PropertyTesting.defaultIterationCount = 123// will use 555 iterations specified in the testforAll<String, String>(555) { a,b -> a + b == "$a$b" }// will use 123 iterations from the global defaultforAll<String, String> { a,b -> a + b == "$a$b" }
```

Example 2 (kotlin):
```kotlin
class KotestConfig : AbstractProjectConfig() {   override suspend fun beforeProject() {      PropertyTesting.defaultIterationCount = 123   }}
```

---

## Extra Arbs | Kotest

**URL:** https://kotest.io/docs/5.2.x/proptest/property-test-extra-arbs.html

**Contents:**
- Extra Arbs

If you are looking for a collection of Arbs for general purpose data generation, then Kotest has such a collection.

To use, add io.kotest.extensions:kotest-property-arbs:version to your build.

---

## Global Configuration | Kotest

**URL:** https://kotest.io/docs/5.4.x/proptest/property-test-global-config.html

**Contents:**
- Global Configuration
  - Default Iterations​
  - Printing Shrink Steps​

Some property test settings can be set globally for all property tests.

The standard default iteration count is 1000. This means when you don't specify the iteration count in a property test, the default will be 1000.

We can override this default either by assigning a value to PropertyTesting.defaultIterationCount, or by using the system property kotest.proptest.default.iteration.count.

Any test which directly sets the iteration count will of course use that value.

If you are using Kotest framework, then you can perform this action before any tests by using project config. For example:

By default, when using shrinking, each shrinking step will not be logged, but only the final shrunk value.

To enable logging of each intermediate value, assign true to PropertyTesting.shouldPrintShrinkSteps or use the system property kotest.proptest.output.shrink-steps=true.

**Examples:**

Example 1 (kotlin):
```kotlin
PropertyTesting.defaultIterationCount = 123// will use 555 iterations specified in the testforAll<String, String>(555) { a,b -> a + b == "$a$b" }// will use 123 iterations from the global defaultforAll<String, String> { a,b -> a + b == "$a$b" }
```

Example 2 (kotlin):
```kotlin
class KotestConfig : AbstractProjectConfig() {   override suspend fun beforeProject() {      PropertyTesting.defaultIterationCount = 123   }}
```

---

## Arrow Generators | Kotest

**URL:** https://kotest.io/docs/5.8.x/proptest/property-test-generators-arrow.html

**Contents:**
- Arrow Generators

Kotest provides an optional module that provides generators for Arrow.

To use, add io.kotest.extensions:kotest-property-arrow:versionand io.arrow-kt:arrow-core:arrow-version to your build. This holds true for the optics module kotest-property-arrow-optics, by adding io.arrow-kt:arrow-optics:arrow-version, too.

---

## Generators | Kotest

**URL:** https://kotest.io/docs/proptest/property-test-generators.html

**Contents:**
- Generators
- Arbitrary​
- Exhaustive​

Generated values are provided by instances of the sealed class Gen. You can think of a Gen as kind of like an input stream but for property test values. Each Gen will provide a (usually) infinite stream of these values for one particular type.

Kotest has two types of generators - Arb for generating arbitrary (random) values and Exhaustive for generating a finite set of values in a closed space.

Both types of gens can be mixed and matched in property tests. For example, you could test a function with 100 random positive integers (an arb) alongside every even number from 0 to 200 (exhaustive).

Some generators are only available on the JVM. See the full list here.

Arbs generate two types of values - a hard coded set of edge cases and an infinite stream of randomly chosen samples.

The samples may be repeated, and some values may never be generated at all. For example generating 1000 integers between 0 and Int.MAX cannot return all possible values, and some values may happen to be generated more than once. Similarly, generating 1000 random integers between 0 and 500 will definitely result in some values appearing more than once.

Some common arbitraries include numbers with or without a range, strings in the unicode set, random lists, data classes with random parameters, emails, codepoints, chars and so on.

In addition to the random values, arbs may provide edge cases. One of the design features of Kotest's property testing is that values for some types will always include "common" edge cases that you probably want to be included in your tests.

For example, when testing a function that accepts an integer, you probably want to ensure that, at the very least, it is tested with zero, a positive number and a negative number. If only random values were provided, the chances of zero appearing would be fairly low, so Kotest will always provide some "edge cases" for integers (unless you specify otherwise).

When executing tests, the framework will alternate randomly between samples and edge cases. The split is determined by a configuration value which defaults to 2% edge cases.

Not all arbs have edge cases, but the arbs for the most common types do. Here are some examples of edge cases used by some arbs:

Exhaustives generate all values from a given space. This is useful when you want to ensure every value in that space is used. For example, for enum values, it is usually more helpful to ensure each enum is used, rather than picking randomly from the enums values and potentially missing some and duplicating others.

Typical exhaustives include small collections, enums, boolean values, powerset of a list or set, pre-defined integer ranges, and predefined string ranges.

Once an exhaustive has provided all it's values, it will loop and start again, so an exhaustive can be used in a test that requires any number of inputs.

Here we asked for 100 iterations, so each value of Season would be provided 25 times on average, but the exact counts will randomly change from run to run, and are very likely not to be exactly 25.

**Examples:**

Example 1 (php):
```php
enum class Season { Winter, Fall, Spring, Summer }forAll<Int, Season>(100) { a, season -> ... }
```

---

## Generators List | Kotest

**URL:** https://kotest.io/docs/5.8.x/proptest/property-test-generators-list.html

**Contents:**
- Generators List

This page lists all current generators in Kotest. There are two types of generator: arbitrary and exhaustive.

Most generators are available on all platforms. Some are JVM or JS specific.

We also provide generators for Arrow as a separate module.

---

## Kotlinx DateTime Gens | Kotest

**URL:** https://kotest.io/docs/5.7.x/proptest/kotlinx-datetime-gens.html

**Contents:**
- Kotlinx DateTime Gens

Kotest provides an optional module that provides generators for KotlinX DateTime.

To use, add io.kotest.extensions:kotest-property-datetime:version to your build.

---

## Generators List | Kotest

**URL:** https://kotest.io/docs/5.2.x/proptest/property-test-generators-list.html

**Contents:**
- Generators List

This page lists all current generators in Kotest. There are two types of generator: arbitrary and exhaustive.

Most generators are available on all platforms. Some are JVM or JS specific.

We also provide generators for Arrow as a separate module.

---

## Reflective Arbs | Kotest

**URL:** https://kotest.io/docs/5.3.x/proptest/reflective-arbs.html

**Contents:**
- Reflective Arbs

When running tests on JVM, Kotest supports generating more complex Arbs automatically. This can be useful when you have a data class which carries a simple combination of data which can already be automatically derived.

Reflective binding is supported for:

**Examples:**

Example 1 (kotlin):
```kotlin
enum class Currency {  USD, GBP, EUR}data class CurrencyAmount(  val amount: Long,  val currency: Currency)context("All currencies converts to EUR") { // In some spec  checkAll(Arb.bind<CurrencyAmount>()) { currencyAmount ->    val converted = currencyAmount.convertTo(EUR)    converted.currency shouldBe EUR  }}
```

---

## Configuration | Kotest

**URL:** https://kotest.io/docs/6.0/proptest/property-test-config.html

**Contents:**
- Configuration
  - Seed​
  - Min Failure​
  - PropTestListener​
  - Handling Unprintable Characters in Failure Messages​

Kotest provides for the ability to specify some configuration options when running a property test. We do this by passing in an instance of PropTestConfig to the test methods.

A commonly used configuration option is specifying the seed used by the random source. This is used when you want to repeat the same values each time the test is run. You might want to do this if you find a test failure, and you want to ensure that that particular set of values continues to be executed in the future as a regression test.

For full details on how the seed is used click here.

By default, Kotest tolerates no failure. Perhaps you want to run some non-deterministic test a bunch of times, and you're happy to accept some small number of failures. You can specify that in config.

Sometimes in property test it is required to perform some setup and tear down in each iteration of test. For this purpose you can register a PropTestListener with PropTestConfig.

When property tests involve strings with unprintable characters, failure messages can be hard to read and debug. By setting outputHexForUnprintableChars to true in PropTestConfig, unprintable characters in failure messages are displayed as their Unicode code points in the format U+XXXX.

Default Value: outputHexForUnprintableChars is false by default.

Alternatively, you can set this option as a common project-wide setting in a kotest.properties file located in your classpath:

**Examples:**

Example 1 (kotlin):
```kotlin
class PropertyExample: FreeSpec({   "String size" {      forAll<String, String>(PropTestConfig(options here...)) { a,b ->         (a + b).length == a.length + b.length      }   }})
```

Example 2 (kotlin):
```kotlin
class PropertyExample: FreeSpec({   "some flakey test" {      forAll<String, String>(PropTestConfig(maxFailure = 3)) { a,b ->         // max of 3 inputs can fail      }   }})
```

Example 3 (kotlin):
```kotlin
class PropertyExample: FreeSpec({   "some property test which require setup and tear down in each iteration" {      forAll<String, String>(PropTestConfig(listeners = listOf(MyPropTestListener))) { a,b ->         // some assertion      }   }})
```

Example 4 (kotlin):
```kotlin
class PropertyExample : FreeSpec({    "handle unprintable characters in failure messages" {        forAll<String>(            PropTestConfig(outputHexForUnprintableChars = true)        ) { str ->            // some assertion        }    }})
```

---

## Property Test Functions | Kotest

**URL:** https://kotest.io/docs/proptest/property-test-functions.html

**Contents:**
- Property Test Functions
  - For All​
  - Check All​
  - Iterations​
  - Specifying Generators​

There are two variants of functions that are used to execute a property test in Kotest: forAll and checkAll.

The first, forAll, accepts an n-arity function (a, ..., n) -> Boolean that tests the property. The test will pass if, for all input values, the function returns true.

Notice that this functions accepts type parameters for the argument types, with arity up to 14. Kotest uses these type parameters to locate a generator which provides (generates) random values of a suitable type.

For example, forAll<String, Int, Boolean> { a, b, c -> } is a 3-arity property test where argument a is a random String, argument b is a random int, and argument c is a random boolean.

The second, checkAll, accepts an n-arity function (a, ..., n) -> Unit in which you can simply execute assertions against the inputs. This approach will consider a test valid if no exceptions are thrown. Here is the same example again written in the equivalent way using checkAll.

The second approach is more general purpose than returning a boolean, but the first approach is from the original haskell libraries that inspired this library.

By default, Kotest will run the property test 1000 times. We can easily customize this by specifying the iteration count when invoking the test method.

Let's say we want to run a test 10,000 times.

You saw in the previous examples that Kotest would provide values automatically based on the type parameter(s). It does this by locating a generator that generates values for the required type.

For example, the automatically provided Integer generator generates random ints from all possible values - negative, positive, infinities, zero and so on.

This is fine for basic tests but often we want more control over the sample space. For example, we may want to test a function for numbers in a certain range only.

Then you would need to specify the generator(s) manually.

You can see we created two tests and in each test passed a generator into the forAll function with a suitable int range.

See here for a list of the built in generators.

**Examples:**

Example 1 (kotlin):
```kotlin
class PropertyExample: FreeSpec({   "String size" {      forAll<String, String> { a, b ->         (a + b).length == a.length + b.length      }   }})
```

Example 2 (kotlin):
```kotlin
class PropertyExample: FreeSpec({   "String size" {      checkAll<String, String> { a, b ->         a + b shouldHaveLength a.length + b.length      }   }})
```

Example 3 (kotlin):
```kotlin
class PropertyExample: FreeSpec({   "a many iterations test" {      checkAll<Double, Double>(10_000) { a, b ->         // test here      }   }})
```

Example 4 (kotlin):
```kotlin
class PropertyExample: FreeSpec({   "is allowed to drink in Chicago" {      forAll(Arb.int(21..150)) { a ->         isDrinkingAge(a) // assuming some function that calculates if we're old enough to drink      }   }   "is allowed to drink in London" {      forAll(Arb.int(18..150)) { a ->         isDrinkingAge(a) // assuming some function that calculates if we're old enough to drink      }   }})
```

---

## Arrow Generators | Kotest

**URL:** https://kotest.io/docs/proptest/property-test-generators-arrow.html

**Contents:**
- Arrow Generators

Kotest provides an optional module that provides generators for Arrow.

To use, add io.kotest.extensions:kotest-property-arrow:versionand io.arrow-kt:arrow-core:arrow-version to your build. This holds true for the optics module kotest-property-arrow-optics, by adding io.arrow-kt:arrow-optics:arrow-version, too.

---

## Shrinking | Kotest

**URL:** https://kotest.io/docs/5.3.x/proptest/property-test-shrinking.html

**Contents:**
- Shrinking
- Shrinking for built-in generators​
- Shrinking for custom generators​

In property-based testing, the initially found failing case may contain a lot of complexity that does actually cause the test to fail. Shrinking is the mechanism through which a property-based testing framework can simplify failing cases in order to find out the minimal reproducible case is. In Kotest, the way in which failing cases from generators are shrunk is defined by implementations of the Shrinker interface. Built-in generators generally have a default Shrinker defined by the framework, while custom generators can be given a custom Shrinker implementation.

Built-in generators (see Generators List) have a default Shrinker defined by the framework. A shrink function takes as input the value that failed the test and returns a list of new values on which Kotest can appy the test. The exact behaviour depends on the data type. For instance, a string could be shrunk by dropping the first or last character while for integers we could decrement or halve the value. In addition, shrink behaviour is defined for edge cases such as an empty string or the integer 0. The shrinking is performed when a test that uses such generator fails.

If the test fails for one of the generated inputs then the shrinking result is shown:

By default, Kotest will shrink 1000 times. This behaviour is configurable. For example, if you want to continue shrinking without bounds:

Custom generators do not have a Shrinker defined by Kotest. Instead, custom Shrinkers can be implemented. Below is an example where the Shrinker returns coordinates that are next to the value itself.

**Examples:**

Example 1 (kotlin):
```kotlin
Arb.positiveInt().checkAll { i ->    calculateProperty(i) shouldBe true}
```

Example 2 (typescript):
```typescript
Property test failed for inputs0) 1792716902Caused by io.kotest.assertions.AssertionFailedError: expected:<1792716902> but was:<0> at    PropertyBasedTest$1$1$3$1.invokeSuspend(PropertyBasedTest.kt:54)    PropertyBasedTest$1$1$3$1.invoke(PropertyBasedTest.kt)    PropertyBasedTest$1$1$3$1.invoke(PropertyBasedTest.kt)    io.kotest.property.internal.ProptestKt$proptest$3$2.invokeSuspend(proptest.kt:45)Attempting to shrink arg 1792716902Shrink #1: 1 passShrink #2: 597572300 failShrink #3: 199190766 failShrink #4: 66396922 failShrink #5: 22132307 failShrink #6: 7377435 failShrink #7: 2459145 fail[...]Shrink #999: 29948 passShrink #1000: 44922 passShrink #1001: 59896 passShrink #1002: 89839 failShrink result (after 1002 shrinks) => 89839Caused by io.kotest.assertions.AssertionFailedError: expected:<89839> but was:<0> at    PropertyBasedTest$1$1$3$1.invokeSuspend(PropertyBasedTest.kt:54)    PropertyBasedTest$1$1$3$1.invoke(PropertyBasedTest.kt)    PropertyBasedTest$1$1$3$1.invoke(PropertyBasedTest.kt)    io.kotest.property.internal.ShrinkfnsKt$shrinkfn$1$1$smallestA$1.invokeSuspend(shrinkfns.kt:19)
```

Example 3 (kotlin):
```kotlin
Arb.positiveInt().checkAll(PropTestConfig(shrinkingMode = ShrinkingMode.Unbounded)) { i ->    calculateProperty(i) shouldBe true}
```

Example 4 (kotlin):
```kotlin
data class Coordinate(val x: Int, val y: Int)class CoordinateTest : FunSpec({    context("Coordinate Transformations") {        // Shrinker takes the four neighbouring coordinates        val coordinateShrinker = Shrinker<Coordinate> { c ->            listOf(                Coordinate(c.x - 1, c.y),                Coordinate(c.x, c.y - 1),                Coordinate(c.x + 1, c.y),                Coordinate(c.x, c.y + 1),            )        }        val coordinateArb = arbitrary(coordinateShrinker) {            Coordinate(Arb.nonNegativeInt().bind(), Arb.nonNegativeInt().bind())        }        test("Coordinates are always positive after transformation") {            coordinateArb.checkAll {                transform(it).x shouldBeGreaterThanOrEqualTo 0                transform(it).y shouldBeGreaterThanOrEqualTo 0            }        }    }})
```

---

## Generator Operations | Kotest

**URL:** https://kotest.io/docs/5.6.x/proptest/generator-operations.html

**Contents:**
- Generator Operations
- Next​
- Filter​
- Map​
- FlatMap​
- Merging​
- Bind​

If you want to use an Arb to just return a value (even outside of a property test), then you can call next on it.

If you have an arb and you want to create a new arb that provides a subset of values, you can call filter on the source arb. For example, one way of generating even numbers is to take the integer arb, and filter out odd values. Viz:

If you have an arb and you want to transform the value generated, you can use map.

If you have an arb whose emission or edge cases depends on the emission of the previous arbitraries, you can use flatMap.

Two generators can be merged together, so that the resulting elements are equally sampled from both generators.

So with the following example would have an equal chance to yield either "a" or "b" on each random sample:

For merging more than two arbitraries, Arb.choice or Arb.choose might be more appropriate. For instance we can use:

Bind is useful if you want to apply multiple arbitraries. We can take a look at how we might construct values for a data class using bind.

**Examples:**

Example 1 (kotlin):
```kotlin
val arbA: Arb<A> = ...val a = arbA.next() // use Random.Defaultval a2 = arbA.next(rs) // pass in Random
```

Example 2 (kotlin):
```kotlin
val evens = Arb.int().filter { it.value % 2 == 0 }val odds = Arb.int().filter { it.value % 2 == 1 }
```

Example 3 (kotlin):
```kotlin
val integerStrings: Arb<String> = Arb.int().map { it.toString() }
```

Example 4 (kotlin):
```kotlin
val dependentArbs: Arb<String> = Arb.of("foo", "bar").flatMap { prefix ->   Arb.int(1..10).map { integer ->      "${prefix}-${integer}"   }}
```

---

## Generator Operations | Kotest

**URL:** https://kotest.io/docs/5.5.x/proptest/generator-operations.html

**Contents:**
- Generator Operations
- Next​
- Filter​
- Map​
- FlatMap​
- Merging​
- Bind​

If you want to use an Arb to just return a value (even outside of a property test), then you can call next on it.

If you have an arb and you want to create a new arb that provides a subset of values, you can call filter on the source arb. For example, one way of generating even numbers is to take the integer arb, and filter out odd values. Viz:

If you have an arb and you want to transform the value generated, you can use map.

If you have an arb whose emission or edge cases depends on the emission of the previous arbitraries, you can use flatMap.

Two generators can be merged together, so that the resulting elements are equally sampled from both generators.

So with the following example would have an equal chance to yield either "a" or "b" on each random sample:

For merging more than two arbitraries, Arb.choice or Arb.choose might be more appropriate. For instance we can use:

Bind is useful if you want to apply multiple arbitraries. We can take a look at how we might construct values for a data class using bind.

**Examples:**

Example 1 (kotlin):
```kotlin
val arbA: Arb<A> = ...val a = arbA.next() // use Random.Defaultval a2 = arbA.next(rs) // pass in Random
```

Example 2 (kotlin):
```kotlin
val evens = Arb.int().filter { it.value % 2 == 0 }val odds = Arb.int().filter { it.value % 2 == 1 }
```

Example 3 (kotlin):
```kotlin
val integerStrings: Arb<String> = Arb.int().map { it.toString() }
```

Example 4 (kotlin):
```kotlin
val dependentArbs: Arb<String> = Arb.of("foo", "bar").flatMap { prefix ->   Arb.int(1..10).map { integer ->      "${prefix}-${integer}"   }}
```

---

## Arrow Generators | Kotest

**URL:** https://kotest.io/docs/6.0/proptest/property-test-generators-arrow.html

**Contents:**
- Arrow Generators

Kotest provides an optional module that provides generators for Arrow.

To use, add io.kotest.extensions:kotest-property-arrow:versionand io.arrow-kt:arrow-core:arrow-version to your build. This holds true for the optics module kotest-property-arrow-optics, by adding io.arrow-kt:arrow-optics:arrow-version, too.

---

## Extra Arbs | Kotest

**URL:** https://kotest.io/docs/5.4.x/proptest/property-test-extra-arbs.html

**Contents:**
- Extra Arbs

If you are looking for a collection of Arbs for general purpose data generation, then Kotest has such a collection.

To use, add io.kotest.extensions:kotest-property-arbs:version to your build.

---

## Reflective Arbs | Kotest

**URL:** https://kotest.io/docs/6.0/proptest/reflective-arbs.html

**Contents:**
- Reflective Arbs
- Provided Arbs​

When running tests on JVM, Kotest supports generating more complex Arbs automatically. The generated Arb relies on build-in default and further reflective Arbs to populate the class parameters. If you just need to create and instance and don't need filtering you can use the class type in the checkAll/forAll calls directly. When you want to obtain the Arb to manipulate it further or filter invalid values, you can use Arb.bind with the type argument to obtain the Arb. If the required type depends on types that are not supported by default, it is possible to provide Arbs for those types in the call to Arb.bind.

Reflective binding is supported for:

When doing reflective binding, Kotest supports a builder API to provide Arbs for specific types (classes) and properties. Binding specific properties allow greater control in cases where types might be more widely used, like primitives for instance.

**Examples:**

Example 1 (kotlin):
```kotlin
enum class Currency {  USD, GBP, EUR}class CurrencyAmount(  val amount: Long,  val currency: Currency)context("Currencies converts to EUR") { // In some spec  checkAll(Arb.bind<CurrencyAmount>().filter { it.currency != EUR }) { currencyAmount ->    val converted = currencyAmount.convertTo(EUR)    converted.currency shouldBe EUR  }}context("Converting to a currency and back yields the same amount") { // In some spec  checkAll<CurrencyAmount, Currency>() { currencyAmount, currency ->    val converted = currencyAmount.convertTo(currency).convertTo(currencyAmount.currency)    converted.currency shouldBe currencyAmount.currency  }}
```

Example 2 (kotlin):
```kotlin
data class User(  val name: String,  val password: String,  val age: Int,)// in some speccontext("Some tests with an arbitrary user") {  checkAll(Arb.bind<User> {    bind(User::name to Arb.string(1..10))    bind(User::password to Arb.string(24..80)) // binds a specific property to an arb    bind(Int::class to Arb.int(0..100))  // binds a type to an arb  }) { user ->    // ...  }}
```

---

## Property-based Testing | Kotest

**URL:** https://kotest.io/docs/5.3.x/proptest/property-based-testing.html

**Contents:**
- Property-based Testing
- What is Property Testing?​
- Getting Started​
- Next Steps​

Kotest is split into several subprojects which can be used independently. One of these subprojects is the property test framework. You do not need to be using Kotest as your test framework (although you should!) to benefit from the property test support.

Developers typically write example-based tests. These are your garden variety unit tests you know and love. You provide some inputs, and some expected results, and a test framework like Kotest or JUnit checks that the actual results meet the expectations.

One problem with this approach is that it is very easy to miss errors due to edge cases that the developer didn't think about, or lack of coverage in the chosen inputs. Instead, with property testing, hundreds or thousands of values are fed into the same test, and the values are (usually) randomly generated by your property test framework.

For example, a good property test framework will include values like negative infinity, empty lists, strings with non-ascii characters, and so on. Things we often forget about when writing example based tests.

Property tests were originally conceived in frameworks like Quickcheck with the notion of testing a property on some object, ie. something that should hold true for all inputs. An invariant in other words. An example of an invariant is given two strings, a and b, then length(a + b) should always be equal to length(a) + length(b).

That is where the term property testing originates.

However, you do not have to use property tests to only test things like monad laws or basic numeric functions. Any test that would benefit from a wide array of input values is a good candidate. For example, we might have a function that validates usernames, and we want to test that valid emails are accepted. A property test would be useful here in generating 1000s of combinations to help harden our validation logic.

The property test framework is supported on all targets.

Add the following dependency to your build:

Add the following dependency to your build.

Add the following dependency to your commonTest sourceset:

Alternatively, add the dependency to a specific target. For example, we could add to the Javascript target only be using the jsTest sourceset.

To create input values for tests, Kotest uses the term generator. One generator per input argument is passed to a test function, and the test will execute for a set number of iterations.

**Examples:**

Example 1 (kotlin):
```kotlin
dependencies {   testImplementation("io.kotest:kotest-property:$version")}
```

Example 2 (xml):
```xml
<dependency>   <groupId>io.kotest</groupId>   <artifactId>kotest-property-jvm</artifactId>   <version>${version}</version>   <scope>test</scope></dependency>
```

Example 3 (kotlin):
```kotlin
kotlin {  sourceSets {    val commonTest by getting {      dependencies {        implementation("io.kotest:kotest-property:$version")      }    }  }}
```

Example 4 (kotlin):
```kotlin
kotlin {   targets {      js {         browser()         nodejs()      }   }   sourceSets {      val jsTest by getting {         dependencies {            implementation("io.kotest:kotest-property:$version")         }      }   }}
```

---

## Configuration | Kotest

**URL:** https://kotest.io/docs/5.9.x/proptest/property-test-config.html

**Contents:**
- Configuration
  - Seed​
  - Min Failure​
  - PropTestListener​

Kotest provides for the ability to specify some configuration options when running a property test. We do this by passing in an instance of PropTestConfig to the test methods.

A commonly used configuration option is specifying the seed used by the random source. This is used when you want to repeat the same values each time the test is run. You might want to do this if you find a test failure, and you want to ensure that that particular set of values continues to be executed in the future as a regression test.

For full details on how the seed is used click here.

By default, Kotest tolerates no failure. Perhaps you want to run some non-deterministic test a bunch of times, and you're happy to accept some small number of failures. You can specify that in config.

Sometimes in property test it is required to perform some setup and tear down in each iteration of test. For this purpose you can register a PropTestListener with PropTestConfig.

**Examples:**

Example 1 (kotlin):
```kotlin
class PropertyExample: StringSpec({   "String size" {      forAll<String, String>(PropTestConfig(options here...)) { a,b ->         (a + b).length == a.length + b.length      }   }})
```

Example 2 (kotlin):
```kotlin
class PropertyExample: StringSpec({   "some flakey test" {      forAll<String, String>(PropTestConfig(maxFailure = 3)) { a,b ->         // max of 3 inputs can fail      }   }})
```

Example 3 (kotlin):
```kotlin
class PropertyExample: StringSpec({   "some property test which require setup and tear down in each iteration" {      forAll<String, String>(PropTestConfig(listeners = listOf(MyPropTestListener))) { a,b ->         // some assertion      }   }})
```

---

## Reflective Arbs | Kotest

**URL:** https://kotest.io/docs/5.4.x/proptest/reflective-arbs.html

**Contents:**
- Reflective Arbs

When running tests on JVM, Kotest supports generating more complex Arbs automatically. The generated Arb relies on build-in default and further reflective Arbs to populate the class parameters. If you just need to create and instance and don't need filtering you can use the class type in the checkAll/forAll calls directly. When you want to obtain the Arb to manipulate it further or filter invalid values, you can use Arb.bind with the type argument to obtain the Arb. If the required type depends on types that are not supported by default, it is possible to provide Arbs for those types in the call to Arb.bind.

Reflective binding is supported for:

**Examples:**

Example 1 (kotlin):
```kotlin
enum class Currency {  USD, GBP, EUR}class CurrencyAmount(  val amount: Long,  val currency: Currency)context("Currencies converts to EUR") { // In some spec  checkAll(Arb.bind<CurrencyAmount>().filter { it.currency != EUR }) { currencyAmount ->    val converted = currencyAmount.convertTo(EUR)    converted.currency shouldBe EUR  }}context("Converting to a currency and back yields the same amount") { // In some spec  checkAll<CurrencyAmount, Currency>() { currencyAmount, currency ->    val converted = currencyAmount.convertTo(currency).convertTo(currencyAmount.currency)    converted.currency shouldBe currencyAmount.currency  }}
```

---

## Extra Arbs | Kotest

**URL:** https://kotest.io/docs/5.7.x/proptest/property-test-extra-arbs.html

**Contents:**
- Extra Arbs

If you are looking for a collection of Arbs for general purpose data generation, then Kotest has such a collection.

To use, add io.kotest.extensions:kotest-property-arbs:version to your build.

---

## Statistics | Kotest

**URL:** https://kotest.io/docs/next/proptest/property-test-statistics.html

**Contents:**
- Statistics
- Labels​
  - Report Mode​
  - Checking Coverage of Statistics​
  - Custom Reports​

Sometimes you may like to know the types of values being generated by Kotest, to ensure that your generators are configured in the way you expect. Property test statistics are designed to fulfil this need.

The function collect is the entry point to statistcs and is used to count categories of values. We use this by invoking it inside a property test with the category we want to increment.

For example, lets say we wanted to gather statistics on the values of RoundingMode used by a BigDecimal. We would invoke checkAll as normal, passing the rounding mode to the collect function.

Now after the test is completed, Kotest will output the test name, and the counts/percentages for each category:

The category we use does not have to be an enum. It can be any object, and you can wrap in conditionals if you want more control. For example:

Sometimes you may wish to have orthogonal sets of statistics. For example, for a simple number test, we might want to confirm that a certain percentage are even numbers, and a certain percentage are negative. One way would be to have EVEN_POS, EVEN_NEG, ODD_POS, ODD_NEG:

This gives us one set of outputs:

However, as the combinations grow this will become unwieldy, so Kotest supports labelled statistics. You can think of this as distinct sets of statistics. To use labels, just pass the label name as the first arg to the collect method.

Now, Kotest will output multiple sets of statistics, with the label name in the title:

By default, statistics are printed for every property test. There are four modes which can be configured using the global configuration object PropertyTesting.

The possible options are:

If you wish to programmatically assert that certain values are being generated, then you can use the specify constraints that must be met.

For example, in our previous rounding example, we can check that at least 10% of inputs are covering HALF_DOWN, and 10% are covering FLOOR using withCoveragePercentages:

If we want to check by absolute numbers rather than percentages, we can use withCoverageCounts:

You can customize the report format, or generate reports from the raw data, by using your own instance of StatisticsReporter. This is configured via the global configuration object PropertyTesting.

**Examples:**

Example 1 (kotlin):
```kotlin
checkAll(Arb.enum<RoundingMode>(), Arb.bigDecimal()) { mode, decimal ->  collect(mode)  // test here}
```

Example 2 (yaml):
```yaml
Statistics: [collecting stats] (1000 iterations, 1 args)HALF_DOWN                                                     142 (14%)HALF_UP                                                       141 (14%)CEILING                                                       132 (13%)FLOOR                                                         122 (12%)UP                                                            119 (12%)UNNECESSARY                                                   119 (12%)HALF_EVEN                                                     118 (12%)DOWN                                                          107 (11%)
```

Example 3 (kotlin):
```kotlin
checkAll(Arb.int()) { k ->  when {    k % 2 == 0 -> collect("EVEN")    else -> collect("ODD")  }  // test here}
```

Example 4 (kotlin):
```kotlin
checkAll(Arb.int()) { k ->  when {    k > 0 && k % 2 == 0 -> collect("EVEN_POS")    k % 2 == 0 -> collect("EVEN_NEG")    k > 0 -> collect("ODD_POS")    else -> collect("ODD_NEG")  }  // test here}
```

---

## Configuration | Kotest

**URL:** https://kotest.io/docs/5.4.x/proptest/property-test-config.html

**Contents:**
- Configuration
  - Seed​
  - Min Failure​
  - PropTestListener​

Kotest provides for the ability to specify some configuration options when running a property test. We do this by passing in an instance of PropTestConfig to the test methods.

A commonly used configuration option is specifying the seed used by the random source. This is used when you want to repeat the same values each time the test is run. You might want to do this if you find a test failure, and you want to ensure that that particular set of values continues to be executed in the future as a regression test.

For full details on how the seed is used click here.

By default, Kotest tolerates no failure. Perhaps you want to run some non-deterministic test a bunch of times, and you're happy to accept some small number of failures. You can specify that in config.

Sometimes in property test it is required to perform some setup and tear down in each iteration of test. For this purpose you can register a PropTestListener with PropTestConfig.

**Examples:**

Example 1 (kotlin):
```kotlin
class PropertyExample: StringSpec({   "String size" {      forAll<String, String>(PropTestConfig(options here...)) { a,b ->         (a + b).length == a.length + b.length      }   }})
```

Example 2 (kotlin):
```kotlin
class PropertyExample: StringSpec({   "some flakey test" {      forAll<String, String>(PropTestConfig(maxFailure = 3)) { a,b ->         // max of 3 inputs can fail      }   }})
```

Example 3 (kotlin):
```kotlin
class PropertyExample: StringSpec({   "some property test which require setup and tear down in each iteration" {      forAll<String, String>(PropTestConfig(listeners = listOf(MyPropTestListener))) { a,b ->         // some assertion      }   }})
```

---

## Assumptions | Kotest

**URL:** https://kotest.io/docs/proptest/property-test-assumptions.html

**Contents:**
- Assumptions
  - Assertions​
  - Max Discard Percentage​

To restrict the set the generated values from a generator, we can use filter to return a constrained arb.

However if we need to restrict inputs based on the relationship between values, then filtering on a single arb won't work. Consider an example where we want to test that two non-equal strings have a non-zero Levenshtein distance.

This will periodically fail - whenever two equal strings are generated. One approach would be to just wrap the tests in an if/else block and avoid those undesired inputs.

But in more complicated scenarios we could easily introduce a bug and filter all our inputs.

Kotest provides a feature called assumptions that will filter out unwanted combinations, while tracking that we are not filtering too many.

An assumption accepts a boolean value passed to the withAssumptions function, that if true, will allow the property test to continue, but if false, that particular iteration is skipped. For example, the previous example will now pass:

Alternatively, you can use inline syntax:

Kotest expands on basic boolean assumptions by allowing you to specify assertions in an assumption function.

For example, building on the previous example:

Here we are ensuring that all inputs are not equal, and that the inputs have the same length. Any assertion that throws AssertionError can be used here, including all the assertions provided by Kotest.

This also supports inline syntax:

By default, the maximum discard percentage is 10%. If more combinations are discarded than that, the property test will fail. This helps avoid a scenario where we erroneously discard too many, or even all, our inputs.

For example, the following would fail by default because we would be filtering ~50% of values.

But if we wanted to allow this regardless, we can use the maxDiscardPercentage to increase the allowed discard rate.

It is generally better to adjust your arbs to produce values closer to what you need, so that you only need to filter out unwanted edge cases.

**Examples:**

Example 1 (kotlin):
```kotlin
val evens = Arb.int().filter { it.value % 2 == 0 }
```

Example 2 (kotlin):
```kotlin
checkAll<String, String> { a, b ->  levenshtein(a, b) shouldBeGreaterThan 0}
```

Example 3 (kotlin):
```kotlin
checkAll<String, String> { a, b ->  if (a != b)    levenshtein(a, b) shouldBeGreaterThan 0}
```

Example 4 (kotlin):
```kotlin
checkAll<String, String> { a, b ->  withAssumptions(a != b) {    levenshtein(a, b) shouldBeGreaterThan 0  }}
```

---

## Reflective Arbs | Kotest

**URL:** https://kotest.io/docs/5.5.x/proptest/reflective-arbs.html

**Contents:**
- Reflective Arbs

When running tests on JVM, Kotest supports generating more complex Arbs automatically. The generated Arb relies on build-in default and further reflective Arbs to populate the class parameters. If you just need to create and instance and don't need filtering you can use the class type in the checkAll/forAll calls directly. When you want to obtain the Arb to manipulate it further or filter invalid values, you can use Arb.bind with the type argument to obtain the Arb. If the required type depends on types that are not supported by default, it is possible to provide Arbs for those types in the call to Arb.bind.

Reflective binding is supported for:

**Examples:**

Example 1 (kotlin):
```kotlin
enum class Currency {  USD, GBP, EUR}class CurrencyAmount(  val amount: Long,  val currency: Currency)context("Currencies converts to EUR") { // In some spec  checkAll(Arb.bind<CurrencyAmount>().filter { it.currency != EUR }) { currencyAmount ->    val converted = currencyAmount.convertTo(EUR)    converted.currency shouldBe EUR  }}context("Converting to a currency and back yields the same amount") { // In some spec  checkAll<CurrencyAmount, Currency>() { currencyAmount, currency ->    val converted = currencyAmount.convertTo(currency).convertTo(currencyAmount.currency)    converted.currency shouldBe currencyAmount.currency  }}
```

---

## Shrinking | Kotest

**URL:** https://kotest.io/docs/5.8.x/proptest/property-test-shrinking.html

**Contents:**
- Shrinking
- Shrinking for built-in generators​
- Shrinking for custom generators​

In property-based testing, the initially found failing case may contain a lot of complexity that does actually cause the test to fail. Shrinking is the mechanism through which a property-based testing framework can simplify failing cases in order to find out the minimal reproducible case is. In Kotest, the way in which failing cases from generators are shrunk is defined by implementations of the Shrinker interface. Built-in generators generally have a default Shrinker defined by the framework, while custom generators can be given a custom Shrinker implementation.

Built-in generators (see Generators List) have a default Shrinker defined by the framework. A shrink function takes as input the value that failed the test and returns a list of new values on which Kotest can appy the test. The exact behaviour depends on the data type. For instance, a string could be shrunk by dropping the first or last character while for integers we could decrement or halve the value. In addition, shrink behaviour is defined for edge cases such as an empty string or the integer 0. The shrinking is performed when a test that uses such generator fails.

If the test fails for one of the generated inputs then the shrinking result is shown:

By default, Kotest will shrink 1000 times. This behaviour is configurable. For example, if you want to continue shrinking without bounds:

Custom generators do not have a Shrinker defined by Kotest. Instead, custom Shrinkers can be implemented. Below is an example where the Shrinker returns coordinates that are next to the value itself.

**Examples:**

Example 1 (kotlin):
```kotlin
Arb.positiveInt().checkAll { i ->    calculateProperty(i) shouldBe true}
```

Example 2 (typescript):
```typescript
Property test failed for inputs0) 1792716902Caused by io.kotest.assertions.AssertionFailedError: expected:<1792716902> but was:<0> at    PropertyBasedTest$1$1$3$1.invokeSuspend(PropertyBasedTest.kt:54)    PropertyBasedTest$1$1$3$1.invoke(PropertyBasedTest.kt)    PropertyBasedTest$1$1$3$1.invoke(PropertyBasedTest.kt)    io.kotest.property.internal.ProptestKt$proptest$3$2.invokeSuspend(proptest.kt:45)Attempting to shrink arg 1792716902Shrink #1: 1 passShrink #2: 597572300 failShrink #3: 199190766 failShrink #4: 66396922 failShrink #5: 22132307 failShrink #6: 7377435 failShrink #7: 2459145 fail[...]Shrink #999: 29948 passShrink #1000: 44922 passShrink #1001: 59896 passShrink #1002: 89839 failShrink result (after 1002 shrinks) => 89839Caused by io.kotest.assertions.AssertionFailedError: expected:<89839> but was:<0> at    PropertyBasedTest$1$1$3$1.invokeSuspend(PropertyBasedTest.kt:54)    PropertyBasedTest$1$1$3$1.invoke(PropertyBasedTest.kt)    PropertyBasedTest$1$1$3$1.invoke(PropertyBasedTest.kt)    io.kotest.property.internal.ShrinkfnsKt$shrinkfn$1$1$smallestA$1.invokeSuspend(shrinkfns.kt:19)
```

Example 3 (kotlin):
```kotlin
Arb.positiveInt().checkAll(PropTestConfig(shrinkingMode = ShrinkingMode.Unbounded)) { i ->    calculateProperty(i) shouldBe true}
```

Example 4 (kotlin):
```kotlin
data class Coordinate(val x: Int, val y: Int)class CoordinateTest : FunSpec({    context("Coordinate Transformations") {        // Shrinker takes the four neighbouring coordinates        val coordinateShrinker = Shrinker<Coordinate> { c ->            listOf(                Coordinate(c.x - 1, c.y),                Coordinate(c.x, c.y - 1),                Coordinate(c.x + 1, c.y),                Coordinate(c.x, c.y + 1),            )        }        val coordinateArb = arbitrary(coordinateShrinker) {            Coordinate(Arb.nonNegativeInt().bind(), Arb.nonNegativeInt().bind())        }        test("Coordinates are always positive after transformation") {            coordinateArb.checkAll {                transform(it).x shouldBeGreaterThanOrEqualTo 0                transform(it).y shouldBeGreaterThanOrEqualTo 0            }        }    }})
```

---

## Property Test Seeds | Kotest

**URL:** https://kotest.io/docs/next/proptest/property-test-seeds.html

**Contents:**
- Property Test Seeds
  - Manually specifying the seed​
  - Rerunning failed seeds​
  - Failing when seeds set​

When a property test is executed, the values are generated using a random source that is created from a seed value. By default this seed value is itself randomly chosen (using the default kotlin.random.Random instance). However, there are times when this value needs to be fixed or repeated.

You can change the default used by all tests, unless overriden through the options listed below, by changing the configuration value PropertyTesting.defaultSeed.

To manually set the seed, pass an instance of PropTestConfig to your prop test methods. You might want to do this if you find a test failure, and you want to ensure that those values continue to be executed in the future as a regression test.

Whenever a property test fails, Kotest will output the seed that was used. You can duplicate the test, setting it to use this seed so you have permanent regression test for those values.

By default, when a property test fails, the seed used by that test is written to a file in ~/.kotest/seeds/<spec>/<testname>. Whenever a property test runs, this seed is detected if the file exists, and used in place of a random seed. Next time the test is successful, the seed file will be removed.

A manually specified seed always takes precedence over a failed seed.

This feature can be disabled by setting PropertyTesting.writeFailedSeed = false

Some users prefer to avoid manually specifying seeds. They want to use them locally only, when developing, but to avoid checking them in. If this is your style, then set PropertyTesting.failOnSeed = false or the env var kotest.proptest.seed.fail-if-set to false on your server.

Then if a seed is detected, the test suite will fail.

**Examples:**

Example 1 (kotlin):
```kotlin
class PropertyExample : FreeSpec({  "String size" {    forAll<String, String>(PropTestConfig(seed = 127305235)) { a, b ->      (a + b).length == a.length + b.length    }  }})
```

---

## Configuration | Kotest

**URL:** https://kotest.io/docs/5.8.x/proptest/property-test-config.html

**Contents:**
- Configuration
  - Seed​
  - Min Failure​
  - PropTestListener​

Kotest provides for the ability to specify some configuration options when running a property test. We do this by passing in an instance of PropTestConfig to the test methods.

A commonly used configuration option is specifying the seed used by the random source. This is used when you want to repeat the same values each time the test is run. You might want to do this if you find a test failure, and you want to ensure that that particular set of values continues to be executed in the future as a regression test.

For full details on how the seed is used click here.

By default, Kotest tolerates no failure. Perhaps you want to run some non-deterministic test a bunch of times, and you're happy to accept some small number of failures. You can specify that in config.

Sometimes in property test it is required to perform some setup and tear down in each iteration of test. For this purpose you can register a PropTestListener with PropTestConfig.

**Examples:**

Example 1 (kotlin):
```kotlin
class PropertyExample: StringSpec({   "String size" {      forAll<String, String>(PropTestConfig(options here...)) { a,b ->         (a + b).length == a.length + b.length      }   }})
```

Example 2 (kotlin):
```kotlin
class PropertyExample: StringSpec({   "some flakey test" {      forAll<String, String>(PropTestConfig(maxFailure = 3)) { a,b ->         // max of 3 inputs can fail      }   }})
```

Example 3 (kotlin):
```kotlin
class PropertyExample: StringSpec({   "some property test which require setup and tear down in each iteration" {      forAll<String, String>(PropTestConfig(listeners = listOf(MyPropTestListener))) { a,b ->         // some assertion      }   }})
```

---

## Extra Arbs | Kotest

**URL:** https://kotest.io/docs/5.8.x/proptest/property-test-extra-arbs.html

**Contents:**
- Extra Arbs

If you are looking for a collection of Arbs for general purpose data generation, then Kotest has such a collection.

To use, add io.kotest.extensions:kotest-property-arbs:version to your build.

---

## Assumptions | Kotest

**URL:** https://kotest.io/docs/5.6.x/proptest/property-test-assumptions.html

**Contents:**
- Assumptions
  - Assertions​
  - Max Discard Percentage​

To restrict the set the generated values from a generator, we can use filter to return a constrained arb.

However if we need to restrict inputs based on the relationship between values, then filtering on a single arb won't work. Consider an example where we want to test that two non-equal strings have a non-zero Levenshtein distance.

This will periodically fail - whenever two equal strings are generated. One approach would be to just wrap the tests in an if/else block and avoid those undesired inputs.

But in more complicated scenarios we could easily introduce a bug and filter all our inputs.

Kotest provides a feature called assumptions that will filter out unwanted combinations, while tracking that we are not filtering too many.

An assumption accepts a boolean value passed to the withAssumptions function, that if true, will allow the property test to continue, but if false, that particular iteration is skipped. For example, the previous example will now pass:

Alternatively, you can use inline syntax:

Kotest expands on basic boolean assumptions by allowing you to specify assertions in an assumption function.

For example, building on the previous example:

Here we are ensuring that all inputs are not equal, and that the inputs have the same length. Any assertion that throws AssertionError can be used here, including all the assertions provided by Kotest.

This also supports inline syntax:

By default, the maximum discard percentage is 10%. If more combinations are discarded than that, the property test will fail. This helps avoid a scenario where we erroneously discard too many, or even all, our inputs.

For example, the following would fail by default because we would be filtering ~50% of values.

But if we wanted to allow this regardless, we can use the maxDiscardPercentage to increase the allowed discard rate.

It is generally better to adjust your arbs to produce values closer to what you need, so that you only need to filter out unwanted edge cases.

**Examples:**

Example 1 (kotlin):
```kotlin
val evens = Arb.int().filter { it.value % 2 == 0 }
```

Example 2 (kotlin):
```kotlin
checkAll<String, String> { a, b ->  levenshtein(a, b) shouldBeGreaterThan 0}
```

Example 3 (kotlin):
```kotlin
checkAll<String, String> { a, b ->  if (a != b)    levenshtein(a, b) shouldBeGreaterThan 0}
```

Example 4 (kotlin):
```kotlin
checkAll<String, String> { a, b ->  withAssumptions(a != b) {    levenshtein(a, b) shouldBeGreaterThan 0  }}
```

---

## Statistics | Kotest

**URL:** https://kotest.io/docs/6.0/proptest/property-test-statistics.html

**Contents:**
- Statistics
- Labels​
  - Report Mode​
  - Checking Coverage of Statistics​
  - Custom Reports​

Sometimes you may like to know the types of values being generated by Kotest, to ensure that your generators are configured in the way you expect. Property test statistics are designed to fulfil this need.

The function collect is the entry point to statistcs and is used to count categories of values. We use this by invoking it inside a property test with the category we want to increment.

For example, lets say we wanted to gather statistics on the values of RoundingMode used by a BigDecimal. We would invoke checkAll as normal, passing the rounding mode to the collect function.

Now after the test is completed, Kotest will output the test name, and the counts/percentages for each category:

The category we use does not have to be an enum. It can be any object, and you can wrap in conditionals if you want more control. For example:

Sometimes you may wish to have orthogonal sets of statistics. For example, for a simple number test, we might want to confirm that a certain percentage are even numbers, and a certain percentage are negative. One way would be to have EVEN_POS, EVEN_NEG, ODD_POS, ODD_NEG:

This gives us one set of outputs:

However, as the combinations grow this will become unwieldy, so Kotest supports labelled statistics. You can think of this as distinct sets of statistics. To use labels, just pass the label name as the first arg to the collect method.

Now, Kotest will output multiple sets of statistics, with the label name in the title:

By default, statistics are printed for every property test. There are four modes which can be configured using the global configuration object PropertyTesting.

The possible options are:

If you wish to programmatically assert that certain values are being generated, then you can use the specify constraints that must be met.

For example, in our previous rounding example, we can check that at least 10% of inputs are covering HALF_DOWN, and 10% are covering FLOOR using withCoveragePercentages:

If we want to check by absolute numbers rather than percentages, we can use withCoverageCounts:

You can customize the report format, or generate reports from the raw data, by using your own instance of StatisticsReporter. This is configured via the global configuration object PropertyTesting.

**Examples:**

Example 1 (kotlin):
```kotlin
checkAll(Arb.enum<RoundingMode>(), Arb.bigDecimal()) { mode, decimal ->  collect(mode)  // test here}
```

Example 2 (yaml):
```yaml
Statistics: [collecting stats] (1000 iterations, 1 args)HALF_DOWN                                                     142 (14%)HALF_UP                                                       141 (14%)CEILING                                                       132 (13%)FLOOR                                                         122 (12%)UP                                                            119 (12%)UNNECESSARY                                                   119 (12%)HALF_EVEN                                                     118 (12%)DOWN                                                          107 (11%)
```

Example 3 (kotlin):
```kotlin
checkAll(Arb.int()) { k ->  when {    k % 2 == 0 -> collect("EVEN")    else -> collect("ODD")  }  // test here}
```

Example 4 (kotlin):
```kotlin
checkAll(Arb.int()) { k ->  when {    k > 0 && k % 2 == 0 -> collect("EVEN_POS")    k % 2 == 0 -> collect("EVEN_NEG")    k > 0 -> collect("ODD_POS")    else -> collect("ODD_NEG")  }  // test here}
```

---

## Global Configuration | Kotest

**URL:** https://kotest.io/docs/next/proptest/property-test-global-config.html

**Contents:**
- Global Configuration
  - Default Iterations​
  - Printing Shrink Steps​

Some property test settings can be set globally for all property tests.

The standard default iteration count is 1000. This means when you don't specify the iteration count in a property test, the default will be 1000.

We can override this default either by assigning a value to PropertyTesting.defaultIterationCount, or by using the system property kotest.proptest.default.iteration.count.

Any test which directly sets the iteration count will of course use that value.

If you are using Kotest framework, then you can perform this action before any tests by using project config. For example:

By default, when using shrinking, each shrinking step will not be logged, but only the final shrunk value.

To enable logging of each intermediate value, assign true to PropertyTesting.shouldPrintShrinkSteps or use the system property kotest.proptest.output.shrink-steps=true.

**Examples:**

Example 1 (kotlin):
```kotlin
PropertyTesting.defaultIterationCount = 123// will use 555 iterations specified in the testforAll<String, String>(555) { a,b -> a + b == "$a$b" }// will use 123 iterations from the global defaultforAll<String, String> { a,b -> a + b == "$a$b" }
```

Example 2 (kotlin):
```kotlin
class KotestConfig : AbstractProjectConfig() {   override suspend fun beforeProject() {      PropertyTesting.defaultIterationCount = 123   }}
```

---

## Property-based Testing | Kotest

**URL:** https://kotest.io/docs/proptest/property-based-testing.html

**Contents:**
- Property-based Testing
- What is Property Testing?​
- Getting Started​
- Next Steps​

Kotest is split into several subprojects which can be used independently. One of these subprojects is the property test framework. You do not need to be using Kotest as your test framework (although you should!) to benefit from the property test support.

Developers typically write example-based tests. These are your garden variety unit tests you know and love. You provide some inputs, and some expected results, and a test framework like Kotest or JUnit checks that the actual results meet the expectations.

One problem with this approach is that it is very easy to miss errors due to edge cases that the developer didn't think about, or lack of coverage in the chosen inputs. Instead, with property testing, hundreds or thousands of values are fed into the same test, and the values are (usually) randomly generated by your property test framework.

For example, a good property test framework will include values like negative infinity, empty lists, strings with non-ascii characters, and so on. Things we often forget about when writing example based tests.

Property tests were originally conceived in frameworks like Quickcheck with the notion of testing a property on some object, ie. something that should hold true for all inputs. An invariant in other words. An example of an invariant is given two strings, a and b, then length(a + b) should always be equal to length(a) + length(b).

That is where the term property testing originates.

However, you do not have to use property tests to only test things like monad laws or basic numeric functions. Any test that would benefit from a wide array of input values is a good candidate. For example, we might have a function that validates usernames, and we want to test that valid emails are accepted. A property test would be useful here in generating 1000s of combinations to help harden our validation logic.

The property test framework is supported on all targets.

Add the following dependency to your build:

Add the following dependency to your build.

Add the following dependency to your commonTest sourceset:

Alternatively, add the dependency to a specific target. For example, we could add to the Javascript target only be using the jsTest sourceset.

To create input values for tests, Kotest uses the term generator. One generator per input argument is passed to a test function, and the test will execute for a set number of iterations.

**Examples:**

Example 1 (kotlin):
```kotlin
dependencies {   testImplementation("io.kotest:kotest-property:$version")}
```

Example 2 (xml):
```xml
<dependency>   <groupId>io.kotest</groupId>   <artifactId>kotest-property-jvm</artifactId>   <version>${version}</version>   <scope>test</scope></dependency>
```

Example 3 (kotlin):
```kotlin
kotlin {  sourceSets {    val commonTest by getting {      dependencies {        implementation("io.kotest:kotest-property:$version")      }    }  }}
```

Example 4 (kotlin):
```kotlin
kotlin {   targets {      js {         browser()         nodejs()      }   }   sourceSets {      val jsTest by getting {         dependencies {            implementation("io.kotest:kotest-property:$version")         }      }   }}
```

---

## Shrinking | Kotest

**URL:** https://kotest.io/docs/5.7.x/proptest/property-test-shrinking.html

**Contents:**
- Shrinking
- Shrinking for built-in generators​
- Shrinking for custom generators​

In property-based testing, the initially found failing case may contain a lot of complexity that does actually cause the test to fail. Shrinking is the mechanism through which a property-based testing framework can simplify failing cases in order to find out the minimal reproducible case is. In Kotest, the way in which failing cases from generators are shrunk is defined by implementations of the Shrinker interface. Built-in generators generally have a default Shrinker defined by the framework, while custom generators can be given a custom Shrinker implementation.

Built-in generators (see Generators List) have a default Shrinker defined by the framework. A shrink function takes as input the value that failed the test and returns a list of new values on which Kotest can appy the test. The exact behaviour depends on the data type. For instance, a string could be shrunk by dropping the first or last character while for integers we could decrement or halve the value. In addition, shrink behaviour is defined for edge cases such as an empty string or the integer 0. The shrinking is performed when a test that uses such generator fails.

If the test fails for one of the generated inputs then the shrinking result is shown:

By default, Kotest will shrink 1000 times. This behaviour is configurable. For example, if you want to continue shrinking without bounds:

Custom generators do not have a Shrinker defined by Kotest. Instead, custom Shrinkers can be implemented. Below is an example where the Shrinker returns coordinates that are next to the value itself.

**Examples:**

Example 1 (kotlin):
```kotlin
Arb.positiveInt().checkAll { i ->    calculateProperty(i) shouldBe true}
```

Example 2 (typescript):
```typescript
Property test failed for inputs0) 1792716902Caused by io.kotest.assertions.AssertionFailedError: expected:<1792716902> but was:<0> at    PropertyBasedTest$1$1$3$1.invokeSuspend(PropertyBasedTest.kt:54)    PropertyBasedTest$1$1$3$1.invoke(PropertyBasedTest.kt)    PropertyBasedTest$1$1$3$1.invoke(PropertyBasedTest.kt)    io.kotest.property.internal.ProptestKt$proptest$3$2.invokeSuspend(proptest.kt:45)Attempting to shrink arg 1792716902Shrink #1: 1 passShrink #2: 597572300 failShrink #3: 199190766 failShrink #4: 66396922 failShrink #5: 22132307 failShrink #6: 7377435 failShrink #7: 2459145 fail[...]Shrink #999: 29948 passShrink #1000: 44922 passShrink #1001: 59896 passShrink #1002: 89839 failShrink result (after 1002 shrinks) => 89839Caused by io.kotest.assertions.AssertionFailedError: expected:<89839> but was:<0> at    PropertyBasedTest$1$1$3$1.invokeSuspend(PropertyBasedTest.kt:54)    PropertyBasedTest$1$1$3$1.invoke(PropertyBasedTest.kt)    PropertyBasedTest$1$1$3$1.invoke(PropertyBasedTest.kt)    io.kotest.property.internal.ShrinkfnsKt$shrinkfn$1$1$smallestA$1.invokeSuspend(shrinkfns.kt:19)
```

Example 3 (kotlin):
```kotlin
Arb.positiveInt().checkAll(PropTestConfig(shrinkingMode = ShrinkingMode.Unbounded)) { i ->    calculateProperty(i) shouldBe true}
```

Example 4 (kotlin):
```kotlin
data class Coordinate(val x: Int, val y: Int)class CoordinateTest : FunSpec({    context("Coordinate Transformations") {        // Shrinker takes the four neighbouring coordinates        val coordinateShrinker = Shrinker<Coordinate> { c ->            listOf(                Coordinate(c.x - 1, c.y),                Coordinate(c.x, c.y - 1),                Coordinate(c.x + 1, c.y),                Coordinate(c.x, c.y + 1),            )        }        val coordinateArb = arbitrary(coordinateShrinker) {            Coordinate(Arb.nonNegativeInt().bind(), Arb.nonNegativeInt().bind())        }        test("Coordinates are always positive after transformation") {            coordinateArb.checkAll {                transform(it).x shouldBeGreaterThanOrEqualTo 0                transform(it).y shouldBeGreaterThanOrEqualTo 0            }        }    }})
```

---

## Statistics | Kotest

**URL:** https://kotest.io/docs/proptest/property-test-statistics.html

**Contents:**
- Statistics
- Labels​
  - Report Mode​
  - Checking Coverage of Statistics​
  - Custom Reports​

Sometimes you may like to know the types of values being generated by Kotest, to ensure that your generators are configured in the way you expect. Property test statistics are designed to fulfil this need.

The function collect is the entry point to statistcs and is used to count categories of values. We use this by invoking it inside a property test with the category we want to increment.

For example, lets say we wanted to gather statistics on the values of RoundingMode used by a BigDecimal. We would invoke checkAll as normal, passing the rounding mode to the collect function.

Now after the test is completed, Kotest will output the test name, and the counts/percentages for each category:

The category we use does not have to be an enum. It can be any object, and you can wrap in conditionals if you want more control. For example:

Sometimes you may wish to have orthogonal sets of statistics. For example, for a simple number test, we might want to confirm that a certain percentage are even numbers, and a certain percentage are negative. One way would be to have EVEN_POS, EVEN_NEG, ODD_POS, ODD_NEG:

This gives us one set of outputs:

However, as the combinations grow this will become unwieldy, so Kotest supports labelled statistics. You can think of this as distinct sets of statistics. To use labels, just pass the label name as the first arg to the collect method.

Now, Kotest will output multiple sets of statistics, with the label name in the title:

By default, statistics are printed for every property test. There are four modes which can be configured using the global configuration object PropertyTesting.

The possible options are:

If you wish to programmatically assert that certain values are being generated, then you can use the specify constraints that must be met.

For example, in our previous rounding example, we can check that at least 10% of inputs are covering HALF_DOWN, and 10% are covering FLOOR using withCoveragePercentages:

If we want to check by absolute numbers rather than percentages, we can use withCoverageCounts:

You can customize the report format, or generate reports from the raw data, by using your own instance of StatisticsReporter. This is configured via the global configuration object PropertyTesting.

**Examples:**

Example 1 (kotlin):
```kotlin
checkAll(Arb.enum<RoundingMode>(), Arb.bigDecimal()) { mode, decimal ->  collect(mode)  // test here}
```

Example 2 (yaml):
```yaml
Statistics: [collecting stats] (1000 iterations, 1 args)HALF_DOWN                                                     142 (14%)HALF_UP                                                       141 (14%)CEILING                                                       132 (13%)FLOOR                                                         122 (12%)UP                                                            119 (12%)UNNECESSARY                                                   119 (12%)HALF_EVEN                                                     118 (12%)DOWN                                                          107 (11%)
```

Example 3 (kotlin):
```kotlin
checkAll(Arb.int()) { k ->  when {    k % 2 == 0 -> collect("EVEN")    else -> collect("ODD")  }  // test here}
```

Example 4 (kotlin):
```kotlin
checkAll(Arb.int()) { k ->  when {    k > 0 && k % 2 == 0 -> collect("EVEN_POS")    k % 2 == 0 -> collect("EVEN_NEG")    k > 0 -> collect("ODD_POS")    else -> collect("ODD_NEG")  }  // test here}
```

---

## Property Test Seeds | Kotest

**URL:** https://kotest.io/docs/6.0/proptest/property-test-seeds.html

**Contents:**
- Property Test Seeds
  - Manually specifying the seed​
  - Rerunning failed seeds​
  - Failing when seeds set​

When a property test is executed, the values are generated using a random source that is created from a seed value. By default this seed value is itself randomly chosen (using the default kotlin.random.Random instance). However, there are times when this value needs to be fixed or repeated.

You can change the default used by all tests, unless overriden through the options listed below, by changing the configuration value PropertyTesting.defaultSeed.

To manually set the seed, pass an instance of PropTestConfig to your prop test methods. You might want to do this if you find a test failure, and you want to ensure that those values continue to be executed in the future as a regression test.

Whenever a property test fails, Kotest will output the seed that was used. You can duplicate the test, setting it to use this seed so you have permanent regression test for those values.

By default, when a property test fails, the seed used by that test is written to a file in ~/.kotest/seeds/<spec>/<testname>. Whenever a property test runs, this seed is detected if the file exists, and used in place of a random seed. Next time the test is successful, the seed file will be removed.

A manually specified seed always takes precedence over a failed seed.

This feature can be disabled by setting PropertyTesting.writeFailedSeed = false

Some users prefer to avoid manually specifying seeds. They want to use them locally only, when developing, but to avoid checking them in. If this is your style, then set PropertyTesting.failOnSeed = false or the env var kotest.proptest.seed.fail-if-set to false on your server.

Then if a seed is detected, the test suite will fail.

**Examples:**

Example 1 (kotlin):
```kotlin
class PropertyExample : FreeSpec({  "String size" {    forAll<String, String>(PropTestConfig(seed = 127305235)) { a, b ->      (a + b).length == a.length + b.length    }  }})
```

---

## Assumptions | Kotest

**URL:** https://kotest.io/docs/6.0/proptest/property-test-assumptions.html

**Contents:**
- Assumptions
  - Assertions​
  - Max Discard Percentage​

To restrict the set the generated values from a generator, we can use filter to return a constrained arb.

However if we need to restrict inputs based on the relationship between values, then filtering on a single arb won't work. Consider an example where we want to test that two non-equal strings have a non-zero Levenshtein distance.

This will periodically fail - whenever two equal strings are generated. One approach would be to just wrap the tests in an if/else block and avoid those undesired inputs.

But in more complicated scenarios we could easily introduce a bug and filter all our inputs.

Kotest provides a feature called assumptions that will filter out unwanted combinations, while tracking that we are not filtering too many.

An assumption accepts a boolean value passed to the withAssumptions function, that if true, will allow the property test to continue, but if false, that particular iteration is skipped. For example, the previous example will now pass:

Alternatively, you can use inline syntax:

Kotest expands on basic boolean assumptions by allowing you to specify assertions in an assumption function.

For example, building on the previous example:

Here we are ensuring that all inputs are not equal, and that the inputs have the same length. Any assertion that throws AssertionError can be used here, including all the assertions provided by Kotest.

This also supports inline syntax:

By default, the maximum discard percentage is 10%. If more combinations are discarded than that, the property test will fail. This helps avoid a scenario where we erroneously discard too many, or even all, our inputs.

For example, the following would fail by default because we would be filtering ~50% of values.

But if we wanted to allow this regardless, we can use the maxDiscardPercentage to increase the allowed discard rate.

It is generally better to adjust your arbs to produce values closer to what you need, so that you only need to filter out unwanted edge cases.

**Examples:**

Example 1 (kotlin):
```kotlin
val evens = Arb.int().filter { it.value % 2 == 0 }
```

Example 2 (kotlin):
```kotlin
checkAll<String, String> { a, b ->  levenshtein(a, b) shouldBeGreaterThan 0}
```

Example 3 (kotlin):
```kotlin
checkAll<String, String> { a, b ->  if (a != b)    levenshtein(a, b) shouldBeGreaterThan 0}
```

Example 4 (kotlin):
```kotlin
checkAll<String, String> { a, b ->  withAssumptions(a != b) {    levenshtein(a, b) shouldBeGreaterThan 0  }}
```

---

## Property-based Testing | Kotest

**URL:** https://kotest.io/docs/5.6.x/proptest/property-based-testing.html

**Contents:**
- Property-based Testing
- What is Property Testing?​
- Getting Started​
- Next Steps​

Kotest is split into several subprojects which can be used independently. One of these subprojects is the property test framework. You do not need to be using Kotest as your test framework (although you should!) to benefit from the property test support.

Developers typically write example-based tests. These are your garden variety unit tests you know and love. You provide some inputs, and some expected results, and a test framework like Kotest or JUnit checks that the actual results meet the expectations.

One problem with this approach is that it is very easy to miss errors due to edge cases that the developer didn't think about, or lack of coverage in the chosen inputs. Instead, with property testing, hundreds or thousands of values are fed into the same test, and the values are (usually) randomly generated by your property test framework.

For example, a good property test framework will include values like negative infinity, empty lists, strings with non-ascii characters, and so on. Things we often forget about when writing example based tests.

Property tests were originally conceived in frameworks like Quickcheck with the notion of testing a property on some object, ie. something that should hold true for all inputs. An invariant in other words. An example of an invariant is given two strings, a and b, then length(a + b) should always be equal to length(a) + length(b).

That is where the term property testing originates.

However, you do not have to use property tests to only test things like monad laws or basic numeric functions. Any test that would benefit from a wide array of input values is a good candidate. For example, we might have a function that validates usernames, and we want to test that valid emails are accepted. A property test would be useful here in generating 1000s of combinations to help harden our validation logic.

The property test framework is supported on all targets.

Add the following dependency to your build:

Add the following dependency to your build.

Add the following dependency to your commonTest sourceset:

Alternatively, add the dependency to a specific target. For example, we could add to the Javascript target only be using the jsTest sourceset.

To create input values for tests, Kotest uses the term generator. One generator per input argument is passed to a test function, and the test will execute for a set number of iterations.

**Examples:**

Example 1 (kotlin):
```kotlin
dependencies {   testImplementation("io.kotest:kotest-property:$version")}
```

Example 2 (xml):
```xml
<dependency>   <groupId>io.kotest</groupId>   <artifactId>kotest-property-jvm</artifactId>   <version>${version}</version>   <scope>test</scope></dependency>
```

Example 3 (kotlin):
```kotlin
kotlin {  sourceSets {    val commonTest by getting {      dependencies {        implementation("io.kotest:kotest-property:$version")      }    }  }}
```

Example 4 (kotlin):
```kotlin
kotlin {   targets {      js {         browser()         nodejs()      }   }   sourceSets {      val jsTest by getting {         dependencies {            implementation("io.kotest:kotest-property:$version")         }      }   }}
```

---

## Generators List | Kotest

**URL:** https://kotest.io/docs/next/proptest/property-test-generators-list.html

**Contents:**
- Generators List

This page lists all current generators in Kotest. There are two types of generator: arbitrary and exhaustive.

Most generators are available on all platforms. Some are JVM or JS specific.

We also provide generators for Arrow as a separate module.

---

## Property Test Functions | Kotest

**URL:** https://kotest.io/docs/5.3.x/proptest/property-test-functions.html

**Contents:**
- Property Test Functions
  - For All​
  - Check All​
  - Iterations​
  - Specifying Generators​

There are two variants of functions that are used to execute a property test in Kotest: forAll and checkAll.

The first, forAll, accepts an n-arity function (a, ..., n) -> Boolean that tests the property. The test will pass if, for all input values, the function returns true.

Notice that this functions accepts type parameters for the argument types, with arity up to 14. Kotest uses these type parameters to locate a generator which provides (generates) random values of a suitable type.

For example, forAll<String, Int, Boolean> { a, b, c -> } is a 3-arity property test where argument a is a random String, argument b is a random int, and argument c is a random boolean.

The second, checkAll, accepts an n-arity function (a, ..., n) -> Unit in which you can simply execute assertions against the inputs. This approach will consider a test valid if no exceptions are thrown. Here is the same example again written in the equivalent way using checkAll.

The second approach is more general purpose than returning a boolean, but the first approach is from the original haskell libraries that inspired this library.

By default, Kotest will run the property test 1000 times. We can easily customize this by specifying the iteration count when invoking the test method.

Let's say we want to run a test 10,000 times.

You saw in the previous examples that Kotest would provide values automatically based on the type parameter(s). It does this by locating a generator that generates values for the required type.

For example, the automatically provided Integer generator generates random ints from all possible values - negative, positive, infinities, zero and so on.

This is fine for basic tests but often we want more control over the sample space. For example, we may want to test a function for numbers in a certain range only.

Then you would need to specify the generator(s) manually.

You can see we created two tests and in each test passed a generator into the forAll function with a suitable int range.

See here for a list of the built in generators.

**Examples:**

Example 1 (kotlin):
```kotlin
class PropertyExample: StringSpec({   "String size" {      forAll<String, String> { a, b ->         (a + b).length == a.length + b.length      }   }})
```

Example 2 (kotlin):
```kotlin
class PropertyExample: StringSpec({   "String size" {      checkAll<String, String> { a, b ->         a + b shouldHaveLength a.length + b.length      }   }})
```

Example 3 (kotlin):
```kotlin
class PropertyExample: StringSpec({   "a many iterations test" {      checkAll<Double, Double>(10_000) { a, b ->         // test here      }   }})
```

Example 4 (kotlin):
```kotlin
class PropertyExample: StringSpec({   "is allowed to drink in Chicago" {      forAll(Arb.int(21..150)) { a ->         isDrinkingAge(a) // assuming some function that calculates if we're old enough to drink      }   }   "is allowed to drink in London" {      forAll(Arb.int(18..150)) { a ->         isDrinkingAge(a) // assuming some function that calculates if we're old enough to drink      }   }})
```

---

## Property Test Seeds | Kotest

**URL:** https://kotest.io/docs/5.9.x/proptest/property-test-seeds.html

**Contents:**
- Property Test Seeds
  - Manually specifying the seed​
  - Rerunning failed seeds​
  - Failing when seeds set​

When a property test is executed, the values are generated using a random source that is created from a seed value. By default this seed value is itself randomly chosen (using the default kotlin.random.Random instance). However, there are times when this value needs to be fixed or repeated.

You can change the default used by all tests, unless overriden through the options listed below, by changing the configuration value PropertyTesting.defaultSeed.

To manually set the seed, pass an instance of PropTestConfig to your prop test methods. You might want to do this if you find a test failure, and you want to ensure that those values continue to be executed in the future as a regression test.

Whenever a property test fails, Kotest will output the seed that was used. You can duplicate the test, setting it to use this seed so you have permanent regression test for those values.

By default, when a property test fails, the seed used by that test is written to a file in ~/.kotest/seeds/<spec>/<testname>. Whenever a property test runs, this seed is detected if the file exists, and used in place of a random seed. Next time the test is successful, the seed file will be removed.

A manually specified seed always takes precedence over a failed seed.

This feature can be disabled by setting PropertyTesting.writeFailedSeed = false

Some users prefer to avoid manually specifying seeds. They want to use them locally only, when developing, but to avoid checking them in. If this is your style, then set PropertyTesting.failOnSeed = false or the env var kotest.proptest.seed.fail-if-set to false on your server.

Then if a seed is detected, the test suite will fail.

**Examples:**

Example 1 (kotlin):
```kotlin
class PropertyExample : StringSpec({  "String size" {    forAll<String, String>(PropTestConfig(seed = 127305235)) { a, b ->      (a + b).length == a.length + b.length    }  }})
```

---

## Reflective Arbs | Kotest

**URL:** https://kotest.io/docs/5.9.x/proptest/reflective-arbs.html

**Contents:**
- Reflective Arbs
- Provided Arbs​

When running tests on JVM, Kotest supports generating more complex Arbs automatically. The generated Arb relies on build-in default and further reflective Arbs to populate the class parameters. If you just need to create and instance and don't need filtering you can use the class type in the checkAll/forAll calls directly. When you want to obtain the Arb to manipulate it further or filter invalid values, you can use Arb.bind with the type argument to obtain the Arb. If the required type depends on types that are not supported by default, it is possible to provide Arbs for those types in the call to Arb.bind.

Reflective binding is supported for:

When doing reflective binding, Kotest supports a builder API to provide Arbs for specific types (classes) and properties. Binding specific properties allow greater control in cases where types might be more widely used, like primitives for instance.

**Examples:**

Example 1 (kotlin):
```kotlin
enum class Currency {  USD, GBP, EUR}class CurrencyAmount(  val amount: Long,  val currency: Currency)context("Currencies converts to EUR") { // In some spec  checkAll(Arb.bind<CurrencyAmount>().filter { it.currency != EUR }) { currencyAmount ->    val converted = currencyAmount.convertTo(EUR)    converted.currency shouldBe EUR  }}context("Converting to a currency and back yields the same amount") { // In some spec  checkAll<CurrencyAmount, Currency>() { currencyAmount, currency ->    val converted = currencyAmount.convertTo(currency).convertTo(currencyAmount.currency)    converted.currency shouldBe currencyAmount.currency  }}
```

Example 2 (kotlin):
```kotlin
data class User(  val name: String,  val password: String,  val age: Int,)// in some speccontext("Some tests with an arbitrary user") {  checkAll(Arb.bind<User> {    bind(User::name to Arb.string(1..10))    bind(User::password to Arb.string(24..80)) // binds a specific property to an arb    bind(Int::class to Arb.int(0..100))  // binds a type to an arb  }) { user ->    // ...  }}
```

---

## Statistics | Kotest

**URL:** https://kotest.io/docs/5.9.x/proptest/property-test-statistics.html

**Contents:**
- Statistics
- Labels​
  - Report Mode​
  - Checking Coverage of Statistics​
  - Custom Reports​

Sometimes you may like to know the types of values being generated by Kotest, to ensure that your generators are configured in the way you expect. Property test statistics are designed to fulfil this need.

The function collect is the entry point to statistcs and is used to count categories of values. We use this by invoking it inside a property test with the category we want to increment.

For example, lets say we wanted to gather statistics on the values of RoundingMode used by a BigDecimal. We would invoke checkAll as normal, passing the rounding mode to the collect function.

Now after the test is completed, Kotest will output the test name, and the counts/percentages for each category:

The category we use does not have to be an enum. It can be any object, and you can wrap in conditionals if you want more control. For example:

Sometimes you may wish to have orthogonal sets of statistics. For example, for a simple number test, we might want to confirm that a certain percentage are even numbers, and a certain percentage are negative. One way would be to have EVEN_POS, EVEN_NEG, ODD_POS, ODD_NEG:

This gives us one set of outputs:

However, as the combinations grow this will become unwieldy, so Kotest supports labelled statistics. You can think of this as distinct sets of statistics. To use labels, just pass the label name as the first arg to the collect method.

Now, Kotest will output multiple sets of statistics, with the label name in the title:

By default, statistics are printed for every property test. There are four modes which can be configured using the global configuration object PropertyTesting.

The possible options are:

If you wish to programmatically assert that certain values are being generated, then you can use the specify constraints that must be met.

For example, in our previous rounding example, we can check that at least 10% of inputs are covering HALF_DOWN, and 10% are covering FLOOR using withCoveragePercentages:

If we want to check by absolute numbers rather than percentages, we can use withCoverageCounts:

You can customize the report format, or generate reports from the raw data, by using your own instance of StatisticsReporter. This is configured via the global configuration object PropertyTesting.

**Examples:**

Example 1 (kotlin):
```kotlin
checkAll(Arb.enum<RoundingMode>(), Arb.bigDecimal()) { mode, decimal ->  collect(mode)  // test here}
```

Example 2 (yaml):
```yaml
Statistics: [collecting stats] (1000 iterations, 1 args)HALF_DOWN                                                     142 (14%)HALF_UP                                                       141 (14%)CEILING                                                       132 (13%)FLOOR                                                         122 (12%)UP                                                            119 (12%)UNNECESSARY                                                   119 (12%)HALF_EVEN                                                     118 (12%)DOWN                                                          107 (11%)
```

Example 3 (kotlin):
```kotlin
checkAll(Arb.int()) { k ->  when {    k % 2 == 0 -> collect("EVEN")    else -> collect("ODD")  }  // test here}
```

Example 4 (kotlin):
```kotlin
checkAll(Arb.int()) { k ->  when {    k > 0 && k % 2 == 0 -> collect("EVEN_POS")    k % 2 == 0 -> collect("EVEN_NEG")    k > 0 -> collect("ODD_POS")    else -> collect("ODD_NEG")  }  // test here}
```

---

## Property Test Functions | Kotest

**URL:** https://kotest.io/docs/5.7.x/proptest/property-test-functions.html

**Contents:**
- Property Test Functions
  - For All​
  - Check All​
  - Iterations​
  - Specifying Generators​

There are two variants of functions that are used to execute a property test in Kotest: forAll and checkAll.

The first, forAll, accepts an n-arity function (a, ..., n) -> Boolean that tests the property. The test will pass if, for all input values, the function returns true.

Notice that this functions accepts type parameters for the argument types, with arity up to 14. Kotest uses these type parameters to locate a generator which provides (generates) random values of a suitable type.

For example, forAll<String, Int, Boolean> { a, b, c -> } is a 3-arity property test where argument a is a random String, argument b is a random int, and argument c is a random boolean.

The second, checkAll, accepts an n-arity function (a, ..., n) -> Unit in which you can simply execute assertions against the inputs. This approach will consider a test valid if no exceptions are thrown. Here is the same example again written in the equivalent way using checkAll.

The second approach is more general purpose than returning a boolean, but the first approach is from the original haskell libraries that inspired this library.

By default, Kotest will run the property test 1000 times. We can easily customize this by specifying the iteration count when invoking the test method.

Let's say we want to run a test 10,000 times.

You saw in the previous examples that Kotest would provide values automatically based on the type parameter(s). It does this by locating a generator that generates values for the required type.

For example, the automatically provided Integer generator generates random ints from all possible values - negative, positive, infinities, zero and so on.

This is fine for basic tests but often we want more control over the sample space. For example, we may want to test a function for numbers in a certain range only.

Then you would need to specify the generator(s) manually.

You can see we created two tests and in each test passed a generator into the forAll function with a suitable int range.

See here for a list of the built in generators.

**Examples:**

Example 1 (kotlin):
```kotlin
class PropertyExample: StringSpec({   "String size" {      forAll<String, String> { a, b ->         (a + b).length == a.length + b.length      }   }})
```

Example 2 (kotlin):
```kotlin
class PropertyExample: StringSpec({   "String size" {      checkAll<String, String> { a, b ->         a + b shouldHaveLength a.length + b.length      }   }})
```

Example 3 (kotlin):
```kotlin
class PropertyExample: StringSpec({   "a many iterations test" {      checkAll<Double, Double>(10_000) { a, b ->         // test here      }   }})
```

Example 4 (kotlin):
```kotlin
class PropertyExample: StringSpec({   "is allowed to drink in Chicago" {      forAll(Arb.int(21..150)) { a ->         isDrinkingAge(a) // assuming some function that calculates if we're old enough to drink      }   }   "is allowed to drink in London" {      forAll(Arb.int(18..150)) { a ->         isDrinkingAge(a) // assuming some function that calculates if we're old enough to drink      }   }})
```

---

## Generators | Kotest

**URL:** https://kotest.io/docs/next/proptest/property-test-generators.html

**Contents:**
- Generators
- Arbitrary​
- Exhaustive​

Generated values are provided by instances of the sealed class Gen. You can think of a Gen as kind of like an input stream but for property test values. Each Gen will provide a (usually) infinite stream of these values for one particular type.

Kotest has two types of generators - Arb for generating arbitrary (random) values and Exhaustive for generating a finite set of values in a closed space.

Both types of gens can be mixed and matched in property tests. For example, you could test a function with 100 random positive integers (an arb) alongside every even number from 0 to 200 (exhaustive).

Some generators are only available on the JVM. See the full list here.

Arbs generate two types of values - a hard coded set of edge cases and an infinite stream of randomly chosen samples.

The samples may be repeated, and some values may never be generated at all. For example generating 1000 integers between 0 and Int.MAX cannot return all possible values, and some values may happen to be generated more than once. Similarly, generating 1000 random integers between 0 and 500 will definitely result in some values appearing more than once.

Some common arbitraries include numbers with or without a range, strings in the unicode set, random lists, data classes with random parameters, emails, codepoints, chars and so on.

In addition to the random values, arbs may provide edge cases. One of the design features of Kotest's property testing is that values for some types will always include "common" edge cases that you probably want to be included in your tests.

For example, when testing a function that accepts an integer, you probably want to ensure that, at the very least, it is tested with zero, a positive number and a negative number. If only random values were provided, the chances of zero appearing would be fairly low, so Kotest will always provide some "edge cases" for integers (unless you specify otherwise).

When executing tests, the framework will alternate randomly between samples and edge cases. The split is determined by a configuration value which defaults to 2% edge cases.

Not all arbs have edge cases, but the arbs for the most common types do. Here are some examples of edge cases used by some arbs:

Exhaustives generate all values from a given space. This is useful when you want to ensure every value in that space is used. For example, for enum values, it is usually more helpful to ensure each enum is used, rather than picking randomly from the enums values and potentially missing some and duplicating others.

Typical exhaustives include small collections, enums, boolean values, powerset of a list or set, pre-defined integer ranges, and predefined string ranges.

Once an exhaustive has provided all it's values, it will loop and start again, so an exhaustive can be used in a test that requires any number of inputs.

Here we asked for 100 iterations, so each value of Season would be provided 25 times on average, but the exact counts will randomly change from run to run, and are very likely not to be exactly 25.

**Examples:**

Example 1 (php):
```php
enum class Season { Winter, Fall, Spring, Summer }forAll<Int, Season>(100) { a, season -> ... }
```

---

## Property Test Functions | Kotest

**URL:** https://kotest.io/docs/5.4.x/proptest/property-test-functions.html

**Contents:**
- Property Test Functions
  - For All​
  - Check All​
  - Iterations​
  - Specifying Generators​

There are two variants of functions that are used to execute a property test in Kotest: forAll and checkAll.

The first, forAll, accepts an n-arity function (a, ..., n) -> Boolean that tests the property. The test will pass if, for all input values, the function returns true.

Notice that this functions accepts type parameters for the argument types, with arity up to 14. Kotest uses these type parameters to locate a generator which provides (generates) random values of a suitable type.

For example, forAll<String, Int, Boolean> { a, b, c -> } is a 3-arity property test where argument a is a random String, argument b is a random int, and argument c is a random boolean.

The second, checkAll, accepts an n-arity function (a, ..., n) -> Unit in which you can simply execute assertions against the inputs. This approach will consider a test valid if no exceptions are thrown. Here is the same example again written in the equivalent way using checkAll.

The second approach is more general purpose than returning a boolean, but the first approach is from the original haskell libraries that inspired this library.

By default, Kotest will run the property test 1000 times. We can easily customize this by specifying the iteration count when invoking the test method.

Let's say we want to run a test 10,000 times.

You saw in the previous examples that Kotest would provide values automatically based on the type parameter(s). It does this by locating a generator that generates values for the required type.

For example, the automatically provided Integer generator generates random ints from all possible values - negative, positive, infinities, zero and so on.

This is fine for basic tests but often we want more control over the sample space. For example, we may want to test a function for numbers in a certain range only.

Then you would need to specify the generator(s) manually.

You can see we created two tests and in each test passed a generator into the forAll function with a suitable int range.

See here for a list of the built in generators.

**Examples:**

Example 1 (kotlin):
```kotlin
class PropertyExample: StringSpec({   "String size" {      forAll<String, String> { a, b ->         (a + b).length == a.length + b.length      }   }})
```

Example 2 (kotlin):
```kotlin
class PropertyExample: StringSpec({   "String size" {      checkAll<String, String> { a, b ->         a + b shouldHaveLength a.length + b.length      }   }})
```

Example 3 (kotlin):
```kotlin
class PropertyExample: StringSpec({   "a many iterations test" {      checkAll<Double, Double>(10_000) { a, b ->         // test here      }   }})
```

Example 4 (kotlin):
```kotlin
class PropertyExample: StringSpec({   "is allowed to drink in Chicago" {      forAll(Arb.int(21..150)) { a ->         isDrinkingAge(a) // assuming some function that calculates if we're old enough to drink      }   }   "is allowed to drink in London" {      forAll(Arb.int(18..150)) { a ->         isDrinkingAge(a) // assuming some function that calculates if we're old enough to drink      }   }})
```

---

## Global Configuration | Kotest

**URL:** https://kotest.io/docs/5.9.x/proptest/property-test-global-config.html

**Contents:**
- Global Configuration
  - Default Iterations​
  - Printing Shrink Steps​

Some property test settings can be set globally for all property tests.

The standard default iteration count is 1000. This means when you don't specify the iteration count in a property test, the default will be 1000.

We can override this default either by assigning a value to PropertyTesting.defaultIterationCount, or by using the system property kotest.proptest.default.iteration.count.

Any test which directly sets the iteration count will of course use that value.

If you are using Kotest framework, then you can perform this action before any tests by using project config. For example:

By default, when using shrinking, each shrinking step will not be logged, but only the final shrunk value.

To enable logging of each intermediate value, assign true to PropertyTesting.shouldPrintShrinkSteps or use the system property kotest.proptest.output.shrink-steps=true.

**Examples:**

Example 1 (kotlin):
```kotlin
PropertyTesting.defaultIterationCount = 123// will use 555 iterations specified in the testforAll<String, String>(555) { a,b -> a + b == "$a$b" }// will use 123 iterations from the global defaultforAll<String, String> { a,b -> a + b == "$a$b" }
```

Example 2 (kotlin):
```kotlin
class KotestConfig : AbstractProjectConfig() {   override suspend fun beforeProject() {      PropertyTesting.defaultIterationCount = 123   }}
```

---

## Custom Generators | Kotest

**URL:** https://kotest.io/docs/5.6.x/proptest/custom-generators.html

**Contents:**
- Custom Generators
  - Arbitrary​
  - Exhaustive​

To write your own generator for a type T, you just create an instance of Arb<T> or Exhaustive<T>.

When writing a custom arbitrary we can use the arbitrary builder which accepts a lambda that must return the type we are generating for. The parameter to this lambda is a RandomSource parameter which contains the seed and the Random instance. We should typically use the provided RandomSource if we need access to a kotlin.Random instance, as this instance will have been seeded by the framework to allow for repeatable tests.

For example, here is a custom arb that generates a random int between 3 and 6 using the arbitrary builder.

In addition to the RandomSource parameter, the arbitrary builder lambda also provides the ArbitraryBuilderSyntax context which we can leverage to compose other arbitraries when building ours.

For example, here is an Arbitrary that supports a custom class called Person, delegating to a String arbitrary and an Int arbitrary.

The resulting arbitrary produced using this syntax is equivalent to using map, flatMap and bind.

When writing a custom exhaustive we can use the exhaustive() extension function on a List. Nothing more to it than that really!

**Examples:**

Example 1 (kotlin):
```kotlin
val sillyArb = arbitrary { rs: RandomSource ->   rs.random.nextInt(3..6)}
```

Example 2 (kotlin):
```kotlin
data class Person(val name: String, val age: Int)val personArb = arbitrary {   val name = Arb.string(10..12).bind()   val age = Arb.int(21, 150).bind()   Person(name, age)}
```

Example 3 (kotlin):
```kotlin
val singleDigitPrimes = listOf(2,3,5,7).exhaustive()
```

Example 4 (kotlin):
```kotlin
class PropertyExample: StringSpec({    "testing single digit primes" {        checkAll(singleDigitPrimes) { prime ->           isPrime(prime) shouldBe true           isPrime(prime * prime) shouldBe false        }    }})
```

---

## Property-based Testing | Kotest

**URL:** https://kotest.io/docs/5.5.x/proptest/property-based-testing.html

**Contents:**
- Property-based Testing
- What is Property Testing?​
- Getting Started​
- Next Steps​

Kotest is split into several subprojects which can be used independently. One of these subprojects is the property test framework. You do not need to be using Kotest as your test framework (although you should!) to benefit from the property test support.

Developers typically write example-based tests. These are your garden variety unit tests you know and love. You provide some inputs, and some expected results, and a test framework like Kotest or JUnit checks that the actual results meet the expectations.

One problem with this approach is that it is very easy to miss errors due to edge cases that the developer didn't think about, or lack of coverage in the chosen inputs. Instead, with property testing, hundreds or thousands of values are fed into the same test, and the values are (usually) randomly generated by your property test framework.

For example, a good property test framework will include values like negative infinity, empty lists, strings with non-ascii characters, and so on. Things we often forget about when writing example based tests.

Property tests were originally conceived in frameworks like Quickcheck with the notion of testing a property on some object, ie. something that should hold true for all inputs. An invariant in other words. An example of an invariant is given two strings, a and b, then length(a + b) should always be equal to length(a) + length(b).

That is where the term property testing originates.

However, you do not have to use property tests to only test things like monad laws or basic numeric functions. Any test that would benefit from a wide array of input values is a good candidate. For example, we might have a function that validates usernames, and we want to test that valid emails are accepted. A property test would be useful here in generating 1000s of combinations to help harden our validation logic.

The property test framework is supported on all targets.

Add the following dependency to your build:

Add the following dependency to your build.

Add the following dependency to your commonTest sourceset:

Alternatively, add the dependency to a specific target. For example, we could add to the Javascript target only be using the jsTest sourceset.

To create input values for tests, Kotest uses the term generator. One generator per input argument is passed to a test function, and the test will execute for a set number of iterations.

**Examples:**

Example 1 (kotlin):
```kotlin
dependencies {   testImplementation("io.kotest:kotest-property:$version")}
```

Example 2 (xml):
```xml
<dependency>   <groupId>io.kotest</groupId>   <artifactId>kotest-property-jvm</artifactId>   <version>${version}</version>   <scope>test</scope></dependency>
```

Example 3 (kotlin):
```kotlin
kotlin {  sourceSets {    val commonTest by getting {      dependencies {        implementation("io.kotest:kotest-property:$version")      }    }  }}
```

Example 4 (kotlin):
```kotlin
kotlin {   targets {      js {         browser()         nodejs()      }   }   sourceSets {      val jsTest by getting {         dependencies {            implementation("io.kotest:kotest-property:$version")         }      }   }}
```

---

## Generator Operations | Kotest

**URL:** https://kotest.io/docs/5.8.x/proptest/generator-operations.html

**Contents:**
- Generator Operations
- Next​
- Filter​
- Map​
- FlatMap​
- Merging​
- Bind​

If you want to use an Arb to just return a value (even outside of a property test), then you can call next on it.

If you have an arb and you want to create a new arb that provides a subset of values, you can call filter on the source arb. For example, one way of generating even numbers is to take the integer arb, and filter out odd values. Viz:

If you have an arb and you want to transform the value generated, you can use map.

If you have an arb whose emission or edge cases depends on the emission of the previous arbitraries, you can use flatMap.

Two generators can be merged together, so that the resulting elements are equally sampled from both generators.

So with the following example would have an equal chance to yield either "a" or "b" on each random sample:

For merging more than two arbitraries, Arb.choice or Arb.choose might be more appropriate. For instance we can use:

Bind is useful if you want to apply multiple arbitraries. We can take a look at how we might construct values for a data class using bind.

**Examples:**

Example 1 (kotlin):
```kotlin
val arbA: Arb<A> = ...val a = arbA.next() // use Random.Defaultval a2 = arbA.next(rs) // pass in Random
```

Example 2 (kotlin):
```kotlin
val evens = Arb.int().filter { it.value % 2 == 0 }val odds = Arb.int().filter { it.value % 2 == 1 }
```

Example 3 (kotlin):
```kotlin
val integerStrings: Arb<String> = Arb.int().map { it.toString() }
```

Example 4 (kotlin):
```kotlin
val dependentArbs: Arb<String> = Arb.of("foo", "bar").flatMap { prefix ->   Arb.int(1..10).map { integer ->      "${prefix}-${integer}"   }}
```

---

## Shrinking | Kotest

**URL:** https://kotest.io/docs/5.2.x/proptest/property-test-shrinking.html

**Contents:**
- Shrinking
- Shrinking for built-in generators​
- Shrinking for custom generators​

In property-based testing, the initially found failing case may contain a lot of complexity that does actually cause the test to fail. Shrinking is the mechanism through which a property-based testing framework can simplify failing cases in order to find out the minimal reproducible case is. In Kotest, the way in which failing cases from generators are shrunk is defined by implementations of the Shrinker interface. Built-in generators generally have a default Shrinker defined by the framework, while custom generators can be given a custom Shrinker implementation.

Built-in generators (see Generators List) have a default Shrinker defined by the framework. A shrink function takes as input the value that failed the test and returns a list of new values on which Kotest can appy the test. The exact behaviour depends on the data type. For instance, a string could be shrunk by dropping the first or last character while for integers we could decrement or halve the value. In addition, shrink behaviour is defined for edge cases such as an empty string or the integer 0. The shrinking is performed when a test that uses such generator fails.

If the test fails for one of the generated inputs then the shrinking result is shown:

By default, Kotest will shrink 1000 times. This behaviour is configurable. For example, if you want to continue shrinking without bounds:

Custom generators do not have a Shrinker defined by Kotest. Instead, custom Shrinkers can be implemented. Below is an example where the Shrinker returns coordinates that are next to the value itself.

**Examples:**

Example 1 (kotlin):
```kotlin
Arb.positiveInt().checkAll { i ->    calculateProperty(i) shouldBe true}
```

Example 2 (typescript):
```typescript
Property test failed for inputs0) 1792716902Caused by io.kotest.assertions.AssertionFailedError: expected:<1792716902> but was:<0> at    PropertyBasedTest$1$1$3$1.invokeSuspend(PropertyBasedTest.kt:54)    PropertyBasedTest$1$1$3$1.invoke(PropertyBasedTest.kt)    PropertyBasedTest$1$1$3$1.invoke(PropertyBasedTest.kt)    io.kotest.property.internal.ProptestKt$proptest$3$2.invokeSuspend(proptest.kt:45)Attempting to shrink arg 1792716902Shrink #1: 1 passShrink #2: 597572300 failShrink #3: 199190766 failShrink #4: 66396922 failShrink #5: 22132307 failShrink #6: 7377435 failShrink #7: 2459145 fail[...]Shrink #999: 29948 passShrink #1000: 44922 passShrink #1001: 59896 passShrink #1002: 89839 failShrink result (after 1002 shrinks) => 89839Caused by io.kotest.assertions.AssertionFailedError: expected:<89839> but was:<0> at    PropertyBasedTest$1$1$3$1.invokeSuspend(PropertyBasedTest.kt:54)    PropertyBasedTest$1$1$3$1.invoke(PropertyBasedTest.kt)    PropertyBasedTest$1$1$3$1.invoke(PropertyBasedTest.kt)    io.kotest.property.internal.ShrinkfnsKt$shrinkfn$1$1$smallestA$1.invokeSuspend(shrinkfns.kt:19)
```

Example 3 (kotlin):
```kotlin
Arb.positiveInt().checkAll(PropTestConfig(shrinkingMode = ShrinkingMode.Unbounded)) { i ->    calculateProperty(i) shouldBe true}
```

Example 4 (kotlin):
```kotlin
data class Coordinate(val x: Int, val y: Int)class CoordinateTest : FunSpec({    context("Coordinate Transformations") {        // Shrinker takes the four neighbouring coordinates        val coordinateShrinker = Shrinker<Coordinate> { c ->            listOf(                Coordinate(c.x - 1, c.y),                Coordinate(c.x, c.y - 1),                Coordinate(c.x + 1, c.y),                Coordinate(c.x, c.y + 1),            )        }        val coordinateArb = arbitrary(coordinateShrinker) {            Coordinate(Arb.nonNegativeInt().bind(), Arb.nonNegativeInt().bind())        }        test("Coordinates are always positive after transformation") {            coordinateArb.checkAll {                transform(it).x shouldBeGreaterThanOrEqualTo 0                transform(it).y shouldBeGreaterThanOrEqualTo 0            }        }    }})
```

---

## Configuration | Kotest

**URL:** https://kotest.io/docs/5.2.x/proptest/property-test-config.html

**Contents:**
- Configuration
- Configuration​
  - Seed​
  - Min Failure​
  - PropTestListener​

Kotest provides for the ability to specify some configuration options when running a property test. We do this by passing in an instance of PropTestConfig to the test methods.

The most common configuration option is specifying the seed for the random instance. This is used when you want to reliably create the same values each time the test is run. You might want to do this if you find a test failure, and you want to ensure that that particular set of values continues to be executed in the future as a kind of regression test.

Whenever a property test fails, Kotest will output the seed that was used. You can duplicate the test, setting it to use this seed so you have permanent regression test for those values.

By default, Kotest tolerates no failure. Perhaps you want to run some non-deterministic test a bunch of times, and you're happy to accept some small number of failures. You can specify that in config.

Sometimes in property test it is required to perform some setup and tear down in each iteration of test. For this purpose you can register a PropTestListener with PropTestConfig.

**Examples:**

Example 1 (kotlin):
```kotlin
class PropertyExample: StringSpec({   "String size" {      forAll<String, String>(PropTestConfig(options here...)) { a,b ->         (a + b).length == a.length + b.length      }   }})
```

Example 2 (kotlin):
```kotlin
class PropertyExample: StringSpec({   "String size" {      forAll<String, String>(PropTestConfig(seed = 127305235)) { a,b ->         (a + b).length == a.length + b.length      }   }})
```

Example 3 (kotlin):
```kotlin
class PropertyExample: StringSpec({   "some flakey test" {      forAll<String, String>(PropTestConfig(maxFailure = 3)) { a,b ->         // max of 3 inputs can fail      }   }})
```

Example 4 (kotlin):
```kotlin
class PropertyExample: StringSpec({   "some property test which require setup and tear down in each iteration" {      forAll<String, String>(PropTestConfig(listeners = listOf(MyPropTestListener))) { a,b ->         // some assertion      }   }})
```

---

## Property Test Seeds | Kotest

**URL:** https://kotest.io/docs/5.5.x/proptest/property-test-seeds.html

**Contents:**
- Property Test Seeds
  - Manually specifying the seed​
  - Rerunning failed seeds​
  - Failing when seeds set​

When a property test is executed, the values are generated using a random source that is created from a seed value. By default this seed value is itself randomly chosen (using the default kotlin.random.Random instance). However, there are times when this value needs to be fixed or repeated.

You can change the default used by all tests, unless overriden through the options listed below, by changing the configuration value PropertyTesting.defaultSeed.

To manually set the seed, pass an instance of PropTestConfig to your prop test methods. You might want to do this if you find a test failure, and you want to ensure that those values continue to be executed in the future as a regression test.

Whenever a property test fails, Kotest will output the seed that was used. You can duplicate the test, setting it to use this seed so you have permanent regression test for those values.

By default, when a property test fails, the seed used by that test is written to a file in ~/.kotest/seeds/<spec>/<testname>. Whenever a property test runs, this seed is detected if the file exists, and used in place of a random seed. Next time the test is successful, the seed file will be removed.

A manually specified seed always takes precedence over a failed seed.

This feature can be disabled by setting PropertyTesting.writeFailedSeed = false

Some users prefer to avoid manually specifying seeds. They want to use them locally only, when developing, but to avoid checking them in. If this is your style, then set PropertyTesting.failOnSeed = false or the env var kotest.proptest.seed.fail-if-set to false on your server.

Then if a seed is detected, the test suite will fail.

**Examples:**

Example 1 (kotlin):
```kotlin
class PropertyExample : StringSpec({  "String size" {    forAll<String, String>(PropTestConfig(seed = 127305235)) { a, b ->      (a + b).length == a.length + b.length    }  }})
```

---

## Property Test Functions | Kotest

**URL:** https://kotest.io/docs/5.6.x/proptest/property-test-functions.html

**Contents:**
- Property Test Functions
  - For All​
  - Check All​
  - Iterations​
  - Specifying Generators​

There are two variants of functions that are used to execute a property test in Kotest: forAll and checkAll.

The first, forAll, accepts an n-arity function (a, ..., n) -> Boolean that tests the property. The test will pass if, for all input values, the function returns true.

Notice that this functions accepts type parameters for the argument types, with arity up to 14. Kotest uses these type parameters to locate a generator which provides (generates) random values of a suitable type.

For example, forAll<String, Int, Boolean> { a, b, c -> } is a 3-arity property test where argument a is a random String, argument b is a random int, and argument c is a random boolean.

The second, checkAll, accepts an n-arity function (a, ..., n) -> Unit in which you can simply execute assertions against the inputs. This approach will consider a test valid if no exceptions are thrown. Here is the same example again written in the equivalent way using checkAll.

The second approach is more general purpose than returning a boolean, but the first approach is from the original haskell libraries that inspired this library.

By default, Kotest will run the property test 1000 times. We can easily customize this by specifying the iteration count when invoking the test method.

Let's say we want to run a test 10,000 times.

You saw in the previous examples that Kotest would provide values automatically based on the type parameter(s). It does this by locating a generator that generates values for the required type.

For example, the automatically provided Integer generator generates random ints from all possible values - negative, positive, infinities, zero and so on.

This is fine for basic tests but often we want more control over the sample space. For example, we may want to test a function for numbers in a certain range only.

Then you would need to specify the generator(s) manually.

You can see we created two tests and in each test passed a generator into the forAll function with a suitable int range.

See here for a list of the built in generators.

**Examples:**

Example 1 (kotlin):
```kotlin
class PropertyExample: StringSpec({   "String size" {      forAll<String, String> { a, b ->         (a + b).length == a.length + b.length      }   }})
```

Example 2 (kotlin):
```kotlin
class PropertyExample: StringSpec({   "String size" {      checkAll<String, String> { a, b ->         a + b shouldHaveLength a.length + b.length      }   }})
```

Example 3 (kotlin):
```kotlin
class PropertyExample: StringSpec({   "a many iterations test" {      checkAll<Double, Double>(10_000) { a, b ->         // test here      }   }})
```

Example 4 (kotlin):
```kotlin
class PropertyExample: StringSpec({   "is allowed to drink in Chicago" {      forAll(Arb.int(21..150)) { a ->         isDrinkingAge(a) // assuming some function that calculates if we're old enough to drink      }   }   "is allowed to drink in London" {      forAll(Arb.int(18..150)) { a ->         isDrinkingAge(a) // assuming some function that calculates if we're old enough to drink      }   }})
```

---

## Custom Generators | Kotest

**URL:** https://kotest.io/docs/5.2.x/proptest/custom-generators.html

**Contents:**
- Custom Generators
  - Arbitrary​
  - Exhaustive​

To write your own generator for a type T, you just create an instance of Arb<T> or Exhaustive<T>.

When writing a custom arbitrary we can use the arbitrary builder which accepts a lambda that must return the type we are generating for. The parameter to this lambda is a RandomSource parameter which contains the seed and the Random instance. We should typically use the provided RandomSource if we need access to a kotlin.Random instance, as this instance will have been seeded by the framework to allow for repeatable tests.

For example, here is a custom arb that generates a random int between 3 and 6 using the arbitrary builder.

In addition to the RandomSource parameter, the arbitrary builder lambda also provides the ArbitraryBuilderSyntax context which we can leverage to compose other arbitraries when building ours.

For example, here is an Arbitrary that supports a custom class called Person, delegating to a String arbitrary and an Int arbitrary.

The resulting arbitrary produced using this syntax is equivalent to using map, flatMap and bind.

When writing a custom exhaustive we can use the exhaustive() extension function on a List. Nothing more to it than that really!

**Examples:**

Example 1 (kotlin):
```kotlin
val sillyArb = arbitrary { rs: RandomSource ->   rs.random.nextInt(3..6)}
```

Example 2 (kotlin):
```kotlin
data class Person(val name: String, val age: Int)val personArb = arbitrary {   val name = Arb.string(10..12).bind()   val age = Arb.int(21, 150).bind()   Person(name, age)}
```

Example 3 (kotlin):
```kotlin
val singleDigitPrimes = listOf(2,3,5,7).exhaustive()
```

Example 4 (kotlin):
```kotlin
class PropertyExample: StringSpec({    "testing single digit primes" {        checkAll(singleDigitPrimes) { prime ->           isPrime(prime) shouldBe true           isPrime(prime * prime) shouldBe false        }    }})
```

---

## Generators | Kotest

**URL:** https://kotest.io/docs/5.2.x/proptest/property-test-generators.html

**Contents:**
- Generators
- Arbitrary​
- Exhaustive​

Generated values are provided by instances of the sealed class Gen. You can think of a Gen as kind of like an input stream but for property test values. Each Gen will provide a (usually) infinite stream of these values for one particular type.

Kotest has two types of generators - Arb for generating arbitrary (random) values and Exhaustive for generating a finite set of values in a closed space.

Both types of gens can be mixed and matched in property tests. For example, you could test a function with 100 random positive integers (an arb) alongside every even number from 0 to 200 (exhaustive).

Some generators are only available on the JVM. See the full list here.

Arbs generate two types of values - a hard coded set of edge cases and an infinite stream of randomly chosen samples.

The samples may be repeated, and some values may never be generated at all. For example generating 1000 integers between 0 and Int.MAX cannot return all possible values, and some values may happen to be generated more than once. Similarly, generating 1000 random integers between 0 and 500 will definitely result in some values appearing more than once.

Some common arbitraries include numbers with or without a range, strings in the unicode set, random lists, data classes with random parameters, emails, codepoints, chars and so on.

In addition to the random values, arbs may provide edge cases. One of the design features of Kotest's property testing is that values for some types will always include "common" edge cases that you probably want to be included in your tests.

For example, when testing a function that accepts an integer, you probably want to ensure that, at the very least, it is tested with zero, a positive number and a negative number. If only random values were provided, the chances of zero appearing would be fairly low, so Kotest will always provide some "edge cases" for integers (unless you specify otherwise).

When executing tests, the framework will alternate randomly between samples and edge cases. The split is determined by a configuration value which defaults to 2% edge cases.

Not all arbs have edge cases, but the arbs for the most common types do. Here are some examples of edge cases used by some arbs:

Exhaustives generate all values from a given space. This is useful when you want to ensure every value in that space is used. For example, for enum values, it is usually more helpful to ensure each enum is used, rather than picking randomly from the enums values and potentially missing some and duplicating others.

Typical exhaustives include small collections, enums, boolean values, powerset of a list or set, pre-defined integer ranges, and predefined string ranges.

Once an exhaustive has provided all it's values, it will loop and start again, so an exhaustive can be used in a test that requires any number of inputs.

Here we asked for 100 iterations, so each value of Season would be provided 25 times.

**Examples:**

Example 1 (php):
```php
enum class Season { Winter, Fall, Spring, Summer }forAll<Int, Season>(100) { a, season -> ... }
```

---

## Property Test Functions | Kotest

**URL:** https://kotest.io/docs/5.9.x/proptest/property-test-functions.html

**Contents:**
- Property Test Functions
  - For All​
  - Check All​
  - Iterations​
  - Specifying Generators​

There are two variants of functions that are used to execute a property test in Kotest: forAll and checkAll.

The first, forAll, accepts an n-arity function (a, ..., n) -> Boolean that tests the property. The test will pass if, for all input values, the function returns true.

Notice that this functions accepts type parameters for the argument types, with arity up to 14. Kotest uses these type parameters to locate a generator which provides (generates) random values of a suitable type.

For example, forAll<String, Int, Boolean> { a, b, c -> } is a 3-arity property test where argument a is a random String, argument b is a random int, and argument c is a random boolean.

The second, checkAll, accepts an n-arity function (a, ..., n) -> Unit in which you can simply execute assertions against the inputs. This approach will consider a test valid if no exceptions are thrown. Here is the same example again written in the equivalent way using checkAll.

The second approach is more general purpose than returning a boolean, but the first approach is from the original haskell libraries that inspired this library.

By default, Kotest will run the property test 1000 times. We can easily customize this by specifying the iteration count when invoking the test method.

Let's say we want to run a test 10,000 times.

You saw in the previous examples that Kotest would provide values automatically based on the type parameter(s). It does this by locating a generator that generates values for the required type.

For example, the automatically provided Integer generator generates random ints from all possible values - negative, positive, infinities, zero and so on.

This is fine for basic tests but often we want more control over the sample space. For example, we may want to test a function for numbers in a certain range only.

Then you would need to specify the generator(s) manually.

You can see we created two tests and in each test passed a generator into the forAll function with a suitable int range.

See here for a list of the built in generators.

**Examples:**

Example 1 (kotlin):
```kotlin
class PropertyExample: StringSpec({   "String size" {      forAll<String, String> { a, b ->         (a + b).length == a.length + b.length      }   }})
```

Example 2 (kotlin):
```kotlin
class PropertyExample: StringSpec({   "String size" {      checkAll<String, String> { a, b ->         a + b shouldHaveLength a.length + b.length      }   }})
```

Example 3 (kotlin):
```kotlin
class PropertyExample: StringSpec({   "a many iterations test" {      checkAll<Double, Double>(10_000) { a, b ->         // test here      }   }})
```

Example 4 (kotlin):
```kotlin
class PropertyExample: StringSpec({   "is allowed to drink in Chicago" {      forAll(Arb.int(21..150)) { a ->         isDrinkingAge(a) // assuming some function that calculates if we're old enough to drink      }   }   "is allowed to drink in London" {      forAll(Arb.int(18..150)) { a ->         isDrinkingAge(a) // assuming some function that calculates if we're old enough to drink      }   }})
```

---

## Extra Arbs | Kotest

**URL:** https://kotest.io/docs/5.5.x/proptest/property-test-extra-arbs.html

**Contents:**
- Extra Arbs

If you are looking for a collection of Arbs for general purpose data generation, then Kotest has such a collection.

To use, add io.kotest.extensions:kotest-property-arbs:version to your build.

---

## Property Test Functions | Kotest

**URL:** https://kotest.io/docs/5.2.x/proptest/property-test-functions.html

**Contents:**
- Property Test Functions
  - For All​
  - Check All​
  - Iterations​
  - Specifying Generators​

There are two variants of functions that are used to execute a property test in Kotest: forAll and checkAll.

The first, forAll, accepts an n-arity function (a, ..., n) -> Boolean that tests the property. The test will pass if, for all input values, the function returns true.

Notice that this functions accepts type parameters for the argument types, with arity up to 14. Kotest uses these type parameters to locate a generator which provides (generates) random values of a suitable type.

For example, forAll<String, Int, Boolean> { a, b, c -> } is a 3-arity property test where argument a is a random String, argument b is a random int, and argument c is a random boolean.

The second, checkAll, accepts an n-arity function (a, ..., n) -> Unit in which you can simply execute assertions against the inputs. This approach will consider a test valid if no exceptions are thrown. Here is the same example again written in the equivalent way using checkAll.

The second approach is more general purpose than returning a boolean, but the first approach is from the original haskell libraries that inspired this library.

By default, Kotest will run the property test 1000 times. We can easily customize this by specifying the iteration count when invoking the test method.

Let's say we want to run a test 10,000 times.

You saw in the previous examples that Kotest would provide values automatically based on the type parameter(s). It does this by locating a generator that generates values for the required type.

For example, the automatically provided Integer generator generates random ints from all possible values - negative, positive, infinities, zero and so on.

This is fine for basic tests but often we want more control over the sample space. For example, we may want to test a function for numbers in a certain range only.

Then you would need to specify the generator(s) manually.

You can see we created two tests and in each test passed a generator into the forAll function with a suitable int range.

See here for a list of the built in generators.

**Examples:**

Example 1 (kotlin):
```kotlin
class PropertyExample: StringSpec({   "String size" {      forAll<String, String> { a, b ->         (a + b).length == a.length + b.length      }   }})
```

Example 2 (kotlin):
```kotlin
class PropertyExample: StringSpec({   "String size" {      checkAll<String, String> { a, b ->         a + b shouldHaveLength a.length + b.length      }   }})
```

Example 3 (kotlin):
```kotlin
class PropertyExample: StringSpec({   "a many iterations test" {      checkAll<Double, Double>(10_000) { a, b ->         // test here      }   }})
```

Example 4 (kotlin):
```kotlin
class PropertyExample: StringSpec({   "is allowed to drink in Chicago" {      forAll(Arb.int(21..150)) { a ->         isDrinkingAge(a) // assuming some function that calculates if we're old enough to drink      }   }   "is allowed to drink in London" {      forAll(Arb.int(18..150)) { a ->         isDrinkingAge(a) // assuming some function that calculates if we're old enough to drink      }   }})
```

---

## Statistics | Kotest

**URL:** https://kotest.io/docs/5.7.x/proptest/property-test-statistics.html

**Contents:**
- Statistics
- Labels​
  - Report Mode​
  - Checking Coverage of Statistics​
  - Custom Reports​

Sometimes you may like to know the types of values being generated by Kotest, to ensure that your generators are configured in the way you expect. Property test statistics are designed to fulfil this need.

The function collect is the entry point to statistcs and is used to count categories of values. We use this by invoking it inside a property test with the category we want to increment.

For example, lets say we wanted to gather statistics on the values of RoundingMode used by a BigDecimal. We would invoke checkAll as normal, passing the rounding mode to the collect function.

Now after the test is completed, Kotest will output the test name, and the counts/percentages for each category:

The category we use does not have to be an enum. It can be any object, and you can wrap in conditionals if you want more control. For example:

Sometimes you may wish to have orthogonal sets of statistics. For example, for a simple number test, we might want to confirm that a certain percentage are even numbers, and a certain percentage are negative. One way would be to have EVEN_POS, EVEN_NEG, ODD_POS, ODD_NEG:

This gives us one set of outputs:

However, as the combinations grow this will become unwieldy, so Kotest supports labelled statistics. You can think of this as distinct sets of statistics. To use labels, just pass the label name as the first arg to the collect method.

Now, Kotest will output multiple sets of statistics, with the label name in the title:

By default, statistics are printed for every property test. There are four modes which can be configured using the global configuration object PropertyTesting.

The possible options are:

If you wish to programmatically assert that certain values are being generated, then you can use the specify constraints that must be met.

For example, in our previous rounding example, we can check that at least 10% of inputs are covering HALF_DOWN, and 10% are covering FLOOR using withCoveragePercentages:

If we want to check by absolute numbers rather than percentages, we can use withCoverageCounts:

You can customize the report format, or generate reports from the raw data, by using your own instance of StatisticsReporter. This is configured via the global configuration object PropertyTesting.

**Examples:**

Example 1 (kotlin):
```kotlin
checkAll(Arb.enum<RoundingMode>(), Arb.bigDecimal()) { mode, decimal ->  collect(mode)  // test here}
```

Example 2 (yaml):
```yaml
Statistics: [collecting stats] (1000 iterations, 1 args)HALF_DOWN                                                     142 (14%)HALF_UP                                                       141 (14%)CEILING                                                       132 (13%)FLOOR                                                         122 (12%)UP                                                            119 (12%)UNNECESSARY                                                   119 (12%)HALF_EVEN                                                     118 (12%)DOWN                                                          107 (11%)
```

Example 3 (kotlin):
```kotlin
checkAll(Arb.int()) { k ->  when {    k % 2 == 0 -> collect("EVEN")    else -> collect("ODD")  }  // test here}
```

Example 4 (kotlin):
```kotlin
checkAll(Arb.int()) { k ->  when {    k > 0 && k % 2 == 0 -> collect("EVEN_POS")    k % 2 == 0 -> collect("EVEN_NEG")    k > 0 -> collect("ODD_POS")    else -> collect("ODD_NEG")  }  // test here}
```

---

## Global Configuration | Kotest

**URL:** https://kotest.io/docs/5.5.x/proptest/property-test-global-config.html

**Contents:**
- Global Configuration
  - Default Iterations​
  - Printing Shrink Steps​

Some property test settings can be set globally for all property tests.

The standard default iteration count is 1000. This means when you don't specify the iteration count in a property test, the default will be 1000.

We can override this default either by assigning a value to PropertyTesting.defaultIterationCount, or by using the system property kotest.proptest.default.iteration.count.

Any test which directly sets the iteration count will of course use that value.

If you are using Kotest framework, then you can perform this action before any tests by using project config. For example:

By default, when using shrinking, each shrinking step will not be logged, but only the final shrunk value.

To enable logging of each intermediate value, assign true to PropertyTesting.shouldPrintShrinkSteps or use the system property kotest.proptest.output.shrink-steps=true.

**Examples:**

Example 1 (kotlin):
```kotlin
PropertyTesting.defaultIterationCount = 123// will use 555 iterations specified in the testforAll<String, String>(555) { a,b -> a + b == "$a$b" }// will use 123 iterations from the global defaultforAll<String, String> { a,b -> a + b == "$a$b" }
```

Example 2 (kotlin):
```kotlin
class KotestConfig : AbstractProjectConfig() {   override suspend fun beforeProject() {      PropertyTesting.defaultIterationCount = 123   }}
```

---

## Global Configuration | Kotest

**URL:** https://kotest.io/docs/5.2.x/proptest/property-test-global-config.html

**Contents:**
- Global Configuration
  - Default Iterations​
  - Printing Shrink Steps​

Some property test settings can be set globally for all property tests.

The standard default iteration count is 1000. This means when you don't specify the iteration count in a property test, the default will be 1000.

We can override this default either by assigning a value to PropertyTesting.defaultIterationCount, or by using the system property kotest.proptest.default.iteration.count.

Any test which directly sets the iteration count will of course use that value.

By default, when using shrinking, each shrinking step will not be logged, but only the final shrunk value.

To enable logging of each intermediate value, assign true to PropertyTesting.shouldPrintShrinkSteps or use the system property kotest.proptest.output.shrink-steps=true.

**Examples:**

Example 1 (kotlin):
```kotlin
PropertyTesting.defaultIterationCount = 123// will use 555 iterations specified in the testforAll<String, String>(555) { a,b -> a + b == "$a$b" }// will use 123 iterations from the global defaultforAll<String, String> { a,b -> a + b == "$a$b" }
```

---

## Arrow Generators | Kotest

**URL:** https://kotest.io/docs/5.3.x/proptest/property-test-generators-arrow.html

**Contents:**
- Arrow Generators

Kotest provides an optional module that provides generators for Arrow.

To use, add io.kotest.extensions:kotest-property-arrow:versionand io.arrow-kt:arrow-core:arrow-version to your build. This holds true for the optics module kotest-property-arrow-optics, by adding io.arrow-kt:arrow-optics:arrow-version, too.

---

## Property-based Testing | Kotest

**URL:** https://kotest.io/docs/next/proptest/property-based-testing.html

**Contents:**
- Property-based Testing
- What is Property Testing?​
- Getting Started​
- Next Steps​

Kotest is split into several subprojects which can be used independently. One of these subprojects is the property test framework. You do not need to be using Kotest as your test framework (although you should!) to benefit from the property test support.

Developers typically write example-based tests. These are your garden variety unit tests you know and love. You provide some inputs, and some expected results, and a test framework like Kotest or JUnit checks that the actual results meet the expectations.

One problem with this approach is that it is very easy to miss errors due to edge cases that the developer didn't think about, or lack of coverage in the chosen inputs. Instead, with property testing, hundreds or thousands of values are fed into the same test, and the values are (usually) randomly generated by your property test framework.

For example, a good property test framework will include values like negative infinity, empty lists, strings with non-ascii characters, and so on. Things we often forget about when writing example based tests.

Property tests were originally conceived in frameworks like Quickcheck with the notion of testing a property on some object, ie. something that should hold true for all inputs. An invariant in other words. An example of an invariant is given two strings, a and b, then length(a + b) should always be equal to length(a) + length(b).

That is where the term property testing originates.

However, you do not have to use property tests to only test things like monad laws or basic numeric functions. Any test that would benefit from a wide array of input values is a good candidate. For example, we might have a function that validates usernames, and we want to test that valid emails are accepted. A property test would be useful here in generating 1000s of combinations to help harden our validation logic.

The property test framework is supported on all targets.

Add the following dependency to your build:

Add the following dependency to your build.

Add the following dependency to your commonTest sourceset:

Alternatively, add the dependency to a specific target. For example, we could add to the Javascript target only be using the jsTest sourceset.

To create input values for tests, Kotest uses the term generator. One generator per input argument is passed to a test function, and the test will execute for a set number of iterations.

**Examples:**

Example 1 (kotlin):
```kotlin
dependencies {   testImplementation("io.kotest:kotest-property:$version")}
```

Example 2 (xml):
```xml
<dependency>   <groupId>io.kotest</groupId>   <artifactId>kotest-property-jvm</artifactId>   <version>${version}</version>   <scope>test</scope></dependency>
```

Example 3 (kotlin):
```kotlin
kotlin {  sourceSets {    val commonTest by getting {      dependencies {        implementation("io.kotest:kotest-property:$version")      }    }  }}
```

Example 4 (kotlin):
```kotlin
kotlin {   targets {      js {         browser()         nodejs()      }   }   sourceSets {      val jsTest by getting {         dependencies {            implementation("io.kotest:kotest-property:$version")         }      }   }}
```

---

## Configuration | Kotest

**URL:** https://kotest.io/docs/proptest/property-test-config.html

**Contents:**
- Configuration
  - Seed​
  - Min Failure​
  - PropTestListener​
  - Handling Unprintable Characters in Failure Messages​

Kotest provides for the ability to specify some configuration options when running a property test. We do this by passing in an instance of PropTestConfig to the test methods.

A commonly used configuration option is specifying the seed used by the random source. This is used when you want to repeat the same values each time the test is run. You might want to do this if you find a test failure, and you want to ensure that that particular set of values continues to be executed in the future as a regression test.

For full details on how the seed is used click here.

By default, Kotest tolerates no failure. Perhaps you want to run some non-deterministic test a bunch of times, and you're happy to accept some small number of failures. You can specify that in config.

Sometimes in property test it is required to perform some setup and tear down in each iteration of test. For this purpose you can register a PropTestListener with PropTestConfig.

When property tests involve strings with unprintable characters, failure messages can be hard to read and debug. By setting outputHexForUnprintableChars to true in PropTestConfig, unprintable characters in failure messages are displayed as their Unicode code points in the format U+XXXX.

Default Value: outputHexForUnprintableChars is false by default.

Alternatively, you can set this option as a common project-wide setting in a kotest.properties file located in your classpath:

**Examples:**

Example 1 (kotlin):
```kotlin
class PropertyExample: FreeSpec({   "String size" {      forAll<String, String>(PropTestConfig(options here...)) { a,b ->         (a + b).length == a.length + b.length      }   }})
```

Example 2 (kotlin):
```kotlin
class PropertyExample: FreeSpec({   "some flakey test" {      forAll<String, String>(PropTestConfig(maxFailure = 3)) { a,b ->         // max of 3 inputs can fail      }   }})
```

Example 3 (kotlin):
```kotlin
class PropertyExample: FreeSpec({   "some property test which require setup and tear down in each iteration" {      forAll<String, String>(PropTestConfig(listeners = listOf(MyPropTestListener))) { a,b ->         // some assertion      }   }})
```

Example 4 (kotlin):
```kotlin
class PropertyExample : FreeSpec({    "handle unprintable characters in failure messages" {        forAll<String>(            PropTestConfig(outputHexForUnprintableChars = true)        ) { str ->            // some assertion        }    }})
```

---

## Extra Arbs | Kotest

**URL:** https://kotest.io/docs/5.3.x/proptest/property-test-extra-arbs.html

**Contents:**
- Extra Arbs

If you are looking for a collection of Arbs for general purpose data generation, then Kotest has such a collection.

To use, add io.kotest.extensions:kotest-property-arbs:version to your build.

---

## Custom Generators | Kotest

**URL:** https://kotest.io/docs/5.8.x/proptest/custom-generators.html

**Contents:**
- Custom Generators
  - Arbitrary​
  - Exhaustive​

To write your own generator for a type T, you just create an instance of Arb<T> or Exhaustive<T>.

When writing a custom arbitrary we can use the arbitrary builder which accepts a lambda that must return the type we are generating for. The parameter to this lambda is a RandomSource parameter which contains the seed and the Random instance. We should typically use the provided RandomSource if we need access to a kotlin.Random instance, as this instance will have been seeded by the framework to allow for repeatable tests.

For example, here is a custom arb that generates a random int between 3 and 6 using the arbitrary builder.

In addition to the RandomSource parameter, the arbitrary builder lambda also provides the ArbitraryBuilderSyntax context which we can leverage to compose other arbitraries when building ours.

For example, here is an Arbitrary that supports a custom class called Person, delegating to a String arbitrary and an Int arbitrary.

The resulting arbitrary produced using this syntax is equivalent to using map, flatMap and bind.

When writing a custom exhaustive we can use the exhaustive() extension function on a List. Nothing more to it than that really!

**Examples:**

Example 1 (kotlin):
```kotlin
val sillyArb = arbitrary { rs: RandomSource ->   rs.random.nextInt(3..6)}
```

Example 2 (kotlin):
```kotlin
data class Person(val name: String, val age: Int)val personArb = arbitrary {   val name = Arb.string(10..12).bind()   val age = Arb.int(21, 150).bind()   Person(name, age)}
```

Example 3 (kotlin):
```kotlin
val singleDigitPrimes = listOf(2,3,5,7).exhaustive()
```

Example 4 (kotlin):
```kotlin
class PropertyExample: StringSpec({    "testing single digit primes" {        checkAll(singleDigitPrimes) { prime ->           isPrime(prime) shouldBe true           isPrime(prime * prime) shouldBe false        }    }})
```

---

## Property Test Functions | Kotest

**URL:** https://kotest.io/docs/6.0/proptest/property-test-functions.html

**Contents:**
- Property Test Functions
  - For All​
  - Check All​
  - Iterations​
  - Specifying Generators​

There are two variants of functions that are used to execute a property test in Kotest: forAll and checkAll.

The first, forAll, accepts an n-arity function (a, ..., n) -> Boolean that tests the property. The test will pass if, for all input values, the function returns true.

Notice that this functions accepts type parameters for the argument types, with arity up to 14. Kotest uses these type parameters to locate a generator which provides (generates) random values of a suitable type.

For example, forAll<String, Int, Boolean> { a, b, c -> } is a 3-arity property test where argument a is a random String, argument b is a random int, and argument c is a random boolean.

The second, checkAll, accepts an n-arity function (a, ..., n) -> Unit in which you can simply execute assertions against the inputs. This approach will consider a test valid if no exceptions are thrown. Here is the same example again written in the equivalent way using checkAll.

The second approach is more general purpose than returning a boolean, but the first approach is from the original haskell libraries that inspired this library.

By default, Kotest will run the property test 1000 times. We can easily customize this by specifying the iteration count when invoking the test method.

Let's say we want to run a test 10,000 times.

You saw in the previous examples that Kotest would provide values automatically based on the type parameter(s). It does this by locating a generator that generates values for the required type.

For example, the automatically provided Integer generator generates random ints from all possible values - negative, positive, infinities, zero and so on.

This is fine for basic tests but often we want more control over the sample space. For example, we may want to test a function for numbers in a certain range only.

Then you would need to specify the generator(s) manually.

You can see we created two tests and in each test passed a generator into the forAll function with a suitable int range.

See here for a list of the built in generators.

**Examples:**

Example 1 (kotlin):
```kotlin
class PropertyExample: FreeSpec({   "String size" {      forAll<String, String> { a, b ->         (a + b).length == a.length + b.length      }   }})
```

Example 2 (kotlin):
```kotlin
class PropertyExample: FreeSpec({   "String size" {      checkAll<String, String> { a, b ->         a + b shouldHaveLength a.length + b.length      }   }})
```

Example 3 (kotlin):
```kotlin
class PropertyExample: FreeSpec({   "a many iterations test" {      checkAll<Double, Double>(10_000) { a, b ->         // test here      }   }})
```

Example 4 (kotlin):
```kotlin
class PropertyExample: FreeSpec({   "is allowed to drink in Chicago" {      forAll(Arb.int(21..150)) { a ->         isDrinkingAge(a) // assuming some function that calculates if we're old enough to drink      }   }   "is allowed to drink in London" {      forAll(Arb.int(18..150)) { a ->         isDrinkingAge(a) // assuming some function that calculates if we're old enough to drink      }   }})
```

---

## Kotlinx DateTime Gens | Kotest

**URL:** https://kotest.io/docs/5.3.x/proptest/kotlinx-datetime-gens.html

**Contents:**
- Kotlinx DateTime Gens

Kotest provides an optional module that provides generators for KotlinX DateTime.

To use, add io.kotest.extensions:kotest-property-datetime:version to your build.

---

## Custom Generators | Kotest

**URL:** https://kotest.io/docs/next/proptest/custom-generators.html

**Contents:**
- Custom Generators
  - Arbitrary​
  - Exhaustive​

To write your own generator for a type T, you just create an instance of Arb<T> or Exhaustive<T>.

When writing a custom arbitrary we can use the arbitrary builder which accepts a lambda that must return the type we are generating for. The parameter to this lambda is a RandomSource parameter which contains the seed and the Random instance. We should typically use the provided RandomSource if we need access to a kotlin.Random instance, as this instance will have been seeded by the framework to allow for repeatable tests.

For example, here is a custom arb that generates a random int between 3 and 6 using the arbitrary builder.

In addition to the RandomSource parameter, the arbitrary builder lambda also provides the ArbitraryBuilderSyntax context which we can leverage to compose other arbitraries when building ours.

For example, here is an Arbitrary that supports a custom class called Person, delegating to a String arbitrary and an Int arbitrary.

The resulting arbitrary produced using this syntax is equivalent to using map, flatMap and bind.

When writing a custom exhaustive we can use the exhaustive() extension function on a List. Nothing more to it than that really!

**Examples:**

Example 1 (kotlin):
```kotlin
val sillyArb = arbitrary { rs: RandomSource ->   rs.random.nextInt(3..6)}
```

Example 2 (kotlin):
```kotlin
data class Person(val name: String, val age: Int)val personArb = arbitrary {   val name = Arb.string(10..12).bind()   val age = Arb.int(21, 150).bind()   Person(name, age)}
```

Example 3 (kotlin):
```kotlin
val singleDigitPrimes = listOf(2,3,5,7).exhaustive()
```

Example 4 (kotlin):
```kotlin
class PropertyExample: FreeSpec({    "testing single digit primes" {        checkAll(singleDigitPrimes) { prime ->           isPrime(prime) shouldBe true           isPrime(prime * prime) shouldBe false        }    }})
```

---

## Generator Operations | Kotest

**URL:** https://kotest.io/docs/5.3.x/proptest/generator-operations.html

**Contents:**
- Generator Operations
- Next​
- Filter​
- Map​
- FlatMap​
- Merging​
- Bind​

If you want to use an Arb to just return a value (even outside of a property test), then you can call next on it.

If you have an arb and you want to create a new arb that provides a subset of values, you can call filter on the source arb. For example, one way of generating even numbers is to take the integer arb, and filter out odd values. Viz:

If you have an arb and you want to transform the value generated, you can use map.

If you have an arb whose emission or edge cases depends on the emission of the previous arbitraries, you can use flatMap.

Two generators can be merged together, so that the resulting elements are equally sampled from both generators.

So with the following example would have an equal chance to yield either "a" or "b" on each random sample:

For merging more than two arbitraries, Arb.choice or Arb.choose might be more appropriate. For instance we can use:

Bind is useful if you want to apply multiple arbitraries. We can take a look at how we might construct values for a data class using bind.

**Examples:**

Example 1 (kotlin):
```kotlin
val arbA: Arb<A> = ...val a = arbA.next() // use Random.Defaultval a2 = arbA.next(rs) // pass in Random
```

Example 2 (kotlin):
```kotlin
val evens = Arb.int().filter { it.value % 2 == 0 }val odds = Arb.int().filter { it.value % 2 == 1 }
```

Example 3 (kotlin):
```kotlin
val integerStrings: Arb<String> = Arb.int().map { it.toString() }
```

Example 4 (kotlin):
```kotlin
val dependentArbs: Arb<String> = Arb.of("foo", "bar").flatMap { prefix ->   Arb.int(1..10).map { integer ->      "${prefix}-${integer}"   }}
```

---

## Assumptions | Kotest

**URL:** https://kotest.io/docs/5.7.x/proptest/property-test-assumptions.html

**Contents:**
- Assumptions
  - Assertions​
  - Max Discard Percentage​

To restrict the set the generated values from a generator, we can use filter to return a constrained arb.

However if we need to restrict inputs based on the relationship between values, then filtering on a single arb won't work. Consider an example where we want to test that two non-equal strings have a non-zero Levenshtein distance.

This will periodically fail - whenever two equal strings are generated. One approach would be to just wrap the tests in an if/else block and avoid those undesired inputs.

But in more complicated scenarios we could easily introduce a bug and filter all our inputs.

Kotest provides a feature called assumptions that will filter out unwanted combinations, while tracking that we are not filtering too many.

An assumption accepts a boolean value passed to the withAssumptions function, that if true, will allow the property test to continue, but if false, that particular iteration is skipped. For example, the previous example will now pass:

Alternatively, you can use inline syntax:

Kotest expands on basic boolean assumptions by allowing you to specify assertions in an assumption function.

For example, building on the previous example:

Here we are ensuring that all inputs are not equal, and that the inputs have the same length. Any assertion that throws AssertionError can be used here, including all the assertions provided by Kotest.

This also supports inline syntax:

By default, the maximum discard percentage is 10%. If more combinations are discarded than that, the property test will fail. This helps avoid a scenario where we erroneously discard too many, or even all, our inputs.

For example, the following would fail by default because we would be filtering ~50% of values.

But if we wanted to allow this regardless, we can use the maxDiscardPercentage to increase the allowed discard rate.

It is generally better to adjust your arbs to produce values closer to what you need, so that you only need to filter out unwanted edge cases.

**Examples:**

Example 1 (kotlin):
```kotlin
val evens = Arb.int().filter { it.value % 2 == 0 }
```

Example 2 (kotlin):
```kotlin
checkAll<String, String> { a, b ->  levenshtein(a, b) shouldBeGreaterThan 0}
```

Example 3 (kotlin):
```kotlin
checkAll<String, String> { a, b ->  if (a != b)    levenshtein(a, b) shouldBeGreaterThan 0}
```

Example 4 (kotlin):
```kotlin
checkAll<String, String> { a, b ->  withAssumptions(a != b) {    levenshtein(a, b) shouldBeGreaterThan 0  }}
```

---

## Global Configuration | Kotest

**URL:** https://kotest.io/docs/5.7.x/proptest/property-test-global-config.html

**Contents:**
- Global Configuration
  - Default Iterations​
  - Printing Shrink Steps​

Some property test settings can be set globally for all property tests.

The standard default iteration count is 1000. This means when you don't specify the iteration count in a property test, the default will be 1000.

We can override this default either by assigning a value to PropertyTesting.defaultIterationCount, or by using the system property kotest.proptest.default.iteration.count.

Any test which directly sets the iteration count will of course use that value.

If you are using Kotest framework, then you can perform this action before any tests by using project config. For example:

By default, when using shrinking, each shrinking step will not be logged, but only the final shrunk value.

To enable logging of each intermediate value, assign true to PropertyTesting.shouldPrintShrinkSteps or use the system property kotest.proptest.output.shrink-steps=true.

**Examples:**

Example 1 (kotlin):
```kotlin
PropertyTesting.defaultIterationCount = 123// will use 555 iterations specified in the testforAll<String, String>(555) { a,b -> a + b == "$a$b" }// will use 123 iterations from the global defaultforAll<String, String> { a,b -> a + b == "$a$b" }
```

Example 2 (kotlin):
```kotlin
class KotestConfig : AbstractProjectConfig() {   override suspend fun beforeProject() {      PropertyTesting.defaultIterationCount = 123   }}
```

---

## Assumptions | Kotest

**URL:** https://kotest.io/docs/5.5.x/proptest/property-test-assumptions.html

**Contents:**
- Assumptions
  - Assertions​
  - Max Discard Percentage​

To restrict the set the generated values from a generator, we can use filter to return a constrained arb.

However if we need to restrict inputs based on the relationship between values, then filtering on a single arb won't work. Consider an example where we want to test that two non-equal strings have a non-zero Levenshtein distance.

This will periodically fail - whenever two equal strings are generated. One approach would be to just wrap the tests in an if/else block and avoid those undesired inputs.

But in more complicated scenarios we could easily introduce a bug and filter all our inputs.

Kotest provides a feature called assumptions that will filter out unwanted combinations, while tracking that we are not filtering too many.

An assumption accepts a boolean value passed to the withAssumptions function, that if true, will allow the property test to continue, but if false, that particular iteration is skipped. For example, the previous example will now pass:

Alternatively, you can use inline syntax:

Kotest expands on basic boolean assumptions by allowing you to specify assertions in an assumption function.

For example, building on the previous example:

Here we are ensuring that all inputs are not equal, and that the inputs have the same length. Any assertion that throws AssertionError can be used here, including all the assertions provided by Kotest.

This also supports inline syntax:

By default, the maximum discard percentage is 10%. If more combinations are discarded than that, the property test will fail. This helps avoid a scenario where we erroneously discard too many, or even all, our inputs.

For example, the following would fail by default because we would be filtering ~50% of values.

But if we wanted to allow this regardless, we can use the maxDiscardPercentage to increase the allowed discard rate.

It is generally better to adjust your arbs to produce values closer to what you need, so that you only need to filter out unwanted edge cases.

**Examples:**

Example 1 (kotlin):
```kotlin
val evens = Arb.int().filter { it.value % 2 == 0 }
```

Example 2 (kotlin):
```kotlin
checkAll<String, String> { a, b ->  levenshtein(a, b) shouldBeGreaterThan 0}
```

Example 3 (kotlin):
```kotlin
checkAll<String, String> { a, b ->  if (a != b)    levenshtein(a, b) shouldBeGreaterThan 0}
```

Example 4 (kotlin):
```kotlin
checkAll<String, String> { a, b ->  withAssumptions(a != b) {    levenshtein(a, b) shouldBeGreaterThan 0  }}
```

---

## Generators | Kotest

**URL:** https://kotest.io/docs/5.9.x/proptest/property-test-generators.html

**Contents:**
- Generators
- Arbitrary​
- Exhaustive​

Generated values are provided by instances of the sealed class Gen. You can think of a Gen as kind of like an input stream but for property test values. Each Gen will provide a (usually) infinite stream of these values for one particular type.

Kotest has two types of generators - Arb for generating arbitrary (random) values and Exhaustive for generating a finite set of values in a closed space.

Both types of gens can be mixed and matched in property tests. For example, you could test a function with 100 random positive integers (an arb) alongside every even number from 0 to 200 (exhaustive).

Some generators are only available on the JVM. See the full list here.

Arbs generate two types of values - a hard coded set of edge cases and an infinite stream of randomly chosen samples.

The samples may be repeated, and some values may never be generated at all. For example generating 1000 integers between 0 and Int.MAX cannot return all possible values, and some values may happen to be generated more than once. Similarly, generating 1000 random integers between 0 and 500 will definitely result in some values appearing more than once.

Some common arbitraries include numbers with or without a range, strings in the unicode set, random lists, data classes with random parameters, emails, codepoints, chars and so on.

In addition to the random values, arbs may provide edge cases. One of the design features of Kotest's property testing is that values for some types will always include "common" edge cases that you probably want to be included in your tests.

For example, when testing a function that accepts an integer, you probably want to ensure that, at the very least, it is tested with zero, a positive number and a negative number. If only random values were provided, the chances of zero appearing would be fairly low, so Kotest will always provide some "edge cases" for integers (unless you specify otherwise).

When executing tests, the framework will alternate randomly between samples and edge cases. The split is determined by a configuration value which defaults to 2% edge cases.

Not all arbs have edge cases, but the arbs for the most common types do. Here are some examples of edge cases used by some arbs:

Exhaustives generate all values from a given space. This is useful when you want to ensure every value in that space is used. For example, for enum values, it is usually more helpful to ensure each enum is used, rather than picking randomly from the enums values and potentially missing some and duplicating others.

Typical exhaustives include small collections, enums, boolean values, powerset of a list or set, pre-defined integer ranges, and predefined string ranges.

Once an exhaustive has provided all it's values, it will loop and start again, so an exhaustive can be used in a test that requires any number of inputs.

Here we asked for 100 iterations, so each value of Season would be provided 25 times.

**Examples:**

Example 1 (php):
```php
enum class Season { Winter, Fall, Spring, Summer }forAll<Int, Season>(100) { a, season -> ... }
```

---

## Extra Arbs | Kotest

**URL:** https://kotest.io/docs/next/proptest/property-test-extra-arbs.html

**Contents:**
- Extra Arbs

If you are looking for a collection of Arbs for general purpose data generation, then Kotest has such a collection.

To use, add io.kotest.extensions:kotest-property-arbs:version to your build.

---

## Shrinking | Kotest

**URL:** https://kotest.io/docs/proptest/property-test-shrinking.html

**Contents:**
- Shrinking
- Shrinking for built-in generators​
- Shrinking for custom generators​

In property-based testing, the initially found failing case may contain a lot of complexity that does actually cause the test to fail. Shrinking is the mechanism through which a property-based testing framework can simplify failing cases in order to find out the minimal reproducible case is. In Kotest, the way in which failing cases from generators are shrunk is defined by implementations of the Shrinker interface. Built-in generators generally have a default Shrinker defined by the framework, while custom generators can be given a custom Shrinker implementation.

Built-in generators (see Generators List) have a default Shrinker defined by the framework. A shrink function takes as input the value that failed the test and returns a list of new values on which Kotest can apply the test. The exact behaviour depends on the data type. For instance, a string could be shrunk by dropping the first or last character while for integers we could decrement or halve the value. In addition, shrink behaviour is defined for edge cases such as an empty string or the integer 0. The shrinking is performed when a test that uses such generator fails.

If the test fails for one of the generated inputs then the shrinking result is shown:

By default, Kotest will shrink 1000 times. This behaviour is configurable. For example, if you want to continue shrinking without bounds:

If you want to configure the behaviour globally, then either modify the value of PropertyTesting.defaultShrinkingMode or use the following system properties as in this example:

Custom generators do not have a Shrinker defined by Kotest. Instead, custom Shrinkers can be implemented. Below is an example where the Shrinker returns coordinates that are next to the value itself.

**Examples:**

Example 1 (kotlin):
```kotlin
Arb.positiveInt().checkAll { i ->    calculateProperty(i) shouldBe true}
```

Example 2 (typescript):
```typescript
Property test failed for inputs0) 1792716902Caused by io.kotest.assertions.AssertionFailedError: expected:<1792716902> but was:<0> at    PropertyBasedTest$1$1$3$1.invokeSuspend(PropertyBasedTest.kt:54)    PropertyBasedTest$1$1$3$1.invoke(PropertyBasedTest.kt)    PropertyBasedTest$1$1$3$1.invoke(PropertyBasedTest.kt)    io.kotest.property.internal.ProptestKt$proptest$3$2.invokeSuspend(proptest.kt:45)Attempting to shrink arg 1792716902Shrink #1: 1 passShrink #2: 597572300 failShrink #3: 199190766 failShrink #4: 66396922 failShrink #5: 22132307 failShrink #6: 7377435 failShrink #7: 2459145 fail[...]Shrink #999: 29948 passShrink #1000: 44922 passShrink #1001: 59896 passShrink #1002: 89839 failShrink result (after 1002 shrinks) => 89839Caused by io.kotest.assertions.AssertionFailedError: expected:<89839> but was:<0> at    PropertyBasedTest$1$1$3$1.invokeSuspend(PropertyBasedTest.kt:54)    PropertyBasedTest$1$1$3$1.invoke(PropertyBasedTest.kt)    PropertyBasedTest$1$1$3$1.invoke(PropertyBasedTest.kt)    io.kotest.property.internal.ShrinkfnsKt$shrinkfn$1$1$smallestA$1.invokeSuspend(shrinkfns.kt:19)
```

Example 3 (kotlin):
```kotlin
Arb.positiveInt().checkAll(PropTestConfig(shrinkingMode = ShrinkingMode.Unbounded)) { i ->    calculateProperty(i) shouldBe true}
```

Example 4 (markdown):
```markdown
# If you want to disable shrinking:kotest.proptest.default.shrinking.mode=off# If you want to continue shrinking without bounds:kotest.proptest.default.shrinking.mode=unbounded# If you want to shrink a bounded number of times (e.g. 500):kotest.proptest.default.shrinking.mode=boundedkotest.proptest.default.shrinking.bound=500
```

---

## Extra Arbs | Kotest

**URL:** https://kotest.io/docs/5.9.x/proptest/property-test-extra-arbs.html

**Contents:**
- Extra Arbs

If you are looking for a collection of Arbs for general purpose data generation, then Kotest has such a collection.

To use, add io.kotest.extensions:kotest-property-arbs:version to your build.

---

## Custom Generators | Kotest

**URL:** https://kotest.io/docs/5.3.x/proptest/custom-generators.html

**Contents:**
- Custom Generators
  - Arbitrary​
  - Exhaustive​

To write your own generator for a type T, you just create an instance of Arb<T> or Exhaustive<T>.

When writing a custom arbitrary we can use the arbitrary builder which accepts a lambda that must return the type we are generating for. The parameter to this lambda is a RandomSource parameter which contains the seed and the Random instance. We should typically use the provided RandomSource if we need access to a kotlin.Random instance, as this instance will have been seeded by the framework to allow for repeatable tests.

For example, here is a custom arb that generates a random int between 3 and 6 using the arbitrary builder.

In addition to the RandomSource parameter, the arbitrary builder lambda also provides the ArbitraryBuilderSyntax context which we can leverage to compose other arbitraries when building ours.

For example, here is an Arbitrary that supports a custom class called Person, delegating to a String arbitrary and an Int arbitrary.

The resulting arbitrary produced using this syntax is equivalent to using map, flatMap and bind.

When writing a custom exhaustive we can use the exhaustive() extension function on a List. Nothing more to it than that really!

**Examples:**

Example 1 (kotlin):
```kotlin
val sillyArb = arbitrary { rs: RandomSource ->   rs.random.nextInt(3..6)}
```

Example 2 (kotlin):
```kotlin
data class Person(val name: String, val age: Int)val personArb = arbitrary {   val name = Arb.string(10..12).bind()   val age = Arb.int(21, 150).bind()   Person(name, age)}
```

Example 3 (kotlin):
```kotlin
val singleDigitPrimes = listOf(2,3,5,7).exhaustive()
```

Example 4 (kotlin):
```kotlin
class PropertyExample: StringSpec({    "testing single digit primes" {        checkAll(singleDigitPrimes) { prime ->           isPrime(prime) shouldBe true           isPrime(prime * prime) shouldBe false        }    }})
```

---

## Kotlinx DateTime Gens | Kotest

**URL:** https://kotest.io/docs/5.6.x/proptest/kotlinx-datetime-gens.html

**Contents:**
- Kotlinx DateTime Gens

Kotest provides an optional module that provides generators for KotlinX DateTime.

To use, add io.kotest.extensions:kotest-property-datetime:version to your build.

---

## Shrinking | Kotest

**URL:** https://kotest.io/docs/5.4.x/proptest/property-test-shrinking.html

**Contents:**
- Shrinking
- Shrinking for built-in generators​
- Shrinking for custom generators​

In property-based testing, the initially found failing case may contain a lot of complexity that does actually cause the test to fail. Shrinking is the mechanism through which a property-based testing framework can simplify failing cases in order to find out the minimal reproducible case is. In Kotest, the way in which failing cases from generators are shrunk is defined by implementations of the Shrinker interface. Built-in generators generally have a default Shrinker defined by the framework, while custom generators can be given a custom Shrinker implementation.

Built-in generators (see Generators List) have a default Shrinker defined by the framework. A shrink function takes as input the value that failed the test and returns a list of new values on which Kotest can appy the test. The exact behaviour depends on the data type. For instance, a string could be shrunk by dropping the first or last character while for integers we could decrement or halve the value. In addition, shrink behaviour is defined for edge cases such as an empty string or the integer 0. The shrinking is performed when a test that uses such generator fails.

If the test fails for one of the generated inputs then the shrinking result is shown:

By default, Kotest will shrink 1000 times. This behaviour is configurable. For example, if you want to continue shrinking without bounds:

Custom generators do not have a Shrinker defined by Kotest. Instead, custom Shrinkers can be implemented. Below is an example where the Shrinker returns coordinates that are next to the value itself.

**Examples:**

Example 1 (kotlin):
```kotlin
Arb.positiveInt().checkAll { i ->    calculateProperty(i) shouldBe true}
```

Example 2 (typescript):
```typescript
Property test failed for inputs0) 1792716902Caused by io.kotest.assertions.AssertionFailedError: expected:<1792716902> but was:<0> at    PropertyBasedTest$1$1$3$1.invokeSuspend(PropertyBasedTest.kt:54)    PropertyBasedTest$1$1$3$1.invoke(PropertyBasedTest.kt)    PropertyBasedTest$1$1$3$1.invoke(PropertyBasedTest.kt)    io.kotest.property.internal.ProptestKt$proptest$3$2.invokeSuspend(proptest.kt:45)Attempting to shrink arg 1792716902Shrink #1: 1 passShrink #2: 597572300 failShrink #3: 199190766 failShrink #4: 66396922 failShrink #5: 22132307 failShrink #6: 7377435 failShrink #7: 2459145 fail[...]Shrink #999: 29948 passShrink #1000: 44922 passShrink #1001: 59896 passShrink #1002: 89839 failShrink result (after 1002 shrinks) => 89839Caused by io.kotest.assertions.AssertionFailedError: expected:<89839> but was:<0> at    PropertyBasedTest$1$1$3$1.invokeSuspend(PropertyBasedTest.kt:54)    PropertyBasedTest$1$1$3$1.invoke(PropertyBasedTest.kt)    PropertyBasedTest$1$1$3$1.invoke(PropertyBasedTest.kt)    io.kotest.property.internal.ShrinkfnsKt$shrinkfn$1$1$smallestA$1.invokeSuspend(shrinkfns.kt:19)
```

Example 3 (kotlin):
```kotlin
Arb.positiveInt().checkAll(PropTestConfig(shrinkingMode = ShrinkingMode.Unbounded)) { i ->    calculateProperty(i) shouldBe true}
```

Example 4 (kotlin):
```kotlin
data class Coordinate(val x: Int, val y: Int)class CoordinateTest : FunSpec({    context("Coordinate Transformations") {        // Shrinker takes the four neighbouring coordinates        val coordinateShrinker = Shrinker<Coordinate> { c ->            listOf(                Coordinate(c.x - 1, c.y),                Coordinate(c.x, c.y - 1),                Coordinate(c.x + 1, c.y),                Coordinate(c.x, c.y + 1),            )        }        val coordinateArb = arbitrary(coordinateShrinker) {            Coordinate(Arb.nonNegativeInt().bind(), Arb.nonNegativeInt().bind())        }        test("Coordinates are always positive after transformation") {            coordinateArb.checkAll {                transform(it).x shouldBeGreaterThanOrEqualTo 0                transform(it).y shouldBeGreaterThanOrEqualTo 0            }        }    }})
```

---

## Property Test Functions | Kotest

**URL:** https://kotest.io/docs/5.5.x/proptest/property-test-functions.html

**Contents:**
- Property Test Functions
  - For All​
  - Check All​
  - Iterations​
  - Specifying Generators​

There are two variants of functions that are used to execute a property test in Kotest: forAll and checkAll.

The first, forAll, accepts an n-arity function (a, ..., n) -> Boolean that tests the property. The test will pass if, for all input values, the function returns true.

Notice that this functions accepts type parameters for the argument types, with arity up to 14. Kotest uses these type parameters to locate a generator which provides (generates) random values of a suitable type.

For example, forAll<String, Int, Boolean> { a, b, c -> } is a 3-arity property test where argument a is a random String, argument b is a random int, and argument c is a random boolean.

The second, checkAll, accepts an n-arity function (a, ..., n) -> Unit in which you can simply execute assertions against the inputs. This approach will consider a test valid if no exceptions are thrown. Here is the same example again written in the equivalent way using checkAll.

The second approach is more general purpose than returning a boolean, but the first approach is from the original haskell libraries that inspired this library.

By default, Kotest will run the property test 1000 times. We can easily customize this by specifying the iteration count when invoking the test method.

Let's say we want to run a test 10,000 times.

You saw in the previous examples that Kotest would provide values automatically based on the type parameter(s). It does this by locating a generator that generates values for the required type.

For example, the automatically provided Integer generator generates random ints from all possible values - negative, positive, infinities, zero and so on.

This is fine for basic tests but often we want more control over the sample space. For example, we may want to test a function for numbers in a certain range only.

Then you would need to specify the generator(s) manually.

You can see we created two tests and in each test passed a generator into the forAll function with a suitable int range.

See here for a list of the built in generators.

**Examples:**

Example 1 (kotlin):
```kotlin
class PropertyExample: StringSpec({   "String size" {      forAll<String, String> { a, b ->         (a + b).length == a.length + b.length      }   }})
```

Example 2 (kotlin):
```kotlin
class PropertyExample: StringSpec({   "String size" {      checkAll<String, String> { a, b ->         a + b shouldHaveLength a.length + b.length      }   }})
```

Example 3 (kotlin):
```kotlin
class PropertyExample: StringSpec({   "a many iterations test" {      checkAll<Double, Double>(10_000) { a, b ->         // test here      }   }})
```

Example 4 (kotlin):
```kotlin
class PropertyExample: StringSpec({   "is allowed to drink in Chicago" {      forAll(Arb.int(21..150)) { a ->         isDrinkingAge(a) // assuming some function that calculates if we're old enough to drink      }   }   "is allowed to drink in London" {      forAll(Arb.int(18..150)) { a ->         isDrinkingAge(a) // assuming some function that calculates if we're old enough to drink      }   }})
```

---

## Arrow Generators | Kotest

**URL:** https://kotest.io/docs/5.2.x/proptest/property-test-generators-arrow.html

**Contents:**
- Arrow Generators

Kotest provides an optional module that provides generators for Arrow.

To use, add io.kotest.extensions:kotest-property-arrow:versionand io.arrow-kt:arrow-core:arrow-version to your build. This holds true for the optics module kotest-property-arrow-optics, by adding io.arrow-kt:arrow-optics:arrow-version, too.

---

## Property Test Seeds | Kotest

**URL:** https://kotest.io/docs/5.4.x/proptest/property-test-seeds.html

**Contents:**
- Property Test Seeds
  - Manually specifying the seed​
  - Rerunning failed seeds​
  - Failing when seeds set​

When a property test is executed, the values are generated using a random source that is created from a seed value. By default this seed value is itself randomly chosen (using the default kotlin.random.Random instance). However, there are times when this value needs to be fixed or repeated.

You can change the default used by all tests, unless overriden through the options listed below, by changing the configuration value PropertyTesting.defaultSeed.

To manually set the seed, pass an instance of PropTestConfig to your prop test methods. You might want to do this if you find a test failure, and you want to ensure that those values continue to be executed in the future as a regression test.

Whenever a property test fails, Kotest will output the seed that was used. You can duplicate the test, setting it to use this seed so you have permanent regression test for those values.

By default, when a property test fails, the seed used by that test is written to a file in ~/.kotest/seeds/<spec>/<testname>. Whenever a property test runs, this seed is detected if the file exists, and used in place of a random seed. Next time the test is successful, the seed file will be removed.

A manually specified seed always takes precedence over a failed seed.

This feature can be disabled by setting PropertyTesting.writeFailedSeed = false

Some users prefer to avoid manually specifying seeds. They want to use them locally only, when developing, but to avoid checking them in. If this is your style, then set PropertyTesting.failOnSeed = false or the env var kotest.proptest.seed.fail-if-set to false on your server.

Then if a seed is detected, the test suite will fail.

**Examples:**

Example 1 (kotlin):
```kotlin
class PropertyExample : StringSpec({  "String size" {    forAll<String, String>(PropTestConfig(seed = 127305235)) { a, b ->      (a + b).length == a.length + b.length    }  }})
```

---

## Custom Generators | Kotest

**URL:** https://kotest.io/docs/5.9.x/proptest/custom-generators.html

**Contents:**
- Custom Generators
  - Arbitrary​
  - Exhaustive​

To write your own generator for a type T, you just create an instance of Arb<T> or Exhaustive<T>.

When writing a custom arbitrary we can use the arbitrary builder which accepts a lambda that must return the type we are generating for. The parameter to this lambda is a RandomSource parameter which contains the seed and the Random instance. We should typically use the provided RandomSource if we need access to a kotlin.Random instance, as this instance will have been seeded by the framework to allow for repeatable tests.

For example, here is a custom arb that generates a random int between 3 and 6 using the arbitrary builder.

In addition to the RandomSource parameter, the arbitrary builder lambda also provides the ArbitraryBuilderSyntax context which we can leverage to compose other arbitraries when building ours.

For example, here is an Arbitrary that supports a custom class called Person, delegating to a String arbitrary and an Int arbitrary.

The resulting arbitrary produced using this syntax is equivalent to using map, flatMap and bind.

When writing a custom exhaustive we can use the exhaustive() extension function on a List. Nothing more to it than that really!

**Examples:**

Example 1 (kotlin):
```kotlin
val sillyArb = arbitrary { rs: RandomSource ->   rs.random.nextInt(3..6)}
```

Example 2 (kotlin):
```kotlin
data class Person(val name: String, val age: Int)val personArb = arbitrary {   val name = Arb.string(10..12).bind()   val age = Arb.int(21, 150).bind()   Person(name, age)}
```

Example 3 (kotlin):
```kotlin
val singleDigitPrimes = listOf(2,3,5,7).exhaustive()
```

Example 4 (kotlin):
```kotlin
class PropertyExample: StringSpec({    "testing single digit primes" {        checkAll(singleDigitPrimes) { prime ->           isPrime(prime) shouldBe true           isPrime(prime * prime) shouldBe false        }    }})
```

---

## Shrinking | Kotest

**URL:** https://kotest.io/docs/5.5.x/proptest/property-test-shrinking.html

**Contents:**
- Shrinking
- Shrinking for built-in generators​
- Shrinking for custom generators​

In property-based testing, the initially found failing case may contain a lot of complexity that does actually cause the test to fail. Shrinking is the mechanism through which a property-based testing framework can simplify failing cases in order to find out the minimal reproducible case is. In Kotest, the way in which failing cases from generators are shrunk is defined by implementations of the Shrinker interface. Built-in generators generally have a default Shrinker defined by the framework, while custom generators can be given a custom Shrinker implementation.

Built-in generators (see Generators List) have a default Shrinker defined by the framework. A shrink function takes as input the value that failed the test and returns a list of new values on which Kotest can appy the test. The exact behaviour depends on the data type. For instance, a string could be shrunk by dropping the first or last character while for integers we could decrement or halve the value. In addition, shrink behaviour is defined for edge cases such as an empty string or the integer 0. The shrinking is performed when a test that uses such generator fails.

If the test fails for one of the generated inputs then the shrinking result is shown:

By default, Kotest will shrink 1000 times. This behaviour is configurable. For example, if you want to continue shrinking without bounds:

Custom generators do not have a Shrinker defined by Kotest. Instead, custom Shrinkers can be implemented. Below is an example where the Shrinker returns coordinates that are next to the value itself.

**Examples:**

Example 1 (kotlin):
```kotlin
Arb.positiveInt().checkAll { i ->    calculateProperty(i) shouldBe true}
```

Example 2 (typescript):
```typescript
Property test failed for inputs0) 1792716902Caused by io.kotest.assertions.AssertionFailedError: expected:<1792716902> but was:<0> at    PropertyBasedTest$1$1$3$1.invokeSuspend(PropertyBasedTest.kt:54)    PropertyBasedTest$1$1$3$1.invoke(PropertyBasedTest.kt)    PropertyBasedTest$1$1$3$1.invoke(PropertyBasedTest.kt)    io.kotest.property.internal.ProptestKt$proptest$3$2.invokeSuspend(proptest.kt:45)Attempting to shrink arg 1792716902Shrink #1: 1 passShrink #2: 597572300 failShrink #3: 199190766 failShrink #4: 66396922 failShrink #5: 22132307 failShrink #6: 7377435 failShrink #7: 2459145 fail[...]Shrink #999: 29948 passShrink #1000: 44922 passShrink #1001: 59896 passShrink #1002: 89839 failShrink result (after 1002 shrinks) => 89839Caused by io.kotest.assertions.AssertionFailedError: expected:<89839> but was:<0> at    PropertyBasedTest$1$1$3$1.invokeSuspend(PropertyBasedTest.kt:54)    PropertyBasedTest$1$1$3$1.invoke(PropertyBasedTest.kt)    PropertyBasedTest$1$1$3$1.invoke(PropertyBasedTest.kt)    io.kotest.property.internal.ShrinkfnsKt$shrinkfn$1$1$smallestA$1.invokeSuspend(shrinkfns.kt:19)
```

Example 3 (kotlin):
```kotlin
Arb.positiveInt().checkAll(PropTestConfig(shrinkingMode = ShrinkingMode.Unbounded)) { i ->    calculateProperty(i) shouldBe true}
```

Example 4 (kotlin):
```kotlin
data class Coordinate(val x: Int, val y: Int)class CoordinateTest : FunSpec({    context("Coordinate Transformations") {        // Shrinker takes the four neighbouring coordinates        val coordinateShrinker = Shrinker<Coordinate> { c ->            listOf(                Coordinate(c.x - 1, c.y),                Coordinate(c.x, c.y - 1),                Coordinate(c.x + 1, c.y),                Coordinate(c.x, c.y + 1),            )        }        val coordinateArb = arbitrary(coordinateShrinker) {            Coordinate(Arb.nonNegativeInt().bind(), Arb.nonNegativeInt().bind())        }        test("Coordinates are always positive after transformation") {            coordinateArb.checkAll {                transform(it).x shouldBeGreaterThanOrEqualTo 0                transform(it).y shouldBeGreaterThanOrEqualTo 0            }        }    }})
```

---

## Generator Operations | Kotest

**URL:** https://kotest.io/docs/5.4.x/proptest/generator-operations.html

**Contents:**
- Generator Operations
- Next​
- Filter​
- Map​
- FlatMap​
- Merging​
- Bind​

If you want to use an Arb to just return a value (even outside of a property test), then you can call next on it.

If you have an arb and you want to create a new arb that provides a subset of values, you can call filter on the source arb. For example, one way of generating even numbers is to take the integer arb, and filter out odd values. Viz:

If you have an arb and you want to transform the value generated, you can use map.

If you have an arb whose emission or edge cases depends on the emission of the previous arbitraries, you can use flatMap.

Two generators can be merged together, so that the resulting elements are equally sampled from both generators.

So with the following example would have an equal chance to yield either "a" or "b" on each random sample:

For merging more than two arbitraries, Arb.choice or Arb.choose might be more appropriate. For instance we can use:

Bind is useful if you want to apply multiple arbitraries. We can take a look at how we might construct values for a data class using bind.

**Examples:**

Example 1 (kotlin):
```kotlin
val arbA: Arb<A> = ...val a = arbA.next() // use Random.Defaultval a2 = arbA.next(rs) // pass in Random
```

Example 2 (kotlin):
```kotlin
val evens = Arb.int().filter { it.value % 2 == 0 }val odds = Arb.int().filter { it.value % 2 == 1 }
```

Example 3 (kotlin):
```kotlin
val integerStrings: Arb<String> = Arb.int().map { it.toString() }
```

Example 4 (kotlin):
```kotlin
val dependentArbs: Arb<String> = Arb.of("foo", "bar").flatMap { prefix ->   Arb.int(1..10).map { integer ->      "${prefix}-${integer}"   }}
```

---

## Reflective Arbs | Kotest

**URL:** https://kotest.io/docs/5.8.x/proptest/reflective-arbs.html

**Contents:**
- Reflective Arbs

When running tests on JVM, Kotest supports generating more complex Arbs automatically. The generated Arb relies on build-in default and further reflective Arbs to populate the class parameters. If you just need to create and instance and don't need filtering you can use the class type in the checkAll/forAll calls directly. When you want to obtain the Arb to manipulate it further or filter invalid values, you can use Arb.bind with the type argument to obtain the Arb. If the required type depends on types that are not supported by default, it is possible to provide Arbs for those types in the call to Arb.bind.

Reflective binding is supported for:

**Examples:**

Example 1 (kotlin):
```kotlin
enum class Currency {  USD, GBP, EUR}class CurrencyAmount(  val amount: Long,  val currency: Currency)context("Currencies converts to EUR") { // In some spec  checkAll(Arb.bind<CurrencyAmount>().filter { it.currency != EUR }) { currencyAmount ->    val converted = currencyAmount.convertTo(EUR)    converted.currency shouldBe EUR  }}context("Converting to a currency and back yields the same amount") { // In some spec  checkAll<CurrencyAmount, Currency>() { currencyAmount, currency ->    val converted = currencyAmount.convertTo(currency).convertTo(currencyAmount.currency)    converted.currency shouldBe currencyAmount.currency  }}
```

---

## Kotlinx DateTime Gens | Kotest

**URL:** https://kotest.io/docs/6.0/proptest/kotlinx-datetime-gens.html

**Contents:**
- Kotlinx DateTime Gens

Kotest provides an optional module that provides generators for KotlinX DateTime.

To use, add io.kotest.extensions:kotest-property-datetime:version to your build.

---

## Assumptions | Kotest

**URL:** https://kotest.io/docs/next/proptest/property-test-assumptions.html

**Contents:**
- Assumptions
  - Assertions​
  - Max Discard Percentage​

To restrict the set the generated values from a generator, we can use filter to return a constrained arb.

However if we need to restrict inputs based on the relationship between values, then filtering on a single arb won't work. Consider an example where we want to test that two non-equal strings have a non-zero Levenshtein distance.

This will periodically fail - whenever two equal strings are generated. One approach would be to just wrap the tests in an if/else block and avoid those undesired inputs.

But in more complicated scenarios we could easily introduce a bug and filter all our inputs.

Kotest provides a feature called assumptions that will filter out unwanted combinations, while tracking that we are not filtering too many.

An assumption accepts a boolean value passed to the withAssumptions function, that if true, will allow the property test to continue, but if false, that particular iteration is skipped. For example, the previous example will now pass:

Alternatively, you can use inline syntax:

Kotest expands on basic boolean assumptions by allowing you to specify assertions in an assumption function.

For example, building on the previous example:

Here we are ensuring that all inputs are not equal, and that the inputs have the same length. Any assertion that throws AssertionError can be used here, including all the assertions provided by Kotest.

This also supports inline syntax:

By default, the maximum discard percentage is 10%. If more combinations are discarded than that, the property test will fail. This helps avoid a scenario where we erroneously discard too many, or even all, our inputs.

For example, the following would fail by default because we would be filtering ~50% of values.

But if we wanted to allow this regardless, we can use the maxDiscardPercentage to increase the allowed discard rate.

It is generally better to adjust your arbs to produce values closer to what you need, so that you only need to filter out unwanted edge cases.

**Examples:**

Example 1 (kotlin):
```kotlin
val evens = Arb.int().filter { it.value % 2 == 0 }
```

Example 2 (kotlin):
```kotlin
checkAll<String, String> { a, b ->  levenshtein(a, b) shouldBeGreaterThan 0}
```

Example 3 (kotlin):
```kotlin
checkAll<String, String> { a, b ->  if (a != b)    levenshtein(a, b) shouldBeGreaterThan 0}
```

Example 4 (kotlin):
```kotlin
checkAll<String, String> { a, b ->  withAssumptions(a != b) {    levenshtein(a, b) shouldBeGreaterThan 0  }}
```

---

## Custom Generators | Kotest

**URL:** https://kotest.io/docs/6.0/proptest/custom-generators.html

**Contents:**
- Custom Generators
  - Arbitrary​
  - Exhaustive​

To write your own generator for a type T, you just create an instance of Arb<T> or Exhaustive<T>.

When writing a custom arbitrary we can use the arbitrary builder which accepts a lambda that must return the type we are generating for. The parameter to this lambda is a RandomSource parameter which contains the seed and the Random instance. We should typically use the provided RandomSource if we need access to a kotlin.Random instance, as this instance will have been seeded by the framework to allow for repeatable tests.

For example, here is a custom arb that generates a random int between 3 and 6 using the arbitrary builder.

In addition to the RandomSource parameter, the arbitrary builder lambda also provides the ArbitraryBuilderSyntax context which we can leverage to compose other arbitraries when building ours.

For example, here is an Arbitrary that supports a custom class called Person, delegating to a String arbitrary and an Int arbitrary.

The resulting arbitrary produced using this syntax is equivalent to using map, flatMap and bind.

When writing a custom exhaustive we can use the exhaustive() extension function on a List. Nothing more to it than that really!

**Examples:**

Example 1 (kotlin):
```kotlin
val sillyArb = arbitrary { rs: RandomSource ->   rs.random.nextInt(3..6)}
```

Example 2 (kotlin):
```kotlin
data class Person(val name: String, val age: Int)val personArb = arbitrary {   val name = Arb.string(10..12).bind()   val age = Arb.int(21, 150).bind()   Person(name, age)}
```

Example 3 (kotlin):
```kotlin
val singleDigitPrimes = listOf(2,3,5,7).exhaustive()
```

Example 4 (kotlin):
```kotlin
class PropertyExample: FreeSpec({    "testing single digit primes" {        checkAll(singleDigitPrimes) { prime ->           isPrime(prime) shouldBe true           isPrime(prime * prime) shouldBe false        }    }})
```

---

## Property-based Testing | Kotest

**URL:** https://kotest.io/docs/5.7.x/proptest/property-based-testing.html

**Contents:**
- Property-based Testing
- What is Property Testing?​
- Getting Started​
- Next Steps​

Kotest is split into several subprojects which can be used independently. One of these subprojects is the property test framework. You do not need to be using Kotest as your test framework (although you should!) to benefit from the property test support.

Developers typically write example-based tests. These are your garden variety unit tests you know and love. You provide some inputs, and some expected results, and a test framework like Kotest or JUnit checks that the actual results meet the expectations.

One problem with this approach is that it is very easy to miss errors due to edge cases that the developer didn't think about, or lack of coverage in the chosen inputs. Instead, with property testing, hundreds or thousands of values are fed into the same test, and the values are (usually) randomly generated by your property test framework.

For example, a good property test framework will include values like negative infinity, empty lists, strings with non-ascii characters, and so on. Things we often forget about when writing example based tests.

Property tests were originally conceived in frameworks like Quickcheck with the notion of testing a property on some object, ie. something that should hold true for all inputs. An invariant in other words. An example of an invariant is given two strings, a and b, then length(a + b) should always be equal to length(a) + length(b).

That is where the term property testing originates.

However, you do not have to use property tests to only test things like monad laws or basic numeric functions. Any test that would benefit from a wide array of input values is a good candidate. For example, we might have a function that validates usernames, and we want to test that valid emails are accepted. A property test would be useful here in generating 1000s of combinations to help harden our validation logic.

The property test framework is supported on all targets.

Add the following dependency to your build:

Add the following dependency to your build.

Add the following dependency to your commonTest sourceset:

Alternatively, add the dependency to a specific target. For example, we could add to the Javascript target only be using the jsTest sourceset.

To create input values for tests, Kotest uses the term generator. One generator per input argument is passed to a test function, and the test will execute for a set number of iterations.

**Examples:**

Example 1 (kotlin):
```kotlin
dependencies {   testImplementation("io.kotest:kotest-property:$version")}
```

Example 2 (xml):
```xml
<dependency>   <groupId>io.kotest</groupId>   <artifactId>kotest-property-jvm</artifactId>   <version>${version}</version>   <scope>test</scope></dependency>
```

Example 3 (kotlin):
```kotlin
kotlin {  sourceSets {    val commonTest by getting {      dependencies {        implementation("io.kotest:kotest-property:$version")      }    }  }}
```

Example 4 (kotlin):
```kotlin
kotlin {   targets {      js {         browser()         nodejs()      }   }   sourceSets {      val jsTest by getting {         dependencies {            implementation("io.kotest:kotest-property:$version")         }      }   }}
```

---

## Generators List | Kotest

**URL:** https://kotest.io/docs/5.3.x/proptest/property-test-generators-list.html

**Contents:**
- Generators List

This page lists all current generators in Kotest. There are two types of generator: arbitrary and exhaustive.

Most generators are available on all platforms. Some are JVM or JS specific.

We also provide generators for Arrow as a separate module.

---

## Global Configuration | Kotest

**URL:** https://kotest.io/docs/5.3.x/proptest/property-test-global-config.html

**Contents:**
- Global Configuration
  - Default Iterations​
  - Printing Shrink Steps​

Some property test settings can be set globally for all property tests.

The standard default iteration count is 1000. This means when you don't specify the iteration count in a property test, the default will be 1000.

We can override this default either by assigning a value to PropertyTesting.defaultIterationCount, or by using the system property kotest.proptest.default.iteration.count.

Any test which directly sets the iteration count will of course use that value.

By default, when using shrinking, each shrinking step will not be logged, but only the final shrunk value.

To enable logging of each intermediate value, assign true to PropertyTesting.shouldPrintShrinkSteps or use the system property kotest.proptest.output.shrink-steps=true.

**Examples:**

Example 1 (kotlin):
```kotlin
PropertyTesting.defaultIterationCount = 123// will use 555 iterations specified in the testforAll<String, String>(555) { a,b -> a + b == "$a$b" }// will use 123 iterations from the global defaultforAll<String, String> { a,b -> a + b == "$a$b" }
```

---

## Configuration | Kotest

**URL:** https://kotest.io/docs/5.3.x/proptest/property-test-config.html

**Contents:**
- Configuration
- Configuration​
  - Seed​
  - Min Failure​
  - PropTestListener​

Kotest provides for the ability to specify some configuration options when running a property test. We do this by passing in an instance of PropTestConfig to the test methods.

The most common configuration option is specifying the seed for the random instance. This is used when you want to reliably create the same values each time the test is run. You might want to do this if you find a test failure, and you want to ensure that that particular set of values continues to be executed in the future as a kind of regression test.

Whenever a property test fails, Kotest will output the seed that was used. You can duplicate the test, setting it to use this seed so you have permanent regression test for those values.

By default, Kotest tolerates no failure. Perhaps you want to run some non-deterministic test a bunch of times, and you're happy to accept some small number of failures. You can specify that in config.

Sometimes in property test it is required to perform some setup and tear down in each iteration of test. For this purpose you can register a PropTestListener with PropTestConfig.

**Examples:**

Example 1 (kotlin):
```kotlin
class PropertyExample: StringSpec({   "String size" {      forAll<String, String>(PropTestConfig(options here...)) { a,b ->         (a + b).length == a.length + b.length      }   }})
```

Example 2 (kotlin):
```kotlin
class PropertyExample: StringSpec({   "String size" {      forAll<String, String>(PropTestConfig(seed = 127305235)) { a,b ->         (a + b).length == a.length + b.length      }   }})
```

Example 3 (kotlin):
```kotlin
class PropertyExample: StringSpec({   "some flakey test" {      forAll<String, String>(PropTestConfig(maxFailure = 3)) { a,b ->         // max of 3 inputs can fail      }   }})
```

Example 4 (kotlin):
```kotlin
class PropertyExample: StringSpec({   "some property test which require setup and tear down in each iteration" {      forAll<String, String>(PropTestConfig(listeners = listOf(MyPropTestListener))) { a,b ->         // some assertion      }   }})
```

---

## Configuration | Kotest

**URL:** https://kotest.io/docs/next/proptest/property-test-config.html

**Contents:**
- Configuration
  - Seed​
  - Min Failure​
  - PropTestListener​
  - Handling Unprintable Characters in Failure Messages​

Kotest provides for the ability to specify some configuration options when running a property test. We do this by passing in an instance of PropTestConfig to the test methods.

A commonly used configuration option is specifying the seed used by the random source. This is used when you want to repeat the same values each time the test is run. You might want to do this if you find a test failure, and you want to ensure that that particular set of values continues to be executed in the future as a regression test.

For full details on how the seed is used click here.

By default, Kotest tolerates no failure. Perhaps you want to run some non-deterministic test a bunch of times, and you're happy to accept some small number of failures. You can specify that in config.

Sometimes in property test it is required to perform some setup and tear down in each iteration of test. For this purpose you can register a PropTestListener with PropTestConfig.

When property tests involve strings with unprintable characters, failure messages can be hard to read and debug. By setting outputHexForUnprintableChars to true in PropTestConfig, unprintable characters in failure messages are displayed as their Unicode code points in the format U+XXXX.

Default Value: outputHexForUnprintableChars is false by default.

Alternatively, you can set this option as a common project-wide setting in a kotest.properties file located in your classpath:

**Examples:**

Example 1 (kotlin):
```kotlin
class PropertyExample: FreeSpec({   "String size" {      forAll<String, String>(PropTestConfig(options here...)) { a,b ->         (a + b).length == a.length + b.length      }   }})
```

Example 2 (kotlin):
```kotlin
class PropertyExample: FreeSpec({   "some flakey test" {      forAll<String, String>(PropTestConfig(maxFailure = 3)) { a,b ->         // max of 3 inputs can fail      }   }})
```

Example 3 (kotlin):
```kotlin
class PropertyExample: FreeSpec({   "some property test which require setup and tear down in each iteration" {      forAll<String, String>(PropTestConfig(listeners = listOf(MyPropTestListener))) { a,b ->         // some assertion      }   }})
```

Example 4 (kotlin):
```kotlin
class PropertyExample : FreeSpec({    "handle unprintable characters in failure messages" {        forAll<String>(            PropTestConfig(outputHexForUnprintableChars = true)        ) { str ->            // some assertion        }    }})
```

---

## Property Test Functions | Kotest

**URL:** https://kotest.io/docs/next/proptest/property-test-functions.html

**Contents:**
- Property Test Functions
  - For All​
  - Check All​
  - Iterations​
  - Specifying Generators​

There are two variants of functions that are used to execute a property test in Kotest: forAll and checkAll.

The first, forAll, accepts an n-arity function (a, ..., n) -> Boolean that tests the property. The test will pass if, for all input values, the function returns true.

Notice that this functions accepts type parameters for the argument types, with arity up to 14. Kotest uses these type parameters to locate a generator which provides (generates) random values of a suitable type.

For example, forAll<String, Int, Boolean> { a, b, c -> } is a 3-arity property test where argument a is a random String, argument b is a random int, and argument c is a random boolean.

The second, checkAll, accepts an n-arity function (a, ..., n) -> Unit in which you can simply execute assertions against the inputs. This approach will consider a test valid if no exceptions are thrown. Here is the same example again written in the equivalent way using checkAll.

The second approach is more general purpose than returning a boolean, but the first approach is from the original haskell libraries that inspired this library.

By default, Kotest will run the property test 1000 times. We can easily customize this by specifying the iteration count when invoking the test method.

Let's say we want to run a test 10,000 times.

You saw in the previous examples that Kotest would provide values automatically based on the type parameter(s). It does this by locating a generator that generates values for the required type.

For example, the automatically provided Integer generator generates random ints from all possible values - negative, positive, infinities, zero and so on.

This is fine for basic tests but often we want more control over the sample space. For example, we may want to test a function for numbers in a certain range only.

Then you would need to specify the generator(s) manually.

You can see we created two tests and in each test passed a generator into the forAll function with a suitable int range.

See here for a list of the built in generators.

**Examples:**

Example 1 (kotlin):
```kotlin
class PropertyExample: FreeSpec({   "String size" {      forAll<String, String> { a, b ->         (a + b).length == a.length + b.length      }   }})
```

Example 2 (kotlin):
```kotlin
class PropertyExample: FreeSpec({   "String size" {      checkAll<String, String> { a, b ->         a + b shouldHaveLength a.length + b.length      }   }})
```

Example 3 (kotlin):
```kotlin
class PropertyExample: FreeSpec({   "a many iterations test" {      checkAll<Double, Double>(10_000) { a, b ->         // test here      }   }})
```

Example 4 (kotlin):
```kotlin
class PropertyExample: FreeSpec({   "is allowed to drink in Chicago" {      forAll(Arb.int(21..150)) { a ->         isDrinkingAge(a) // assuming some function that calculates if we're old enough to drink      }   }   "is allowed to drink in London" {      forAll(Arb.int(18..150)) { a ->         isDrinkingAge(a) // assuming some function that calculates if we're old enough to drink      }   }})
```

---

## Generators List | Kotest

**URL:** https://kotest.io/docs/6.0/proptest/property-test-generators-list.html

**Contents:**
- Generators List

This page lists all current generators in Kotest. There are two types of generator: arbitrary and exhaustive.

Most generators are available on all platforms. Some are JVM or JS specific.

We also provide generators for Arrow as a separate module.

---

## Generators | Kotest

**URL:** https://kotest.io/docs/5.6.x/proptest/property-test-generators.html

**Contents:**
- Generators
- Arbitrary​
- Exhaustive​

Generated values are provided by instances of the sealed class Gen. You can think of a Gen as kind of like an input stream but for property test values. Each Gen will provide a (usually) infinite stream of these values for one particular type.

Kotest has two types of generators - Arb for generating arbitrary (random) values and Exhaustive for generating a finite set of values in a closed space.

Both types of gens can be mixed and matched in property tests. For example, you could test a function with 100 random positive integers (an arb) alongside every even number from 0 to 200 (exhaustive).

Some generators are only available on the JVM. See the full list here.

Arbs generate two types of values - a hard coded set of edge cases and an infinite stream of randomly chosen samples.

The samples may be repeated, and some values may never be generated at all. For example generating 1000 integers between 0 and Int.MAX cannot return all possible values, and some values may happen to be generated more than once. Similarly, generating 1000 random integers between 0 and 500 will definitely result in some values appearing more than once.

Some common arbitraries include numbers with or without a range, strings in the unicode set, random lists, data classes with random parameters, emails, codepoints, chars and so on.

In addition to the random values, arbs may provide edge cases. One of the design features of Kotest's property testing is that values for some types will always include "common" edge cases that you probably want to be included in your tests.

For example, when testing a function that accepts an integer, you probably want to ensure that, at the very least, it is tested with zero, a positive number and a negative number. If only random values were provided, the chances of zero appearing would be fairly low, so Kotest will always provide some "edge cases" for integers (unless you specify otherwise).

When executing tests, the framework will alternate randomly between samples and edge cases. The split is determined by a configuration value which defaults to 2% edge cases.

Not all arbs have edge cases, but the arbs for the most common types do. Here are some examples of edge cases used by some arbs:

Exhaustives generate all values from a given space. This is useful when you want to ensure every value in that space is used. For example, for enum values, it is usually more helpful to ensure each enum is used, rather than picking randomly from the enums values and potentially missing some and duplicating others.

Typical exhaustives include small collections, enums, boolean values, powerset of a list or set, pre-defined integer ranges, and predefined string ranges.

Once an exhaustive has provided all it's values, it will loop and start again, so an exhaustive can be used in a test that requires any number of inputs.

Here we asked for 100 iterations, so each value of Season would be provided 25 times.

**Examples:**

Example 1 (php):
```php
enum class Season { Winter, Fall, Spring, Summer }forAll<Int, Season>(100) { a, season -> ... }
```

---

## Configuration | Kotest

**URL:** https://kotest.io/docs/5.6.x/proptest/property-test-config.html

**Contents:**
- Configuration
  - Seed​
  - Min Failure​
  - PropTestListener​

Kotest provides for the ability to specify some configuration options when running a property test. We do this by passing in an instance of PropTestConfig to the test methods.

A commonly used configuration option is specifying the seed used by the random source. This is used when you want to repeat the same values each time the test is run. You might want to do this if you find a test failure, and you want to ensure that that particular set of values continues to be executed in the future as a regression test.

For full details on how the seed is used click here.

By default, Kotest tolerates no failure. Perhaps you want to run some non-deterministic test a bunch of times, and you're happy to accept some small number of failures. You can specify that in config.

Sometimes in property test it is required to perform some setup and tear down in each iteration of test. For this purpose you can register a PropTestListener with PropTestConfig.

**Examples:**

Example 1 (kotlin):
```kotlin
class PropertyExample: StringSpec({   "String size" {      forAll<String, String>(PropTestConfig(options here...)) { a,b ->         (a + b).length == a.length + b.length      }   }})
```

Example 2 (kotlin):
```kotlin
class PropertyExample: StringSpec({   "some flakey test" {      forAll<String, String>(PropTestConfig(maxFailure = 3)) { a,b ->         // max of 3 inputs can fail      }   }})
```

Example 3 (kotlin):
```kotlin
class PropertyExample: StringSpec({   "some property test which require setup and tear down in each iteration" {      forAll<String, String>(PropTestConfig(listeners = listOf(MyPropTestListener))) { a,b ->         // some assertion      }   }})
```

---
