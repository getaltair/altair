# Spec Template Gaps

Generated by `/spec-init` on 2026-04-12.
Review and fill these sections before starting implementation.

---

## Cross-Reference Validation Report

```
Assertion Coverage
  [PASS] 7/7 Core P0 requirements (FR-1.1–FR-1.7) have assertions (A-001–A-009)
  [PASS] 6/6 Guidance P0 requirements (FR-2.1–FR-2.6) have assertions (A-010–A-017)
  [PASS] 6/6 Knowledge P0 requirements (FR-3.1–FR-3.6) have assertions (A-018–A-024)
  [PASS] 6/6 Tracking P0 requirements (FR-4.1–FR-4.6) have assertions (A-025–A-031)
  [PASS] 31 total assertions across 4 PRDs

Invariant-to-Assertion Mapping
  [PASS] 15/31 invariants have direct assertion mappings
  [INFO] 16 invariants without assertions — these are infrastructure/process invariants
         verified by CI, code review, or integration tests rather than user-facing assertions:
         S-4 (monotonic checkpoints), S-6 (soft-delete retention),
         SEC-3 (Argon2id — now handled by Zitadel per ADR-006), SEC-4 (signed URLs), SEC-5 (no secrets), SEC-6 (CSRF),
         C-3 (sync stream names), C-4 (versioning rules), C-5 (AI validation),
         E-1 (UUID+timestamps), E-2 (client UUIDs), E-3 (quest scope), E-4 (routine freq),
         D-1 (migration rollback), D-2 (immutable migrations), D-3 (PowerSync parity), D-4 (SQLite parity)

Entity Consistency
  [PASS] 20 entities in domain model match 20 tables in ERD
  [PASS] All domain model relationships reflected in ERD foreign keys or entity_relations

State Machine Coverage
  [PASS] 11 stateful entities have state machine definitions in 06-state-machines.md
  [PASS] All entities with status/state columns covered

Contract Surfaces
  [PASS] 7 contract surface entries in 05-erd.md

Document Index
  [PASS] 18 spec files generated (matches expected count)
```

---

## Sections Needing Human Review

### INFERRED Content (verify accuracy)

- [x] `01-PRD-004-tracking.md` > UC-T-3: Low Stock to Restock Quest — **Deferred to v2.** The automation rule engine is explicitly out of v1 scope (listed as v2 candidate in PLAN). For v1, low stock triggers a notification (ADR-008) but does not auto-create quests. The UC-T-3 sequence diagram is aspirational; the `Rules` actor does not exist in v1.
- [x] `01-PRD-003-knowledge.md` > UC-K-3: Knowledge Capture with OCR — **OCR is P2 (FR-3.12).** The AI pipeline in v1 covers summarization, quest extraction, suggested links, and embedding generation (ADR-004) but not OCR/transcription. The UC-K-3 flow diagram showing OCR is future scope. For v1, image attachments are stored and synced but not text-extracted.
- [ ] `02-domain-model.md` > Enumerations > Initiative Statuses, Quest Statuses — verify these status enums against actual implementation during Step 3
- [x] `05-erd.md` > tracking_items > quantity column — **Resolved.** Changed to NUMERIC per OQ-T-1. Added `barcode` and `expires_at` columns per OQ-T-3 and OQ-T-4.
- [x] `06-state-machines.md` > Epic Status — **Accept as derived state.** Epic status transitions note "Derived state" in the triggers column. Implementation should compute epic status from child quest states rather than storing independently. No explicit `status` column needed on `guidance_epics`; derive at query time.
- [x] `06-state-machines.md` > Focus Session Lifecycle — **Store explicitly.** While states could be derived from `started_at`/`ended_at`, an explicit `status` column is cleaner for sync and querying. The `abandoned` state (cancelled mid-session) is not derivable from timestamps alone — it requires distinguishing "ended early by choice" from "completed." Keep the `status` column.

### Open Questions Requiring Decisions

From PRD-003 (Knowledge):
- [x] OQ-K-1: **Markdown** with structured editor (Tiptap/Milkdown). Raw markdown stored in DB, rendered visually in editor.
- [x] OQ-K-2: **`[[wiki-link]]` syntax** in markdown, rendered as clickable chips. Links stored as `entity_relations`, parsed on save.
- [x] OQ-K-3: **Automatic with debounce** — snapshot after configurable idle period (~5min) or explicit save. Immutable, append-only. Retention cap to manage storage.
- [x] OQ-K-4: **Conflict copies** (ADR-003); CRDT merge deferred to v2.

From PRD-004 (Tracking):
- [x] OQ-T-1: **Decimal** (`NUMERIC` in Postgres). Supports fractional quantities (0.5kg flour, 1.5L milk). Display rounds when appropriate.
- [x] OQ-T-2: **Single event** with `from_location_id` and `to_location_id`. Atomic move avoids inconsistent intermediate state during sync.
- [x] OQ-T-3: **External lookup with graceful degradation**. Online: query external barcode database (Open Food Facts, UPC Database) to auto-fill product info. Offline/no match: fall back to manual entry. Barcode stored as item identifier either way.
- [x] OQ-T-4: **Optional `expires_at` date on item record**. One expiry per item for v1. Batch tracking (multiple expiry dates per item type) deferred.

### Missing Coverage

- [ ] `08-SUB-001-notifications.md` > Email notification channel — not yet designed (P2)
- [ ] `09-PLAT-004-wearos.md` — entire spec is future scope (Tier 3); review when WearOS work begins
- [ ] `07-user-flows.md` > Animations/Haptics — referenced in DESIGN.md but not fully specified per flow
- [ ] `10-PLAN-001-v1.md` > Effort estimates — no time estimates provided (intentional — estimate during sprint planning)

### Architecture Decisions — All Accepted

All 8 ADRs have been formalized in `Context/Decisions/`:
- [x] ADR-001: Defer desktop (Tauri) to v2 — self-hosted S3 eliminates desktop's unique storage value
- [x] ADR-002: Deployment targets — 4GB RAM min (Pi 4), 8GB recommended
- [x] ADR-003: Sync protocol — PowerSync ops-based, LWW + conflict logging, conflict copies for notes
- [x] ADR-004: Search — Postgres FTS baseline + pgvector with external embeddings, no local GPU
- [x] ADR-005: Attachment storage — self-hosted S3-compatible (Garage/RustFS) behind abstraction trait
- [x] ADR-006: Auth — OAuth 2.0 / OIDC via self-hosted Zitadel, JWT access tokens
- [x] ADR-007: Monorepo — apps/{web,android,server}, packages/, infra/
- [x] ADR-008: Notifications — server-owned generation, FCM (Android), SSE (web), client-local fallback for timers

---

## Recommended Next Steps

1. ~~**Create ADRs**~~ — ✅ All 8 ADRs accepted in `Context/Decisions/`
2. ~~**Resolve open questions**~~ — ✅ All 8 OQs resolved (OQ-K-1 through OQ-K-4, OQ-T-1 through OQ-T-4)
3. **Review INFERRED content** — Verify automation rule engine, AI pipeline, and derived-state assumptions
4. **Update ERD** — Apply OQ-T-1 resolution (quantity → NUMERIC) and OQ-T-4 (optional `expires_at`) to `05-erd.md`
5. **Start implementation** — Follow `10-PLAN-001-v1.md` Step 1 (Foundation) to set up monorepo and Docker Compose stack
