# DEPRECATED: This compose file has been superseded by the root compose.yaml.
# The root compose.yaml defines all development infrastructure (Postgres + PowerSync).
#
# To start the database:
#   From project root: docker compose up -d
#   From this directory: bun run db:start
#
# This file is kept for reference only. Do not add services here.
